# YouTube Subtitles Extractor

Простое расширение для браузера Chrome/Edge, которое извлекает субтитры YouTube в фоновом режиме без визуальных эффектов и выводит их в консоль браузера.

## Особенности

- ✅ Получение нативных субтитров YouTube без открытия панели субтитров
- ✅ Работает в фоновом режиме без визуальных эффектов
- ✅ Вывод субтитров в консоль браузера с временными метками
- ✅ Автоматическое определение языка субтитров (приоритет: русский → английский → первый доступный)
- ✅ Простая кнопка, интегрированная в интерфейс YouTube плеера

## Установка

### Шаг 1: Загрузка расширения

Скачайте или склонируйте папку `extension` с файлами:
- `manifest.json`
- `content.js`
- `styles.css`

### Шаг 2: Установка в Chrome/Edge

1. Откройте Chrome и перейдите на страницу `chrome://extensions/` (или `edge://extensions/` для Edge)
2. Включите **Режим разработчика** (Developer mode) в правом верхнем углу
3. Нажмите кнопку **Загрузить распакованное расширение** (Load unpacked)
4. Выберите папку `extension` с файлами расширения
5. Расширение установлено!

### Шаг 3: Добавление иконок (опционально)

Манифест ссылается на иконки:
- `icon16.png` (16x16 пикселей)
- `icon48.png` (48x48 пикселей)
- `icon128.png` (128x128 пикселей)

Вы можете создать свои иконки или временно удалить секцию `"icons"` из `manifest.json`.

## Использование

1. Откройте любое видео на YouTube (`https://www.youtube.com/watch?v=...`)
2. В плеере YouTube появится новая кнопка с иконкой субтитров (слева от других кнопок управления)
3. Откройте консоль браузера:
   - Windows/Linux: `F12` или `Ctrl + Shift + J`
   - Mac: `Cmd + Option + J`
4. Нажмите на кнопку расширения в плеере
5. Субтитры будут загружены и выведены в консоль

## Пример вывода

```
🎬 Начинаем получение субтитров...
Найден трек субтитров: Русский
📡 Загружаем субтитры...
✅ Получено 150 субтитров

================================================================================
[00:00:00.000 --> 00:00:03.500]
Добро пожаловать в это видео
────────────────────────────────────────────────────────────────────────────────
[00:00:03.500 --> 00:00:07.200]
Сегодня мы поговорим о...
────────────────────────────────────────────────────────────────────────────────
...
================================================================================
✅ Все субтитры выведены в консоль
```

## Как это работает

1. **Извлечение данных**: Расширение извлекает объект `ytInitialPlayerResponse` со страницы YouTube, который содержит метаданные о видео, включая URL субтитров
2. **Получение субтитров**: Делается fetch-запрос к API субтитров YouTube для получения XML файла с субтитрами
3. **Парсинг**: XML парсится и преобразуется в читаемый формат с временными метками
4. **Вывод**: Субтитры выводятся в консоль браузера

Всё это происходит в фоновом режиме без открытия панели субтитров или других визуальных эффектов.

## Структура проекта

```
extension/
├── manifest.json       # Манифест расширения
├── content.js          # Основная логика получения субтитров
├── styles.css          # Стили для кнопки
└── README.md          # Документация
```

## Технические детали

- Использует Manifest V3
- Content script инжектится только на страницы `youtube.com/watch*`
- Работает с любыми видео YouTube, у которых есть субтитры
- Поддерживает автоматические и загруженные автором субтитры
- Отслеживает навигацию YouTube (SPA) и автоматически добавляет кнопку на новых страницах

## Возможные улучшения

- Добавить возможность выбора языка субтитров
- Экспорт субтитров в файл (.srt, .vtt, .txt)
- Копирование субтитров в буфер обмена
- Поиск по субтитрам
- Перевод субтитров

## Лицензия

MIT License - используйте как хотите!

## Поддержка

Если у вас возникли проблемы:
1. Проверьте, что у видео есть субтитры (включите их вручную на YouTube)
2. Откройте консоль браузера для просмотра ошибок
3. Убедитесь, что расширение установлено и активно в `chrome://extensions/`
