# История изменений

## [2025-12-08] - REFACTOR: Модульный pipeline для Deep Comments Analysis (AI v2.1)

### Переписана вся архитектура анализа комментариев

#### Превращение монолитного анализа в модульный pipeline
- 🔧 **`src/lib/ai/comments-modules/`** - новая папка с 10 специализированными модулями:
  - `emotional-overview.ts` — эмоциональный обзор комментариев
  - `key-topics.ts` — ключевые темы из комментариев
  - `positive-triggers.ts` — что нравится аудитории
  - `negative-triggers.ts` — что раздражает аудиторию
  - `faq.ts` — часто задаваемые вопросы
  - `audience-segments.ts` — сегментация аудитории
  - `behavioral-insights.ts` — поведенческие паттерны
  - `missing-elements.ts` — чего не хватает
  - `growth-opportunities.ts` — возможности роста
  - `checklist.ts` — чек-лист действий (ровно 8 пунктов)

- 🔧 **`src/lib/ai/comments-orchestrator.ts`** — главный orchestrator:
  - `analyzeCommentsWithOrchestrator()` — запускает все 10 модулей параллельно
  - `validateResult()` — гарантирует что все 10 полей присутствуют
  - `createEmptySafeResult()` — fallback структура при ошибках

- 🔧 **`src/app/api/channel/[id]/comments/ai/route.ts`**:
  - Заменён импорт analyzeChannelComments на analyzeCommentsWithOrchestrator
  - Убран код разбивки на чанки (orchestrator делает это внутри)

### Ключевые преимущества
- **Параллельность**: Все 10 запросов к LLM идут одновременно (быстрее)
- **Специализация**: Каждый промпт оптимизирован под одну задачу (лучшее качество)
- **Модульность**: Легко добавить новый модуль или изменить существующий
- **Надёжность**: Каждый модуль может упасть независимо, остальные работают
- **Retry механизм**: Каждый модуль имеет 3 попытки + fallback значения
- **Полное логирование**: Каждый модуль логирует свою работу отдельно

### Структура каждого модуля
```typescript
function buildPrompt(comments, language): string
function cleanLLMResponse(response): any
async function generateX(comments, language): Promise<result>
  - Retry 3 раза
  - Fallback на пустой результат
  - Полное логирование
```

### Итоговые результаты
- **Без изменения JSON структуры** — результат DeepAnalysisOrchestratorResult содержит те же 10 полей
- **UI остался без изменений** — все компоненты работают как прежде
- **API endpoint не изменился** — POST/GET остаются теми же
- **Таблица БД не изменилась** — данные сохраняются в тот же формат

---

## [2025-12-07] - UX: Все разделы аналитики закрыты по умолчанию

### Улучшено

#### Более удобный первый взгляд на страницу
- 🔧 **`/src/components/channel/ChannelAnalytics.tsx`**:
  - Все разделы (Growth Over Time, Top Videos, Content Intelligence, Momentum Insights, Audience & Engagement, Comment Intelligence, Deep Analysis) теперь закрыты по умолчанию
  - Изменены дефолтные значения `expanded` с `true` на `false`
  - Обновлен комментарий для ясности

### Результат
- **Быстрое восприятие** — пользователь видит полный обзор всех доступных аналитических блоков с первого взгляда
- **Лучшая производительность** — контент загружается только при открытии раздела
- **Меньше информационного шума** — страница выглядит чище и более организованно на первый взгляд
- **Улучшенный UX** — стандартный паттерн для аккордеонов с большим объемом информации
- **Интерактивность** — пользователь может легко переключаться между интересующими его разделами

---

## [2025-12-07] - FIX: Добавлено правильное padding ко всем компонентам аналитики

### Исправлено

#### Восстановление правильного оформления контента
- 🔧 **Все компоненты аналитики** теперь возвращают `CardContent` вместо простого `div`:
  - `CommentInsights.tsx` — возвращает CardContent с классом `space-y-4 pt-6`
  - `ContentIntelligenceBlock.tsx` — возвращает CardContent с классом `space-y-4 pt-6`
  - `MomentumInsights.tsx` — возвращает CardContent с классом `space-y-4 pt-6`
  - `AudienceInsights.tsx` — возвращает CardContent с классом `space-y-4 pt-6`
  - `DeepCommentAnalysis.tsx` — возвращает CardContent с классом `space-y-4 pt-6`

### Результат
- **Элементы больше не прижимаются к границам** — CardContent обеспечивает стандартный padding (p-6)
- **Красивое и аккуратное оформление** — все элементы имеют правильные отступы от краев контейнера
- **Единообразный вид** — все компоненты следуют одному паттерну:
  ```jsx
  <Card>                                        {/* CollapsibleSection */}
    <CardHeader>Заголовок (кликабельный)</CardHeader>
    <CardContent className="space-y-4 pt-6">   {/* Из компонента */}
      {содержимое с правильным padding и spacing}
    </CardContent>
  </Card>
  ```

---

## [2025-12-07] - FIX: Восстановлена исходная структура компонентов с правильным padding

### Исправлено

#### Восстановление красивого оформления
- 🔧 **`/src/components/channel/TopVideosGrid.tsx`**:
  - Теперь возвращает `CardContent` (вместо условного Card > CardContent)
  - CardContent обеспечивает правильный padding для содержимого
  - Grid и другие элементы больше не прижимаются к границам контейнера

- 🔧 **`/src/components/charts/ChannelGrowthChart.tsx`**:
  - Теперь возвращает `CardContent` (вместо условного Card > CardContent)
  - Диаграмма имеет правильные отступы от границ
  - Удалён неиспользуемый параметр `hideHeader`

- 🔧 **`/src/components/channel/ChannelAnalytics.tsx`**:
  - Убраны `hideHeader={true}` из передачи параметров
  - Упрощён код передачи параметров компонентам

### Результат
- **Восстановлена красивая вёрстка** — элементы имеют правильные отступы
- **Никаких элементов, прижатых к границам** — CardContent обеспечивает padding по дефолту
- **Сохранена функция сворачивания** — каждый раздел всё ещё можно свернуть/развернуть
- **Чистая структура DOM**:
  ```jsx
  <Card>                    {/* CollapsibleSection */}
    <CardHeader>Заголовок</CardHeader>
    <CardContent>           {/* Из компонента с правильным padding */}
      {содержимое с отступами}
    </CardContent>
  </Card>
  ```

---

## [2025-12-07] - REFACTOR: Оптимизирована структура DOM для сворачиваемых разделов

### Улучшено

#### Очищена структура компонентов
- 🔧 **`/src/components/channel/TopVideosGrid.tsx`**:
  - Переработано возвращаемое значение в зависимости от `hideHeader`:
  - Когда `hideHeader={true}` — возвращает только `CardContent` (для обёртки CollapsibleSection)
  - Когда `hideHeader={false}` — возвращает полную структуру `Card > CardContent`
  - Исключены ненужные вложенные Card элементы

- 🔧 **`/src/components/charts/ChannelGrowthChart.tsx`**:
  - Добавлен параметр `hideHeader` в интерфейс
  - Переработана логика возвращаемого значения аналогично TopVideosGrid
  - Содержимое диаграммы извлечено в переменную `chartContent`
  - Избегаются ненужные вложения при использовании в CollapsibleSection

- 🔧 **`/src/components/channel/ChannelAnalytics.tsx`**:
  - Добавлено `hideHeader={true}` при передаче `ChannelGrowthChart`
  - Детализирована передача параметров для совместимости с новой логикой

- 🔧 **`/src/components/channel/SWOTAnalysisBlock.tsx`**:
  - Упрощена структура — удалена внешняя `div` обёртка
  - Контент теперь находится напрямую внутри `Card > CardContent`
  - Заголовок содержит переключатель (chevron) для сворачивания
  - При открытии `isOpen=true` отображается `CardContent` с содержимым

### Результат
- **Минимизирована вложенность div** — используются только необходимые элементы
- **Прозрачная структура DOM** — явно видно, что содержимое принадлежит к разделу
- **Консистентный паттерн** — все сворачиваемые разделы имеют одинаковую структуру:
  ```jsx
  <Card>
    <CardHeader>Заголовок + переключатель</CardHeader>
    <CardContent>содержимое</CardContent>  {/* показывается при isOpen=true */}
  </Card>
  ```
- **Улучшена производительность** — меньше ненужных элементов в DOM

---

## [2025-12-07] - FIX: Исправлено дублирование заголовков в сворачиваемых разделах

### Исправлено

#### Удаление дублей заголовков
- 🔧 **`/src/components/charts/ChannelGrowthChart.tsx`**:
  - Удалены значения по умолчанию для title и description
  - CardHeader теперь не отображается если title не передан
  - Позволяет использовать заголовок из CollapsibleSection обёртки

- 🔧 **`/src/components/channel/TopVideosGrid.tsx`**:
  - Добавлен параметр `hideHeader` для управления отображением CardHeader
  - CardHeader скрывается если `hideHeader={true}`

- 🔧 **`/src/components/channel/ChannelAnalytics.tsx`**:
  - Убраны title и description из вызовов компонентов
  - Добавлен `hideHeader={true}` в TopVideosGrid

### Результат
- Больше нет дублирования заголовков (Growth Over Time × 2, Top Videos × 2 и т.д.)
- Каждый раздел имеет один заголовок - из CollapsibleSection обёртки
- Интерфейс чище и понятнее

---

## [2025-12-07] - FEATURE: Добавлена функция сворачивания SWOT-анализа

### Добавлено

#### Новый компонент
- ✨ Создан **`/src/components/channel/SWOTAnalysisBlock.tsx`**:
  - Клиентский компонент для отображения SWOT-анализа с функцией сворачивания
  - Содержит всю логику отображения SWOT матрицы, паттернов и идей
  - Управляется состоянием `isOpen` для контроля видимости содержимого
  - Открыт по умолчанию для удобства пользователя

#### Сворачиваемость SWOT
- 🔄 **`/src/app/(dashboard)/channel/[id]/page.tsx`**:
  - Заменена встроенная логика SWOT на использование компонента `SWOTAnalysisBlock`
  - Весь SWOT раздел (самый большой на странице) теперь сворачивается/разворачивается
  - Клик на заголовок "AI SWOT-анализ канала" открывает/закрывает раздел
  - Иконка Chevron показывает состояние (открыто/закрыто)

### Результат
- **Главный SWOT-анализ теперь сворачивается** - самый большой раздел на странице
- Кнопка "Обновить анализ" видна в заголовке при открытом разделе
- Вся остальная информация страницы доступна без долгого скролла
- Пользователи могут быстро переключаться между SWOT и другими аналитическими блоками

---

## [2025-12-07] - FEATURE: Добавлена функция сворачивания всех основных блоков на странице канала

### Добавлено

#### Сворачиваемые разделы
- 🔄 **`/src/components/channel/ChannelAnalytics.tsx`**:
  - Создана функция `CollapsibleSection()` для обёртки основных блоков
  - Каждый раздел теперь в отдельной Card с функцией сворачивания
  - Добавлено состояние `expanded` для отслеживания открытых/закрытых разделов
  - Все разделы открыты по умолчанию для удобства

#### Сворачиваемые блоки
1. **Growth Over Time** — график роста метрик канала
2. **Top Videos** — таблица топ видео
3. **Content Intelligence** — аналитический отчёт о контент-стратегии
4. **Momentum Insights** — анализ тренда просмотров
5. **Audience & Engagement** — аудитория и вовлечённость
6. **Comment Intelligence** — анализ комментариев
7. **Deep Comment Analysis** — глубокий анализ отзывов (AI v2.0)

#### UX Улучшения
- Клик на заголовок раздела открывает/закрывает содержимое
- Иконки ChevronUp/ChevronDown показывают состояние раздела
- Hover эффект на заголовке для удобства
- Экономия места на странице при закрывании блоков
- Можно быстро найти нужную информацию, скрывая остальное

### Результат
- Страница канала теперь более компактна и удобна в использовании
- Пользователи могут сосредоточиться на интересующей информации
- Нет необходимости долго скролить страницу
- Все разделы видны в нормальном размере без обрезания

---

## [2025-12-07] - FIX: Исправлена работа сворачивания разделов Content Intelligence

### Изменено

#### Совместимость форматов
- 🔄 **`/src/components/channel/ContentIntelligenceBlock.tsx`**:
  - Добавлена функция `convertOldFormatToMarkdown()` для поддержки старого JSON формата
  - Компонент теперь корректно обрабатывает оба формата (новый markdown и старый JSON)
  - При загрузке старого формата автоматически конвертирует в markdown для единообразного отображения
  - Добавлено логирование в консоль для дебага парсинга разделов

#### Дебаг логирование
- 📝 **`/src/app/(dashboard)/channel/[id]/page.tsx`**:
  - Добавлено логирование формата contentData для помощи в отладке
  - Выводит информацию о наличии поля `report` в данных

### Результат
- Content Intelligence теперь корректно отображает сворачиваемые разделы на странице канала
- Поддержка обоих форматов обеспечивает бесперебойную работу со старыми и новыми данными
- Разделы правильно открываются/закрываются при клике на заголовок

---

## [2025-12-07] - REFACTOR: Обновлена система отображения Content Intelligence

### Изменено

#### API endpoint
- 📝 **`/src/app/api/channel/[id]/content-intelligence/route.ts`**:
  - Удалено ограничение `max_tokens: 4000` — GPT сам завершает ответ
  - Объем отчёта теперь полностью контролируется моделью

#### UI Компонент
- 🔄 **`/src/components/channel/ContentIntelligenceBlock.tsx`** — полная переделка:
  - Миграция с JSON на markdown формат ответа
  - Реализована функция `parseSections()` для разбиения markdown на 7 разделов
  - Реализована функция `CollapsibleSection()` для отображения разворачиваемых секций
  - Каждый раздел в отдельной Card с иконками ChevronUp/ChevronDown
  - Первый раздел (РЕЗЮМЕ) открыт по умолчанию для быстрого просмотра
  - Остальные разделы раскрываются по клику на заголовок
  - Поддержка красивого markdown форматирования через react-markdown

#### Зависимости
- ✨ Добавлена `react-markdown` для отображения markdown контента

### Результат
- Content Intelligence теперь более удобен для пользователя — нет необходимости скролить длинный отчёт
- Каждый раздел сворачивается, экономя место на странице
- Возможность быстро навигировать по разделам (открыть/закрыть нужный)
- Улучшена читаемость с поддержкой markdown форматирования (списки, жирный текст, заголовки)
- Первый раздел показывает суть анализа сразу, остальное можно изучить по необходимости

#### Текстовки
Все текстовки переведены на русский язык:
- Заголовки и кнопки на русском
- Описания секций на русском
- Сообщения об ошибках на русском
- Время генерации отображается в формате ru-RU

---

## [2025-12-07] - UPDATE: Переработан промпт для модуля Content Intelligence

### Изменено

#### Промпт для AI анализа
- 📝 **`/src/app/api/channel/[id]/content-intelligence/route.ts`**:
  - Полностью переписан системный промпт с позиционированием как аналитика продукта
  - Заменён формат ответа: JSON → Markdown-отчёт с 7 структурированными разделами
  - Добавлены критические требования: только реальные данные, никаких галлюцинаций
  - Увеличен max_tokens с 1500 на 4000 для полного, развёрнутого анализа
  - Снижена температура с 0.7 на 0.6 для более консервативных выводов

#### Структура нового отчёта (7 разделов)
1. **РЕЗЮМЕ (summary)** - 3-5 предложений с конкретными цифрами
2. **ОСНОВНЫЕ ТЕМЫ (themes)** - 5-10 тем с количеством видео и среднее просмотром
3. **ФОРМАТЫ КОНТЕНТА (formats)** - 4-8 форматов видео с анализом эффективности
4. **ПОВТОРЯЮЩИЕСЯ ПАТТЕРНЫ (patterns)** - 5-8 явных паттернов успешных видео
5. **СЛАБЫЕ СТОРОНЫ (weaknesses)** - анализ неудачных элементов стратегии
6. **ВОЗМОЖНОСТИ (opportunities)** - недозаполненные ниши и потенциал
7. **РЕКОМЕНДАЦИИ (recommendations)** - 5-8 конкретных практичных рекомендаций

#### Обработка ответа
- Обновлена функция POST: теперь сохраняет markdown отчёт вместо JSON
- Обновлена функция GET: добавлена поддержка обоих форматов (новый markdown и старый JSON)
- Добавлено логирование размера ответа для мониторинга

#### Документация
- ✨ Создан файл `CONTENT_INTELLIGENCE_TESTING.md`:
  - Функциональное тестирование API (POST/GET)
  - Проверка качества анализа и отсутствия галлюцинаций
  - Тестирование граничных случаев (мало видео, много видео, однородный контент)
  - Проверка кэширования (7-дневный кэш)
  - Интеграционное тестирование в UI
  - Примеры тестовых команд curl

### Результат
- Content Intelligence теперь предоставляет профессиональный аналитический отчёт вместо краткого анализа
- Все рекомендации привязаны к конкретным паттернам из реальных данных
- Анализ соответствует уровню премиум-консультации (30-50 тыс. рублей)
- Отсутствуют галлюцинации и общие фразы
- Отчёт более структурирован и легче для понимания

---

## [2025-12-07] - UPDATE: Обновлен промпт для GPT-4o-mini SWOT-анализа

### Изменено

#### Промпт для AI анализа
- 📝 **`/src/lib/ai/analyzeChannel.ts`**:
  - Полностью переписан промпт для более глубокого SWOT-анализа
  - Система теперь требует 5-12 пунктов для каждой категории SWOT (вместо 3-7)
  - Каждый пункт должен быть 1-2 насыщенными абзацами (не одна фраза)
  - Добавлены детальные требования для strategicSummary (3-5 абзацев)
  - Расширены требования к contentPatterns (3-7 паттернов с анализом)
  - Увеличены требования к videoIdeas (3-7 идей, а не просто заголовки)

#### Улучшения в качестве анализа
- Акцент на практичность: "что делать", "почему", "какие результаты"
- Требование конкретности: только цифры, паттерны, факты
- Профессиональный стиль консультанта (50-300 тыс. руб за аналитику)
- Запрет на общие фразы типа "важно делать интересный контент"
- Обязательная связь всех рекомендаций с реальными данными канала

#### Системный промпт
- Обновлен системный промпт для лучшего позиционирования модели
- max_tokens увеличен с 4096 до 8000 для полного, развёрнутого ответа

### Результат
- SWOT-анализ стал более комплексным и детальным
- Рекомендации более практичными и привязанными к реальности
- Анализ теперь соответствует стоимости премиум-консультации
- Больше пунктов = больше инсайтов и вариантов стратегии

---

## [2025-12-07] - FIX: Полная система сохранения длительности видео из ScrapeCreators API

### Исправлено

#### Проблема
При генерации SWOT-анализа возникала ошибка:
```
TypeError: Cannot read properties of undefined (reading 'match')
```

**Корневая причина:** Видео загружались из ScrapeCreators API без информации о длительности. API возвращает `lengthSeconds`, но этот параметр никогда не извлекался и не сохранялся в базе данных.

Последствия:
1. Таблица `channel_videos` в БД не имела столбца для duration
2. Интерфейс `VideoData` не содержал поля для длительности
3. При загрузке видео из API `lengthSeconds` игнорировался
4. При анализе каналов код пытался использовать `v.duration` который был `undefined`

#### Решение

**1. Добавлена поддержка lengthSeconds в API интеграцию** (`/src/lib/scrapecreators.ts`):
   - Добавлена функция `secondsToISO8601Duration()` для конвертации секунд → ISO 8601 формат (PT1H2M10S)
   - Обновлен интерфейс `VideoData` — добавлено поле `duration?: string`
   - Обновлена функция `fetchVideosFromAPI()` для извлечения `lengthSeconds` из API ответа и конвертации в ISO 8601

**2. Добавлена схема базы данных** (`/src/lib/db.ts`):
   - Добавлена миграция `ALTER TABLE channel_videos ADD COLUMN duration TEXT`
   - Поле поддерживает NULL значения для видео без информации о длительности

**3. Обновлен API endpoint сохранения видео** (`/src/app/api/channel/[id]/videos/sync/route.ts`):
   - Обновлена операция INSERT для сохранения `duration` при добавлении видео
   - Обновлена операция UPDATE для сохранения `duration` при обновлении видео

**4. Исправлена обработка undefined duration** (`/src/lib/ai/analyzeChannel.ts`):
   - Функция `formatDuration()` теперь безопасно обрабатывает `undefined` значения
   - Добавлена проверка перед использованием `.match()` при расчёте средней длительности

#### Результат
- Длительность видео теперь корректно получается из ScrapeCreators API
- Видео сохраняются с информацией о длительности в формате ISO 8601
- SWOT-анализ работает без ошибок даже с полными данными о длительности
- Все ошибки `Cannot read properties of undefined` устранены
- HTTP 500 при генерации анализа более не возникает

#### Формат данных
- **API:** ScrapeCreators возвращает `lengthSeconds` (число секунд)
- **БД:** Хранится как `duration` в формате ISO 8601 (например: `PT37M25S` = 37 минут 25 секунд)
- **Использование:** Функция `formatDuration()` парсит ISO 8601 и форматирует для отображения (37м 25с)

---

## [2025-12-07] - LANDING: Отключение шаблонных секций лендинга

### Изменено

#### Структура лендинга
- 📝 **`src/app/landing/landing-page-content.tsx`**:
  - Отключены (закомментированы) импорты и рендер нерелевантных секций
  - Компоненты НЕ удалены, только отключен их вывод на странице

#### Отключенные секции
- ❌ **StatsSection** - статистика (шаблонная)
- ❌ **TeamSection** - команда (фейковые данные)
- ❌ **TestimonialsSection** - отзывы (фейковые данные)
- ❌ **BlogSection** - блог (шаблонный контент)
- ❌ **PricingSection** - тарифы (будет отдельная страница)
- ❌ **FaqSection** - FAQ (будет отдельная страница в dashboard)
- ❌ **ContactSection** - контакты (шаблонный контент)

#### Активные секции (сохранены)
- ✅ **HeroSection** - главный баннер
- ✅ **LogoCarousel** - карусель логотипов (под замену изображений)
- ✅ **AboutSection** - о продукте
- ✅ **FeaturesSection** - функции продукта
- ✅ **CTASection** - призыв к действию

### Детали изменений
- Все отключенные компоненты закомментированы, не удалены
- Можно легко вернуть любую секцию, раскомментировав импорт и рендер
- Структура лендинга упрощена до релевантных секций

---

## [2025-12-07] - BRAND: Замена брендинга ShadcnStore на Beem Analytics

### Изменено

#### Компоненты футеров
- 📝 **`src/components/site-footer.tsx`**:
  - Заменено "Made with ❤️ by ShadcnStore Team" на "Made with ❤️ by Beem Analytics"
  - Удалена внешняя ссылка на shadcnstore.com
  - Сохранен формат с иконкой сердца

- 📝 **`src/app/landing/components/footer.tsx`**:
  - Заменен брендовый блок: "ShadcnStore" → "Beem Analytics"
  - Заменен нижний футер: "Made with ❤️ by ShadcnStore" → "Made with ❤️ by Beem Analytics"
  - Удалены ссылки на shadcnstore.com (2 места)
  - Удалена ссылка на GitHub шаблон (https://github.com/silicondeck/shadcn-dashboard-landing-template)

#### Метаданные страниц
- 📝 **`src/app/(auth)/layout.tsx`**:
  - Title: "Authentication - ShadcnStore" → "Authentication - Beem Analytics"

- 📝 **`src/app/landing/page.tsx`**:
  - Title: "ShadcnStore - Modern Admin Dashboard Template" → "Beem Analytics - YouTube Competitors Analytics Platform"
  - Description: переписан под реальный продукт YouTube Analytics
  - Keywords: обновлены с admin dashboard на youtube analytics, competitor analysis, content strategy, ai analysis
  - OpenGraph и Twitter Card метаданные синхронизированы

### Детали изменений
- Все ссылки на внешние ресурсы шаблона удалены
- Брендинг обновлен в 4 файлах
- Сохранен формат "Made with ❤️" во всех футерах
- Метаданные адаптированы под реальный продукт

---

## [2025-12-07] - CONTENT: Полная переработка FAQ под YouTube Competitors Analytics

### Изменено

#### FAQ файлы
- 📝 **`faq-structure.json`**:
  - Полностью переписаны все 46 вопросов и ответов на русском языке
  - Сохранена структура: 7 категорий (General, Account, Billing, Technical, Privacy, Security, Support)
  - Все вопросы адаптированы под реальный продукт YouTube Competitors Analytics
  - Фокус на пошаговые сценарии работы с сервисом

- 📝 **`src/app/(dashboard)/faqs/data/faqs.json`**:
  - Синхронизирован с faq-structure.json (идентичное содержимое)

#### Основные блоки вопросов

**General (8 вопросов):**
- Что такое YouTube Competitors Analytics
- Как начать работу шаг за шагом
- Как добавить конкурента
- Как работает Sync All Data
- Как пользоваться AI-блоками (Content Intelligence, Momentum Insights, Deep Comment Analysis)
- Как использовать Trending

**Account (6 вопросов):**
- Управление паролем через Google OAuth
- Смена email/аккаунта
- Приглашение команды (в разработке)
- Удаление аккаунта
- Смена языка интерфейса
- Двухфакторная аутентификация

**Billing (8 вопросов):**
- Способы оплаты (в разработке)
- Повышение/понижение тарифа
- Возврат средств (30 дней)
- Биллинг и автоплатежи
- Месячная vs годовая оплата
- Неуспешные платежи
- История платежей

**Technical (9 вопросов):**
- Экспорт данных в PDF
- API для интеграций (в разработке)
- Резервные копии
- Лимиты синхронизации (6 часов метрики, 7 дней комментарии)
- Кэш AI-анализов (3-7 дней)
- Ошибки синхронизации
- Ошибка Insufficient Credits от ScrapeCreators
- Кастомные домены для Enterprise
- Интеграция с внешними БД

**Privacy (5 вопросов):**
- Обработка персональных данных (GDPR)
- Запрос копии данных
- Хранение данных после удаления
- Cookies
- Шифрование данных (TLS 1.3, AES-256)

**Security (4 вопроса):**
- Безопасность данных
- SSO для Enterprise (в планах)
- Требования к паролю (через Google)
- Реагирование на инциденты

**Support (6 вопросов):**
- Каналы поддержки (email, чат и Telegram в планах)
- Время работы поддержки (10:00-19:00 МСК)
- Скорость ответа по тарифам
- Обучающие материалы
- Кастомные внедрения для Enterprise
- Форум/сообщество (в планах)

#### Стиль и подход
- Деловой, но человеческий язык
- Фокус на реальный user flow: регистрация → добавление конкурентов → синхронизация → AI-анализ → отчёты
- Конкретные инструкции "что нажать, если я хочу..."
- Честность о функциях в разработке
- Практичные ответы без маркетинговой воды

#### Что НЕ изменялось
- ✅ Структура JSON
- ✅ Количество вопросов (46)
- ✅ ID элементов (1-46)
- ✅ Названия категорий
- ✅ Формат полей (id, question, answer, category)

---

## [2025-12-07] - UPDATE: Обновление текстов тарифных планов

### Изменено

#### Компоненты UI
- 📝 **`/src/components/pricing-plans.tsx`**:
  - Обновлены текстовые описания (description) для всех трёх тарифов на русский язык
  - Обновлены списки возможностей (features) согласно новым требованиям продукта
  - Basic: описание изменено на "Для начинающих авторов", обновлены 6 пунктов возможностей
  - Professional: описание изменено на "Для растущих каналов", обновлены 7 пунктов возможностей
  - Enterprise: описание изменено на "Для профессиональных команд", обновлены 7 пунктов возможностей

#### Что НЕ изменялось
- ✅ Структура массива тарифов (3 элемента)
- ✅ Идентификаторы тарифов (id)
- ✅ Названия тарифов (name)
- ✅ Цены (price) и периодичность (frequency)
- ✅ Флаг популярности (popular)
- ✅ Типы, интерфейсы и логика компонента
- ✅ Функционал биллинга и проверки лимитов

#### Примечание
Тариф FREE отображается в другом месте интерфейса и не был затронут этими изменениями.

---

## [2025-12-05] - ВРЕМЕННОЕ ОТКЛЮЧЕНИЕ: Ограничения синхронизации метрик

### Временно отключено для разработки

#### Изменения
- 🔧 **`/src/app/api/channel/[id]/sync/route.ts`**:
  - Закомментирована проверка частоты синхронизации (раз в 6 часов) - строки 55-65
  - Закомментировано ограничение на максимальное количество синхронизаций в день (10) - строки 89-98
  - Добавлены комментарии "ОТКЛЮЧЕНО для разработки"

#### Важное примечание
⚠️ **Эти ограничения необходимо ОБЯЗАТЕЛЬНО включить перед запуском проекта в продакшн!**

**Причины для включения ограничений:**
1. **Защита от злоупотреблений** - предотвращение чрезмерного использования API
2. **Экономия кредитов ScrapeCreators** - ограничение количества запросов
3. **Предотвращение спама** - защита от автоматических частых синхронизаций
4. **Соответствие лимитам API** - соблюдение rate limits внешних сервисов

**Как включить обратно:**
1. Раскомментировать блок проверки частоты синхронизации (6 часов)
2. Раскомментировать блок ограничения синхронизаций в день (10)
3. Удалить комментарии "ОТКЛЮЧЕНО для разработки"

#### Статус
✅ **Изменения временные** - только для этапа разработки и тестирования
🚫 **Не для продакшена** - обязательно восстановить перед деплоем

---

## [2025-12-02] - FIX: Обработка ошибки отсутствия кредитов ScrapeCreators

### Исправлено

#### Проблема
При синхронизации комментариев, когда заканчивались кредиты ScrapeCreators API (ошибка 402), код продолжал делать запросы для остальных видео, что приводило к:
- Множественным бесполезным запросам к API
- Увеличению времени синхронизации
- Неинформативным сообщениям об ошибке

#### Решение
- 🐛 **`/src/lib/scrapecreators.ts`**:
  - Добавлена специальная обработка HTTP 402 ошибки
  - Ошибка помечается кодом `INSUFFICIENT_CREDITS` для идентификации
  - Добавлено понятное сообщение на русском языке

- 🐛 **`/src/app/api/channel/[id]/comments/sync/route.ts`**:
  - Добавлена проверка на ошибку `INSUFFICIENT_CREDITS` в цикле синхронизации
  - При обнаружении ошибки 402 синхронизация немедленно прекращается
  - Возвращается ответ с флагом `insufficientCredits: true`
  - Сохраняются частично синхронизированные данные
  - В ответе указывается сколько видео успели синхронизировать

- 🐛 **`/src/components/channel/SyncCommentsButton.tsx`**:
  - Добавлена специальная обработка HTTP 402 ответа
  - При частичной синхронизации показывается `toast.warning` с информацией о синхронизированных видео
  - При полной неудаче показывается `toast.error` с информацией о необходимости пополнить кредиты
  - Страница обновляется даже при частичной синхронизации для отображения полученных данных
  - Увеличена длительность отображения toast до 8 секунд

#### Результат
- Синхронизация прекращается сразу при ошибке 402
- Пользователь получает понятное сообщение о необходимости пополнить баланс
- Частично синхронизированные данные сохраняются и отображаются
- Не тратятся лишние запросы к API

---

## [2025-12-02] - ЭТАП 4.6: Comment Intelligence Integration

### Добавлено

#### UI Компоненты
- ✨ Создан `/src/components/channel/SyncCommentsButton.tsx`:
  - Кнопка синхронизации комментариев для топ видео канала
  - Loading состояние с анимацией при синхронизации
  - Toast уведомления с результатами синхронизации
  - Автоматическое обновление страницы после завершения

#### Утилиты для предобработки
- ✨ Создан `/src/lib/commentPreprocessing.ts`:
  - `prepareCommentForAI()` - основная функция очистки текста комментария
  - `prepareCommentsForAI()` - обработка массива комментариев
  - `extractCleanedTexts()` - извлечение очищенных текстов с фильтрацией по длине
  - Удаление URL, emoji, timestamp меток, специальных символов
  - Нормализация пробелов и повторяющихся символов

#### Интеграция в UI
- ✨ Обновлён `/src/app/(dashboard)/channel/[id]/page.tsx`:
  - Добавлен импорт `commentInsights` из `@/lib/db`
  - Добавлен импорт `SyncCommentsButton` и `CommentInsights` компонентов
  - Добавлена загрузка данных `commentInsights` из БД
  - Добавлена кнопка "Sync Comments" в header канала
  - Интегрирован компонент `CommentInsights` в секцию аналитики

### Технические детали

#### Что уже было реализовано ранее
- ✅ Таблица `video_comments` в `/src/lib/db.ts` (создана ранее)
- ✅ Таблица `comment_insights` в `/src/lib/db.ts` (создана ранее)
- ✅ API endpoint `/src/app/api/channel/[id]/comments/sync/route.ts` (создан ранее)
- ✅ API endpoint `/src/app/api/channel/[id]/comments/insights/route.ts` (создан ранее)
- ✅ Функция `getYoutubeVideoComments()` в `/src/lib/scrapecreators.ts` (создана ранее)
- ✅ Компонент `/src/components/channel/CommentInsights.tsx` (создан ранее)

#### Что было добавлено в этом этапе
**Созданные файлы (2):**
1. `/src/components/channel/SyncCommentsButton.tsx` - кнопка синхронизации комментариев
2. `/src/lib/commentPreprocessing.ts` - утилиты предобработки текста

**Изменённые файлы (1):**
3. `/src/app/(dashboard)/channel/[id]/page.tsx` - интеграция CommentInsights и кнопки синхронизации

#### Как работает синхронизация комментариев
1. Пользователь нажимает кнопку "Sync Comments"
2. API `/api/channel/[id]/comments/sync` получает топ 15 видео канала
3. Для каждого видео загружается до 300 комментариев через ScrapeCreators API
4. Комментарии сохраняются в таблицу `video_comments` (upsert логика)
5. После синхронизации пользователь может сгенерировать AI-анализ через кнопку в CommentInsights
6. API `/api/channel/[id]/comments/insights` анализирует топ 200 комментариев через OpenAI GPT-4o-mini
7. Результат сохраняется в `comment_insights` и отображается в UI

#### НЕ изменялись
- ✅ Существующие API endpoints
- ✅ База данных schemas
- ✅ ScrapeCreators интеграция
- ✅ Другие компоненты аналитики (Content Intelligence, Momentum, Audience)
- ✅ UI компоненты shadcn/ui
- ✅ Стили и Tailwind классы

### Следующие этапы

**ЭТАП 5** (планируется):
- Интеграция платёжных систем (Stripe/YooKassa)
- Обновление тарифов пользователей
- Биллинг и подписки

---

## [2025-12-01] - ЭТАП 4.0.4: Top Videos Analytics

### Добавлено

#### База данных
- ✨ Создана таблица `channel_videos` в `/src/lib/db.ts`:
  - `id` (INTEGER, PRIMARY KEY, AUTOINCREMENT)
  - `channelId` (TEXT) - ID канала из ScrapeCreators
  - `videoId` (TEXT) - YouTube video ID
  - `title` (TEXT) - название видео
  - `thumbnailUrl` (TEXT) - URL миниатюры
  - `viewCount` (INTEGER) - количество просмотров
  - `publishedAt` (TEXT) - дата публикации (ISO8601)
  - `fetchedAt` (INTEGER, timestamp) - время получения данных
- ✨ Создан индекс `idx_channel_videos_lookup` для быстрого поиска видео по каналу и videoId

#### ScrapeCreators интеграция
- ✨ Добавлен интерфейс `VideoData` в `/src/lib/scrapecreators.ts`
- ✨ Создана функция `getYoutubeChannelVideos(channelId: string)` - получение списка видео канала
- ✨ Создана функция `extractThumbnailUrl()` - извлечение URL миниатюры из разных форматов API
- ✨ Нормализация данных видео с маппингом в формат `VideoData`

#### API endpoints
- ✨ Создан `/src/app/api/channel/[id]/videos/sync/route.ts`:
  - **POST** - синхронизация топ видео канала:
    - Проверка доступа пользователя к каналу
    - Fetch списка видео через ScrapeCreators API
    - Проверка существующих видео (upsert логика)
    - Обновление существующих видео или вставка новых
    - Возврат статистики: количество добавленных и обновленных видео

#### UI компоненты
- ✨ Создан `/src/components/channel/TopVideosTable.tsx`:
  - Таблица с колонками: превью, название, просмотры, дата публикации, ссылка
  - Сортировка по viewCount DESC
  - Форматирование чисел (1.2M, 532K)
  - Форматирование дат (dd MMM yyyy)
  - Ссылки на видео в YouTube
  - Empty state для пустого списка
- ✨ Создан `/src/components/channel/SyncVideosButton.tsx`:
  - Кнопка синхронизации видео
  - Loading состояние с анимацией
  - Toast уведомления о результате синхронизации
  - Автоматическое обновление страницы после синхронизации

#### Страница канала
- ✨ Обновлён `/src/app/(dashboard)/channel/[id]/page.tsx`:
  - Добавлен импорт `channelVideos` из `@/lib/db`
  - Добавлены импорты `SyncVideosButton` и `TopVideosTable`
  - Добавлена выборка топ видео из БД с сортировкой по просмотрам
  - Заменён PlaceholderSection "Top Videos" на реальный компонент `TopVideosTable`
  - Добавлена кнопка "Sync Top Videos" рядом с кнопкой синхронизации метрик

### Изменено
- 🔄 Кнопки синхронизации метрик и видео объединены в один flex контейнер
- 🔄 API endpoint использует upsert логику для обновления существующих видео

### Технические детали

#### Файлы, которые были созданы/изменены
**Созданные (3 файла):**
1. `/src/app/api/channel/[id]/videos/sync/route.ts` - API endpoint для синхронизации видео
2. `/src/components/channel/TopVideosTable.tsx` - таблица топ видео
3. `/src/components/channel/SyncVideosButton.tsx` - кнопка синхронизации видео

**Изменённые (3 файла):**
4. `/src/lib/db.ts` - добавлена таблица channel_videos
5. `/src/lib/scrapecreators.ts` - добавлена функция getYoutubeChannelVideos()
6. `/src/app/(dashboard)/channel/[id]/page.tsx` - интеграция Top Videos

#### НЕ изменялись
- ✅ Существующие API endpoints
- ✅ Авторизация и сессии
- ✅ Competitors функционал
- ✅ AI Insights
- ✅ Channel Metrics и графики
- ✅ UI компоненты shadcn/ui
- ✅ Стили и Tailwind классы

### Следующие этапы

**ЭТАП 4.0.5** (планируется):
- AI-анализ контента видео
- Выявление паттернов успешных видео
- Рекомендации по контент-стратегии

**ЭТАП 5** (планируется):
- Интеграция платёжных систем (Stripe/YooKassa)
- Обновление плана пользователя
- Биллинг и подписки

---

## [2025-11-28] - FIX: Замена better-sqlite3 на @libsql/client для кроссплатформенности

### Исправлено
- 🐛 **Ошибка на Windows**: Заменён `better-sqlite3` на `@libsql/client`
  - `better-sqlite3` требует нативной компиляции и не работал на Windows
  - `@libsql/client` - кроссплатформенное решение без нативных зависимостей
  - Поддержка как локальных файлов SQLite, так и удалённых баз данных

### Изменено
#### Зависимости
- ❌ Удалено: `better-sqlite3` (v11.8.1)
- ❌ Удалено: `@types/better-sqlite3` (v7.6.12)
- ✅ Добавлено: `@libsql/client` (v0.15.15)

#### База данных
- 🔄 Обновлён `/src/lib/db.ts`:
  - Заменён импорт: `import Database from "better-sqlite3"` → `import { createClient } from "@libsql/client"`
  - Обновлена инициализация клиента: использование `createClient({ url: "file:..." })`
  - Заменён метод выполнения SQL: `sqlite.exec()` → `_client.execute()`
  - Все комментарии переведены на русский язык

#### Совместимость
- ✅ Работает на **Windows, macOS, Linux** без дополнительных зависимостей
- ✅ Не требует Visual Studio Build Tools на Windows
- ✅ Не требует нативной компиляции

### Технические детали
- Формат URL для локальных файлов: `file:sqlite.db` или `file:path/to/database.db`
- Поддержка удалённых баз данных через `libsql://` протокол (Turso)

---

## [2025-11-28] - ЭТАП 3: Интеграция ScrapeCreators и функционал Competitors

### Добавлено

#### Зависимости
- Нет новых зависимостей (используется встроенный `fetch`)

#### База данных
- ✨ Создана таблица `competitors` в `/src/lib/db.ts`:
  - `id` (INTEGER, PRIMARY KEY, AUTOINCREMENT)
  - `userId` (TEXT, FOREIGN KEY → users.id)
  - `platform` (TEXT, default "youtube")
  - `channelId` (TEXT) - YouTube channel ID
  - `handle` (TEXT) - введённый handle или URL
  - `title` (TEXT) - название канала
  - `avatarUrl` (TEXT) - URL аватара канала
  - `subscriberCount` (INTEGER) - количество подписчиков
  - `videoCount` (INTEGER) - количество видео
  - `viewCount` (INTEGER) - общее количество просмотров
  - `lastSyncedAt` (INTEGER, timestamp) - время последней синхронизации
  - `createdAt` (INTEGER, timestamp) - время добавления
- ✨ Автоматическая инициализация таблицы при первом запуске

#### ScrapeCreators интеграция
- ✨ Создан `/src/lib/scrapecreators.ts` - клиент для ScrapeCreators API
  - Функция `getYoutubeChannelByHandle(handle: string)` - получение данных канала
  - Обработка ошибок API (400, 401, 404, 429, 500)
  - Очистка handle от символов @ и URL частей
  - Маппинг данных в формат `ChannelData`

#### API endpoints
- ✨ Создан `/src/app/api/competitors/route.ts`:
  - **GET** - получение списка конкурентов текущего пользователя
  - **POST** - добавление нового конкурента:
    - Валидация handle
    - Проверка лимита по тарифу (free/basic: 3, professional: 20, enterprise: 200)
    - Fetch данных через ScrapeCreators
    - Проверка на дубликаты
    - Сохранение в БД
- ✨ Создан `/src/app/api/competitors/[id]/route.ts`:
  - **DELETE** - удаление конкурента (с проверкой владельца)

#### Страница /competitors
- ✨ Полностью переработан `/src/app/(dashboard)/competitors/page.tsx`:
  - Форма добавления конкурента (Input + Button)
  - Отображение лимита: "X/Y used"
  - Блокировка добавления при достижении лимита
  - Таблица с конкурентами:
    - Колонки: Channel, Subscribers, Videos, Total Views, Last Synced, Actions
    - Аватар и название канала
    - Форматирование чисел (K, M, B)
    - Кнопка удаления
  - Alert'ы для ошибок и успешных действий
  - Loading состояния
  - Empty state для пустого списка

#### Dashboard агрегация
- ✨ Обновлён `/src/app/(dashboard)/dashboard/page.tsx`:
  - Сделан серверным компонентом (async)
  - Загрузка списка конкурентов из БД
  - Подсчёт агрегированных метрик:
    - `totalCompetitors` - количество отслеживаемых каналов
    -
