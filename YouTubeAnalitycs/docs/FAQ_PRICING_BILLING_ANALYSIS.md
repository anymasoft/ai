# 📊 ПОЛНЫЙ АНАЛИЗ FAQ / PRICING / BILLING АРХИТЕКТУРЫ

**Статус анализа:** ✅ ЗАВЕРШЁН  
**Дата анализа:** 06.12.2025  
**Объект анализа:** Дублирование и взаимосвязи между FAQ, Pricing, Billing страницами  

---

## 1. СТРУКТУРА ВСЕХ КОМПОНЕНТОВ И СТРАНИЦ

### 1.1. FAQ СТРАНИЦА (/faqs)
```
/faqs/page.tsx
├── FAQList компонент
│   ├── Input: faqsData (/faqs/data/faqs.json) - 46 ВОПРОСОВ
│   ├── Input: categoriesData (/faqs/data/categories.json) - 8 категорий
│   └── Features: поиск, фильтрация по категориям, аккордион
│
└── FeaturesGrid компонент
    └── Input: featuresData (/faqs/data/features.json) - 4 features
```

**Данные FAQ страницы:**
- `faqs.json`: 46 вопросов в 8 категориях (General, Account, Billing, Technical, Privacy, Security, Support)
- `features.json`: 4 features (Premium Quality, Secure Shopping, Fast Delivery, 24/7 Support) ← E-COMMERCE контент!
- `categories.json`: Структура категорий с количеством вопросов

**Визуальное представление:**
```
┌─────────────────────────────────────────┐
│           /faqs СТРАНИЦА                │
├─────────────────┬───────────────────────┤
│  Categories     │  FAQ Accordion         │
│  Sidebar        │  (46 вопросов)        │
│  (8 категорий)  │  + Search             │
└─────────────────┴───────────────────────┘
                   ↓
          Features Grid (4 items)
```

---

### 1.2. PRICING СТРАНИЦА (/pricing)
```
/pricing/page.tsx
├── PricingPlans компонент (mode="pricing")
│   └── Hardcoded plans (Basic $19, Professional $79, Enterprise $199)
│
├── FeaturesGrid компонент
│   └── Input: featuresData (/pricing/data/features.json) - 6 features
│       (Fast Performance, Enterprise Security, Instant Setup, Team Collaboration, 24/7 Support, Real-time Analytics)
│
└── FAQSection компонент
    └── Input: faqsData (/pricing/data/faqs.json) - ТОЛЬКО 6 ВОПРОСОВ!
        (Отобранные вопросы о плане и биллинге в 2-колоночном макете)
```

**Данные Pricing страницы:**
- Pricing cards: Hardcoded в /components/pricing-plans.tsx (3 плана YouTube Analytics)
- `features.json`: 6 features (для YouTube Analytics SaaS) ← РАЗНЫЕ от FAQs features!
- `faqs.json`: 6 вопросов (ПОДМНОЖЕСТВО полного FAQ)

**Визуальное представление:**
```
┌──────────────────────────────────────────┐
│      /pricing СТРАНИЦА                   │
├──────────────────────────────────────────┤
│   Pricing Cards (Basic/Pro/Enterprise)   │
├──────────────────────────────────────────┤
│   Features Grid (6 features)             │
├──────────────────┬──────────────────────┤
│  FAQ Left Col    │  FAQ Right Col       │
│  (3 questions)   │  (3 questions)       │
│  ← ДУБЛИРУЕТ FAQ │  ← Из /pricing/faqs  │
└──────────────────┴──────────────────────┘
```

---

### 1.3. BILLING / SETTINGS СТРАНИЦА (/settings/billing)
```
/settings/billing/page.tsx
├── CurrentPlanCard компонент
│   └── Input: currentPlanData (/settings/billing/data/current-plan.json)
│       (Показывает: Professional Plan, $79/month, статус)
│
├── BillingHistoryCard компонент
│   └── Input: billingHistoryData (/settings/billing/data/billing-history.json)
│
└── PricingPlans компонент (mode="billing")
    └── Hardcoded plans (Basic $19, Professional $79, Enterprise $199)
        (Позволяет upgrade/downgrade)
```

**Данные Billing страницы:**
- `current-plan.json`: Текущий план пользователя (Professional, $79/month)
- `billing-history.json`: История платежей
- Pricing Plans: Hardcoded, те же 3 плана что и на /pricing

**Визуальное представление:**
```
┌──────────────────────────────────────────┐
│   /settings/billing СТРАНИЦА             │
├──────────────────┬──────────────────────┤
│ Current Plan     │ Billing History      │
│ Card             │ Card                 │
└──────────────────┴──────────────────────┘
         ↓
┌──────────────────────────────────────────┐
│  Pricing Plans (для upgrade/downgrade)   │
│  (Basic/Pro/Enterprise)                  │
└──────────────────────────────────────────┘
```

---

### 1.4. LANDING СТРАНИЦА (pricing-section)
```
/landing/components/pricing-section.tsx
└── HARDCODED 3 РАЗНЫХ ПЛАНА:
    - Free (0$/month)
    - Pro ($19/month или $15/year)
    - Lifetime ($299 one-time)
    
    ⚠️ ПОЛНОСТЬЮ ОТЛИЧАЕТСЯ от планов на /pricing и /billing!
```

**Визуальное представление:**
```
┌──────────────────────────────────────────┐
│   LANDING PRICING SECTION                │
├──────────────────────────────────────────┤
│  [Monthly / Annually Toggle]             │
├──────────┬──────────────┬────────────────┤
│ Free     │ Pro ★        │ Lifetime       │
│ $0       │ $19/$15 ★    │ $299           │
└──────────┴──────────────┴────────────────┘
```

---

## 2. ТАБЛИЦА ДУБЛИРОВАНИЯ

| Элемент | Количество копий | Где находится | Тип дублирования |
|---------|------------------|---------------|-----------------|
| **FAQ Данные** | 2 | /faqs/data/faqs.json (46) + /pricing/data/faqs.json (6) | ⚠️ ЧАСТИЧНОЕ ДУБЛИРОВАНИЕ |
| **Features Данные** | 2 | /faqs/data/features.json (4) + /pricing/data/features.json (6) | ✓ OK (разные контексты) |
| **Pricing Plans** | 2 | pricing-plans.tsx (YT Analytics) + landing/pricing-section.tsx (Components) | 🔴 ПОЛНОЕ НЕСООТВЕТСТВИЕ |
| **Pricing Cards Компонент** | 1 | /components/pricing-plans.tsx | ✓ OK (переиспользуется) |
| **FAQ Компонент для отображения** | 2 | FAQList (полный) + FAQSection (сокращённый) | ✓ OK (разные варианты) |

---

## 3. ДЕТАЛЬНОЕ ОПИСАНИЕ ДУБЛИРОВАНИЙ

### 🔴 КРИТИЧЕСКОЕ ДУБЛИРОВАНИЕ 1: FAQ ДАННЫЕ

**ПРОБЛЕМА:** FAQ в /pricing/data/faqs.json это КОПИЯ 6 вопросов из /faqs/data/faqs.json

**Доказательство:**
```
/faqs/data/faqs.json - вопрос #15:
"What payment methods do you accept?"
"We accept all major credit cards..."
category: "Billing"

/pricing/data/faqs.json - вопрос #3:
"What payment methods do you accept?"
"We accept all major credit cards..."
← ТОЧНАЯ КОПИЯ!
```

**Масштаб:**
- /pricing/data/faqs.json содержит 6 вопросов
- 6 из 6 вопросов дублируют вопросы из /faqs/data/faqs.json
- Когда вопрос обновляется в одном месте, другое СРАЗУ становится устаревшим

**Последствия:**
- Если нужно изменить FAQ, нужно менять в 2 местах
- Риск несоответствия между версиями
- При добавлении нового вопроса, нужно помнить про две копии

---

### 🔴 КРИТИЧЕСКОЕ ДУБЛИРОВАНИЕ 2: PRICING PLANS

**ПРОБЛЕМА:** 3 РАЗНЫЕ ВЕРСИИ PRICING в разных местах!

**Версия 1 - /components/pricing-plans.tsx (YouTube Analytics):**
```typescript
const defaultPlans = [
  {
    id: 'basic',
    name: 'Basic',
    description: 'Perfect for small creators',
    price: '$19',
    features: ['Track up to 3 competitors', 'Basic analytics', ...],
  },
  {
    id: 'professional',
    name: 'Professional',
    price: '$79',
    features: ['Track up to 20 competitors', 'Advanced analytics', ...],
    popular: true,
  },
  {
    id: 'enterprise',
    name: 'Enterprise',
    price: '$199',
    features: ['Track up to 200 competitors', ...],
  }
]
```

**Версия 2 - /landing/components/pricing-section.tsx (Component Library):**
```typescript
const plans = [
  {
    name: 'Free',
    monthlyPrice: 0,
    features: ['Access to 50+ free components', ...],
  },
  {
    name: 'Pro',
    monthlyPrice: 19,
    features: ['Premium template collection', ...],
    popular: true,
  },
  {
    name: 'Lifetime',
    monthlyPrice: 299,
    features: ['Lifetime updates', ...],
  }
]
```

**ВЫВОД:** Это ПОЛНОСТЬЮ РАЗНЫЕ продукты!
- Landing продаёт компоненты/шаблоны (Free, Pro, Lifetime)
- /pricing и /billing продают YouTube Analytics (Basic, Professional, Enterprise)
- Цены ОДИНАКОВЫЕ ($19, $299?) но планы совершенно разные!

**Последствия:**
- Посетитель на landing видит одно (Free plan)
- После входа в app видит другое (Basic plan)
- Нарушена согласованность UX
- Путанница в ценообразовании

---

### 🟡 ПРОБЛЕМА 3: FEATURES РАЗДЕЛЕНЫ

**Версия 1 - /faqs/data/features.json (4 items, e-commerce):**
- Premium Quality
- Secure Shopping
- Fast Delivery
- 24/7 Support

**Версия 2 - /pricing/data/features.json (6 items, SaaS):**
- Fast Performance
- Enterprise Security
- Instant Setup
- Team Collaboration
- 24/7 Support ← общий только этот!
- Real-time Analytics

**СТАТУС:** ✓ Это не дублирование - это разные features для разных контекстов (e-commerce vs YouTube Analytics)

---

## 4. АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### Проблема A: Single Source of Truth отсутствует
```
FAQ данные:
  ├── /faqs/data/faqs.json ← MASTER (46 вопросов)
  └── /pricing/data/faqs.json ← КОПИЯ (6 вопросов)
       ↑ Если меняется, это обновляется автоматически?
       → НЕТ! Это ручное копирование/дублирование
```

### Проблема B: Pricing Plans не синхронизированы
```
Pricing Plans:
  ├── /components/pricing-plans.tsx ← YouTube Analytics
  ├── /landing/pricing-section.tsx ← Component Library
  └── /settings/billing/data/current-plan.json ← Данные пользователя
      ↑ Какой план является источником истины?
      → НИКАКОЙ! Каждый имеет свой набор данных
```

### Проблема C: Features не централизованы
```
Features:
  ├── /faqs/data/features.json
  └── /pricing/data/features.json
      ↑ Если добавить новую feature, нужно в обе места?
      → Неясно
```

---

## 5. ТАБЛИЦА ВЗАИМОСВЯЗЕЙ

```
КОМПОНЕНТ/СТРАНИЦА         ИСПОЛЬЗУЕТ ДАННЫЕ ИЗ              ТИП СВЯЗИ
──────────────────────────────────────────────────────────────────────
/faqs/page.tsx
  ├─ FAQList                → /faqs/data/faqs.json            Direct
  ├─ FAQList                → /faqs/data/categories.json      Direct
  └─ FeaturesGrid           → /faqs/data/features.json        Direct

/pricing/page.tsx
  ├─ PricingPlans           → Hardcoded (pricing-plans.tsx)  Import
  ├─ FeaturesGrid           → /pricing/data/features.json    Direct
  └─ FAQSection             → /pricing/data/faqs.json        Direct ⚠️

/settings/billing/page.tsx
  ├─ CurrentPlanCard        → /billing/data/current-plan.json Direct
  ├─ BillingHistoryCard     → /billing/data/billing-history  Direct
  └─ PricingPlans           → Hardcoded (pricing-plans.tsx)  Import

/landing/pricing-section.tsx
  └─ (Hardcoded)            → No data source!                Hardcoded 🔴
```

---

## 6. ПОТОК ДАННЫХ: ВИЗУАЛИЗАЦИЯ

```
ФРОНТЕНД - ЧТО ВИДИТ ПОЛЬЗОВАТЕЛЬ:

Landing Page                          Dashboard App
┌──────────────────────┐            ┌──────────────────────────┐
│ Pricing (Free/Pro)   │   VERSUS   │ Pricing (Basic/Pro/Ent)  │
│ $0 / $19 / $299      │            │ $19 / $79 / $199         │
│                      │            │                          │
│ Features (4)         │            │ Features (6)             │
│ E-commerce style     │            │ YouTube Analytics style  │
└──────────────────────┘            └──────────────────────────┘
         ↑                                      ↑
         │ Разные цены?                        │
         │ Разные планы?                       │
         └──────────────────────────────────────┘
                    НЕСОГЛАСОВАННОСТЬ!
```

---

## 7. РЕКОМЕНДАЦИИ ПО УСТРАНЕНИЮ ДУБЛИРОВАНИЯ

### 🎯 РЕКОМЕНДАЦИЯ 1: ЦЕНТРАЛИЗАЦИЯ FAQ ДАННЫХ

**ПРОБЛЕМА:**
- /pricing/data/faqs.json содержит точные копии 6 вопросов из /faqs/data/faqs.json
- Нарушен принцип "Single Source of Truth"
- Любое обновление FAQ требует ручного редактирования в двух местах
- Риск несинхронизации при изменении

**РЕШЕНИЕ:**
```
Структура ДО:
/faqs/data/
  └── faqs.json (46 вопросов) ← MASTER
/pricing/data/
  └── faqs.json (6 вопросов) ← КОПИЯ ❌

Структура ПОСЛЕ:
/shared/data/
  └── faqs.json (46 вопросов с ID) ← ЕДИНСТВЕННЫЙ ИСТОЧНИК ✅

/faqs/page.tsx:
  // Импортировать ВСЕ вопросы из /shared/data/faqs.json
  import faqsData from '@/shared/data/faqs.json'

/pricing/page.tsx:
  // Импортировать ВСЕ вопросы, но выбрать только нужные
  import faqsData from '@/shared/data/faqs.json'
  const pricingFaqs = faqsData.filter(faq =>
    faq.categories.includes('Billing') ||
    faq.categories.includes('Pricing')
  ).slice(0, 6) // Или использовать id конкретных вопросов
```

**ПОЧЕМУ ИМЕННО ТАК:**
1. **Единая точка редактирования** - изменяем один файл, все страницы автоматически синхронизированы
2. **Контроль версий** - git diff покажет что изменилось в FAQ
3. **Масштабируемость** - легко добавить новые фильтры (по категориям, по тегам, по релевантности)
4. **Кэширование** - сервер может кэшировать одну копию вместо двух
5. **Типизация** - можно создать TypeScript интерфейс для FAQ с ID полем:
```typescript
// shared/data/faqs.ts
export interface FAQ {
  id: string // Уникальный идентификатор
  question: string
  answer: string
  category: 'General' | 'Account' | 'Billing' | 'Technical' | 'Privacy' | 'Security' | 'Support'
  tags?: string[]
  relatedTo?: string[] // Для связанных вопросов
}
```

**МИГРАЦИЯ (пошагово):**
1. Создать `/src/shared/data/faqs.json` с полными 46 вопросами + ID каждому
2. Обновить `/faqs/page.tsx` импортировать из `/shared/data/faqs.json`
3. Обновить `/pricing/page.tsx` импортировать из `/shared/data/faqs.json` с фильтром
4. Удалить старые файлы: `/faqs/data/faqs.json` и `/pricing/data/faqs.json`
5. Обновить тесты и типы если есть

---

### 🎯 РЕКОМЕНДАЦИЯ 2: ЦЕНТРАЛИЗАЦИЯ PRICING PLANS

**ПРОБЛЕМА:**
- Pricing Plans hardcoded в `/components/pricing-plans.tsx`
- Нет способа быстро поменять цены или планы через админ панель
- 3 РАЗНЫЕ версии pricing:
  - /pricing: YouTube Analytics (Basic $19, Professional $79, Enterprise $199)
  - /settings/billing: YouTube Analytics (те же)
  - /landing: Component Library (Free $0, Pro $19, Lifetime $299)
- Невозможно A/B тестировать разные ценовые модели

**РЕШЕНИЕ:**
```
Структура ДО:
src/components/pricing-plans.tsx
  └── const defaultPlans = [...] // Hardcoded ❌

Структура ПОСЛЕ:
src/shared/data/
  ├── pricing-plans-app.json (для YouTube Analytics)
  └── pricing-plans-landing.json (для компонент-либрари)

src/components/pricing-plans.tsx:
  import appPlans from '@/shared/data/pricing-plans-app.json'

  const getPricingPlans = (mode: 'app' | 'landing') => {
    return mode === 'app' ? appPlans : landingPlans
  }
```

**Структура файла `/shared/data/pricing-plans-app.json`:**
```json
[
  {
    "id": "basic",
    "name": "Basic",
    "description": "Perfect for small creators",
    "price": {
      "monthly": 19,
      "currency": "USD",
      "billingPeriod": "month"
    },
    "features": ["Track up to 3 competitors", "Basic analytics", ...],
    "popular": false,
    "limits": {
      "competitors": 3,
      "reports": "limited"
    }
  },
  ...
]
```

**ПОЧЕМУ ИМЕННО ТАК:**
1. **Отделение конфигурации от кода** - цены можно менять без пересборки приложения
2. **Версионирование** - каждая версия pricing имеет свой файл с датой
3. **Ясность бизнес-модели** - видно что у app одна модель (YouTube Analytics), у landing другая (Component Library)
4. **Admin интеграция** - позволит позже подключить админ-панель для изменения цен
5. **API интеграция** - можно подключить API вместо JSON когда понадобится

**ПРОБЛЕМА С LANDING:**
- Landing page показывает "Free", "Pro", "Lifetime" планы
- App показывает "Basic", "Professional", "Enterprise" планы
- Это создаёт путаницу для пользователей

**ДВА ВАРИАНТА РЕШЕНИЯ:**

**Вариант A - УНИФИЦИРОВАТЬ (рекомендуется):**
- Обновить landing page на YouTube Analytics ценовой модель
- Показывать "Basic", "Professional", "Enterprise" везде
- Удалить "Free" план или переназвать на "Basic"
- Это создаст единую бизнес-стратегию

**Вариант B - РАЗДЕЛИТЬ (если landing это отдельный продукт):**
- Оставить landing с Free/Pro/Lifetime
- Явно обозначить что это другой продукт (Component Library)
- Создать отдельное доменное имя или подпапку /components/ для component library
- Это потребует дополнительную навигацию и маркетинг

**РЕКОМЕНДУЕТСЯ ВАРИАНТ A** потому что:
- Одна цена = одно сообщение в маркетинге
- Меньше путаницы для пользователей
- Проще поддерживать одну ценовую модель
- Landing будет настоящей landing-page для YouTube Analytics

---

### 🎯 РЕКОМЕНДАЦИЯ 3: УНИФИКАЦИЯ FEATURES

**ТЕКУЩАЯ СИТУАЦИЯ:**
- /faqs/data/features.json - 4 features (e-commerce: Premium Quality, Secure Shopping, Fast Delivery, 24/7 Support)
- /pricing/data/features.json - 6 features (YouTube Analytics: Fast Performance, Enterprise Security, Instant Setup, Team Collaboration, 24/7 Support, Real-time Analytics)

**РЕШЕНИЕ:**
```
Структура ПОСЛЕ:
src/shared/data/
  └── features.json

Формат:
[
  {
    "id": "feature_1",
    "name": "24/7 Support",
    "description": "Always here to help",
    "context": ["faqs", "pricing"], // В каких контекстах появляется
    "icon": "MessageCircle"
  },
  {
    "id": "feature_fast_delivery",
    "name": "Fast Delivery",
    "description": "Get your components instantly",
    "context": ["faqs"], // Только для FAQ
    "icon": "Zap"
  },
  ...
]
```

**ПОЧЕМУ ИМЕННО ТАК:**
1. **Контекстная реиспользуемость** - одна feature может использоваться на разных страницах
2. **Метаданные** - с иконками, описаниями, тегами легче работать программно
3. **Консистентность** - все features определены в одном месте
4. **Локализация** - легче переводить одну копию вместо двух

**МИГРАЦИЯ:**
1. Создать `/src/shared/data/features.json` с объединённым списком (4+6=10 features, но 1 дублирует)
2. Обновить /faqs/page.tsx: импортировать и фильтровать по context
3. Обновить /pricing/page.tsx: импортировать и фильтровать по context
4. Удалить старые файлы

---

### 🎯 РЕКОМЕНДАЦИЯ 4: АРХИТЕКТУРА КАТЕГОРИЙ FAQ

**ПРОБЛЕМА:**
- 8 категорий FAQ определены только в /faqs/data/categories.json
- Нет способа быстро добавить новые категории
- Категории hardcoded в faqs.json вместо поля category в каждом вопросе

**РЕШЕНИЕ:**
```json
// shared/data/faqs.json (обновленная структура)
[
  {
    "id": "faq_001",
    "question": "What is ShadcnStore Admin?",
    "answer": "...",
    "category": "General", // Вместо числового индекса
    "order": 1,
    "tags": ["product", "features"]
  }
]

// shared/data/categories.json (для быстрого доступа)
[
  {
    "id": "general",
    "name": "General",
    "description": "General questions about the platform",
    "icon": "HelpCircle",
    "count": 8
  }
]
```

---

### 📊 ИТОГОВАЯ АРХИТЕКТУРА

```
ДО (текущее состояние - дублирование):
───────────────────────────────────────
src/
├── app/(dashboard)/
│   ├── faqs/
│   │   ├── data/
│   │   │   ├── faqs.json (46) ← КОПИЯ 1
│   │   │   ├── categories.json
│   │   │   └── features.json (4, e-commerce)
│   │   └── page.tsx
│   ├── pricing/
│   │   ├── data/
│   │   │   ├── faqs.json (6) ← КОПИЯ 2 (дублирует фаqs/faqs.json)
│   │   │   └── features.json (6, YouTube Analytics)
│   │   └── page.tsx
│   └── settings/billing/
│       ├── data/
│       │   ├── current-plan.json
│       │   └── billing-history.json
│       └── page.tsx
├── components/
│   └── pricing-plans.tsx ← Hardcoded 3 плана
└── landing/
    └── components/
        └── pricing-section.tsx ← Hardcoded FREE/PRO/LIFETIME

ПОСЛЕ (централизованное управление):
────────────────────────────────────
src/
├── shared/data/
│   ├── faqs.json (46 вопросов с ID - SSOT) ✅
│   ├── categories.json (8 категорий)
│   ├── features.json (10 features с контекстом)
│   ├── pricing-plans-app.json (YouTube Analytics)
│   └── pricing-plans-landing.json (если отдельный продукт)
├── app/(dashboard)/
│   ├── faqs/
│   │   ├── data/ ← УДАЛИТЬ (использует shared/)
│   │   └── page.tsx (импортирует из /shared/data/faqs.json)
│   ├── pricing/
│   │   ├── data/ ← УДАЛИТЬ (использует shared/)
│   │   └── page.tsx (импортирует из /shared/data/)
│   └── settings/billing/
│       └── page.tsx (импортирует из /shared/data/)
├── components/
│   └── pricing-plans.tsx (берёт данные из JSON, не hardcoded)
└── landing/
    └── components/
        └── pricing-section.tsx (берёт данные из JSON)
```

---

### 📈 ПЛАН РЕАЛИЗАЦИИ

**ФАЗА 1 - Подготовка (1-2 дня):**
1. Создать структуру `/src/shared/data/`
2. Создать фалы: faqs.json, categories.json, features.json, pricing-plans.json
3. Переместить данные из разрозненных мест в новую структуру

**ФАЗА 2 - Миграция компонентов (2-3 дня):**
1. Обновить импорты в /faqs/page.tsx
2. Обновить импорты в /pricing/page.tsx
3. Обновить импорты в /settings/billing/page.tsx
4. Обновить pricing-plans.tsx чтобы принимал данные из JSON вместо hardcoded

**ФАЗА 3 - Удаление дублирования (1 день):**
1. Удалить /faqs/data/faqs.json и /faqs/data/features.json
2. Удалить /pricing/data/faqs.json и /pricing/data/features.json
3. Удалить /settings/billing/data/pricing-plans.json если существует

**ФАЗА 4 - Решение Landing Page (1-2 дня):**
- **Вариант A**: Обновить landing на YouTube Analytics ценовой модель
- **Вариант B**: Создать отдельный раздел для component library

**ФАЗА 5 - Тестирование (2-3 дня):**
1. Проверить что все страницы работают
2. Проверить что все URL в компонентах ведут куда надо
3. Проверить что data правильно рендерится

**Общее время:** 1-2 недели в зависимости от параллелизма работ

---

### ✅ ПРЕИМУЩЕСТВА РЕКОМЕНДУЕМОГО ПОДХОДА

1. **Maintainability** - проще поддерживать одну копию данных
2. **Scalability** - легко добавить новые вопросы, планы, features
3. **Performance** - кэширование работает лучше когда данные централизованы
4. **Developer Experience** - всем ясно где находятся данные
5. **Business Agility** - можно менять цены через админ-панель без пересборки
6. **Analytics** - проще отследить какие FAQ чаще всего просматриваются
7. **A/B Testing** - можно быстро создавать разные варианты ценовых планов
8. **Локализация** - одна копия данных = проще переводить

---

**Дата отчёта:** 06.12.2025
**Статус:** ✅ АНАЛИЗ И РЕКОМЕНДАЦИИ ЗАВЕРШЕНЫ
**Требуется действие:** Ожидание решения пользователя (реализовывать ли рекомендации)
