# 📋 PROJECT CONTEXT - YouTube Analytics Dashboard

> ⚠️ **ВАЖНО:** После каждой доработки обновлять ЭТОТ файл вместе с CHANGES.md!
> Этот файл должен содержать СМЫСЛ и КОНТЕКСТ работы, не просто список изменений.
> Каждый раз, открывая проект в новой сессии, читай этот файл первым!

---

## 🎯 Что это за проект?

YouTube Analytics Dashboard — веб-приложение для анализа YouTube канала с использованием AI.
Показывает интерактивные графики, статистику, интеллектуальный анализ контента и рекомендации.

**Основной стек:**
- Next.js 15 (React фреймворк)
- TypeScript
- shadcn/ui (компоненты)
- Recharts (графики)
- OpenAI API (AI анализ)

---

## 🏗️ Текущая архитектура

### Главная страница аналитики: `/app/(dashboard)/channel/[id]/page.tsx`

Содержит 7 основных аналитических разделов:

1. **Growth Over Time** — график роста подписчиков/просмотров/видео
2. **Top Videos** — сетка топ-видео с превью
3. **Content Intelligence** — AI анализ контент-стратегии (7 свертываемых подразделов)
4. **Momentum Insights** — анализ растущих видео
5. **Audience & Engagement** — статистика аудитории
6. **Comment Intelligence** — анализ комментариев
7. **Deep Analysis** — глубокий AI анализ аудитории (v2.0)

### Ключевой компонент: `ChannelAnalytics.tsx`

```jsx
CollapsibleSection wrapper
├── Card (каждый раздел)
│   ├── CardHeader (кликабельный заголовок + Chevron)
│   └── CardContent (содержимое раздела)
└── State: expanded (управляет видимостью)
```

**Текущее состояние по умолчанию:** Все разделы **закрыты** при загрузке страницы.

---

## 🎨 Архитектура компонентов

### Основной паттерн: CardContent вместо div

Все компоненты аналитики возвращают **`CardContent`** вместо простого `div`:

```jsx
// ❌ НЕПРАВИЛЬНО (было раньше)
<div className="space-y-4">
  {content}
</div>

// ✅ ПРАВИЛЬНО (сейчас)
<CardContent className="space-y-4 pt-6">
  {content}
</CardContent>
```

**Почему:** CardContent обеспечивает стандартный padding (p-6), контент не прижимается к границам.

### Компоненты, использующие этот паттерн:

- ✅ `TopVideosGrid.tsx` — возвращает `CardContent className="p-0"`
- ✅ `ChannelGrowthChart.tsx` — возвращает `CardContent`
- ✅ `CommentInsights.tsx` — возвращает `CardContent className="space-y-4 pt-6"`
- ✅ `ContentIntelligenceBlock.tsx` — возвращает `CardContent className="space-y-4 pt-6"`
- ✅ `MomentumInsights.tsx` — возвращает `CardContent className="space-y-4 pt-6"`
- ✅ `AudienceInsights.tsx` — возвращает `CardContent className="space-y-4 pt-6"`
- ✅ `DeepCommentAnalysis.tsx` — возвращает `CardContent className="space-y-4 pt-6"`

---

## 🔄 Последовательность работы сессий

### Сессия 1: Добавление сворачиваемости
- **Задача:** Сделать все разделы сворачиваемыми (очень длинная страница с много контента)
- **Решение:** Создан `CollapsibleSection` компонент с состоянием `expanded`
- **Проблема:** Дублирование заголовков (CollapsibleSection + компонент)
- **Исправление:** Удалены заголовки из компонентов

### Сессия 2: Исправление DOM структуры
- **Проблема:** Лишние div-вложенности, нечеткая структура DOM
- **Решение:** Оптимизирована структура, убраны ненужные wrapper'ы
- **Итог:** Чистая структура `Card > CardHeader + CardContent`

### Сессия 3: Восстановление визуала (ТЕКУЩАЯ)
- **Проблема:** Элементы прижимались к краям (отсутствовал padding)
- **Решение:** Все компоненты теперь возвращают `CardContent` с правильным padding
- **Улучшение:** Все разделы закрыты по умолчанию для лучшей UX
- **Результат:** Красивое, аккуратное оформление с интуитивной навигацией

---

## 🚀 Финальная структура каждого раздела

```jsx
<Card>                                      // CollapsibleSection
  <CardHeader                               // Кликабельный заголовок
    className="cursor-pointer hover:bg-muted/50"
    onClick={toggle}
  >
    <div className="flex items-center justify-between">
      <CardTitle className="text-lg">Заголовок раздела</CardTitle>
      {isOpen ? <ChevronUp /> : <ChevronDown />}
    </div>
  </CardHeader>

  {isOpen && (                              // Условное отображение
    <CardContent className="space-y-4 pt-6"> // Из компонента
      {содержимое с правильным padding}
    </CardContent>
  )}
</Card>
```

---

## ✨ Ключевые улучшения в этой сессии

### 1. Правильный padding для всех элементов
- ✅ Все компоненты используют `CardContent` вместо `div`
- ✅ Элементы больше не прижимаются к границам контейнера
- ✅ Единообразный стиль по всей странице

### 2. Оптимизированная видимость
- ✅ Все разделы закрыты по умолчанию
- ✅ Пользователь видит полный обзор доступных аналитических блоков
- ✅ Контент загружается по требованию (лучше производительность)

### 3. Чистая DOM структура
- ✅ Минимум необходимых div-элементов
- ✅ Ясная иерархия: Card → CardHeader + CardContent
- ✅ Легко понять, какой контент к какому разделу относится

---

## 📝 Как обновлять этот файл

### После каждой доработки:

1. **Описать задачу:** "Что нужно было сделать?"
2. **Объяснить решение:** "Как это решил?"
3. **Указать результат:** "Что получилось?"
4. **Обновить контекст:** "Как это влияет на архитектуру?"

### Пример хорошего обновления:

```markdown
### Сессия N: [Название задачи]
- **Задача:** ...
- **Решение:** ...
- **Проблемы:** ...
- **Итоговая архитектура:** ...
```

**НЕ делать:** Просто копировать `git log` или `CHANGES.md`
**ДЕЛАТЬ:** Объяснять логику и контекст решений

---

## 📦 Deep Comments Analysis v2.1 - Модульная архитектура

### Что изменилось (Сессия 4: ТЕКУЩАЯ)
- Полный рефакторинг системы анализа комментариев
- Вместо одного огромного JSON → 10 независимых mini-модулей
- Каждый модуль отправляет отдельный запрос к GPT-4.1-mini
- Все 10 модулей работают параллельно через Promise.all()

### 10 модулей в src/lib/ai/comments-modules/
1. **emotional-overview.ts** — строка (2-3 предложения об эмоциях)
2. **key-topics.ts** — массив KeyTopic[] (название, описание, примеры, мотив, использование)
3. **positive-triggers.ts** — массив PositiveTrigger[] (что нравится)
4. **negative-triggers.ts** — массив NegativeTrigger[] (что раздражает)
5. **faq.ts** — массив FAQ[] (вопросы, ответы)
6. **audience-segments.ts** — массив AudienceSegment[] (сегменты, описания)
7. **behavioral-insights.ts** — массив string[] (5+ инсайтов о поведении)
8. **missing-elements.ts** — массив string[] (3+ пропущенные элементы)
9. **growth-opportunities.ts** — массив GrowthOpportunity[] (возможности роста)
10. **checklist.ts** — массив string[] (ровно 8 пунктов в порядке: Убрать, Добавить, и т.д.)

### Orchestrator (src/lib/ai/comments-orchestrator.ts)
```typescript
analyzeCommentsWithOrchestrator(comments, language)
  ├─ Promise.all([
  │   generateEmotionalOverview(),
  │   generateKeyTopics(),
  │   generatePositiveTriggers(),
  │   generateNegativeTriggers(),
  │   generateFAQ(),
  │   generateAudienceSegments(),
  │   generateBehavioralInsights(),
  │   generateMissingElements(),
  │   generateGrowthOpportunities(),
  │   generateChecklist()
  │ ])
  └─ Собрать в финальный JSON с жёсткой структурой
```

### Гарантии
- ✅ Все 10 полей ВСЕГДА присутствуют (никогда не undefined)
- ✅ Retry 3x для каждого модуля независимо
- ✅ Fallback пустые значения при ошибках (не ломает отчёт)
- ✅ Параллельное выполнение (быстрее чем последовательно)
- ✅ Полное логирование для отладки

### Как добавить новый модуль (11-й, 12-й и т.д.)
1. Создать файл src/lib/ai/comments-modules/new-module.ts
2. Реализовать buildPrompt(), cleanLLMResponse(), generateNewModule()
3. Добавить в orchestrator Promise.all()
4. Добавить результат в финальный JSON

### ВАЖНО: UI НЕ ИЗМЕНИЛАСЬ
- Все компоненты работают как прежде
- API endpoint остался тем же
- База данных остаётся неизменной
- Структура JSON результата совместима

---

## 🎯 Sentiment анализ v2.2 - Микропромпты для подсчёта (НОВОЕ)

### Архитектура
- **Три микрофункции** вместо одного большого JSON-ответа
- Каждая функция делает **один простой AI-вызов**
- Возвращают **число** (positive count, neutral count, negative count)
- Работают **параллельно** с остальными 10 модулями через Promise.all()

### Микрофункции в `src/lib/ai/comments-modules/sentiment-analyzer.ts`
```ts
countPositive(comments, language) → number
countNeutral(comments, language) → number
countNegative(comments, language) → number
```

### Преимущества
- AI не может сломать JSON (просто число)
- Валидация: `typeof result === 'number'`
- Retry работает независимо для каждой функции
- Температура 0.3 для точных подсчётов
- Fallback: 0 при любых ошибках
- Масштабируется (легко добавить countAnger, countExcitement и т.д.)

### Результат в orchestrator
```ts
sentiment: {
  positive: number,
  neutral: number,
  negative: number
}
```

### UI обработка
- Компонент вычисляет проценты: `positive / (positive + neutral + negative) * 100`
- Показывает % и абсолютное число: "35% Positive (47)"
- Обратно совместим с старой структурой `sentimentSummary`

---

## 🔍 Что нужно помнить в следующих сессиях

1. **Модульная архитектура** — все анализы идут через 10 mini-модулей + 3 микропромпта параллельно
2. **Promise.all()** — 13 задач работают одновременно (10 модулей + 3 sentiment)
3. **Каждый модуль независим** — может упасть, не влияя на остальные (fallback)
4. **Sentiment теперь числа** — countPositive/Neutral/Negative возвращают integer
5. **CardContent, не div** — все компоненты аналитики возвращают CardContent
6. **Разделы закрыты по умолчанию** — нужно открыть в expanded состояние, чтобы увидеть

---

## 📚 Связанные файлы

- `CHANGES.md` — подробный лог всех изменений
- `src/lib/ai/comments-orchestrator.ts` — главный orchestrator (обновлено в v2.2)
- `src/lib/ai/comments-modules/` — папка с 10 модулями + sentiment-analyzer
- `src/lib/ai/comments-modules/sentiment-analyzer.ts` — три микрофункции для sentiment (НОВОЕ в v2.2)
  - `countPositive()` — подсчёт позитивных комментариев
  - `countNeutral()` — подсчёт нейтральных комментариев
  - `countNegative()` — подсчёт негативных комментариев
- `src/app/api/channel/[id]/comments/ai/route.ts` — API endpoint (не изменился)
- `src/components/channel/DeepCommentAnalysis.tsx` — UI для отображения результатов (обновлено в v2.2)
- `src/components/channel/ChannelAnalytics.tsx` — главный компонент с логикой сворачивания
- `src/app/(dashboard)/channel/[id]/page.tsx` — точка входа на страницу аналитики

---

**Последнее обновление:** 2025-12-09
**Сессия:** REFACTOR - Sentiment анализ через микропромпты
**Статус:** ✅ Завершено
