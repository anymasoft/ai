# 📊 ТЕХНИЧЕСКИЙ АУДИТ ПРОЕКТА: YouTube Competitors Analytics

**Дата аудита:** 05.12.2025  
**Версия:** 1.0.0  
**Тип проекта:** Next.js 15.4.7 + React 19 + TypeScript + SQLite

---

## 2. ОПИСАНИЕ ТАБЛИЦ SQLite

### 2.1. Таблицы аутентификации (NextAuth)

#### `users`
**Назначение:** Хранение пользовательских аккаунтов

| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT PRIMARY KEY | Уникальный ID пользователя |
| name | TEXT | Имя пользователя |
| email | TEXT NOT NULL UNIQUE | Email (уникальный) |
| emailVerified | INTEGER | Timestamp верификации email |
| image | TEXT | URL аватара |
| role | TEXT DEFAULT 'user' | Роль (user/admin) |
| plan | TEXT DEFAULT 'free' | Тарифный план (free/basic/professional/enterprise) |
| language | TEXT DEFAULT 'en' | Язык интерфейса (en/ru) |
| createdAt | INTEGER NOT NULL | Дата создания |
| updatedAt | INTEGER NOT NULL | Дата обновления |

**Связи:**
- Связана с `competitors` через Foreign Key (userId)
- Связана с `channel_metrics` через Foreign Key (userId)

---

#### `accounts`
**Назначение:** OAuth провайдеры (Google)

| Поле | Тип | Описание |
|------|-----|----------|
| userId | TEXT NOT NULL | ID пользователя |
| type | TEXT NOT NULL | Тип аккаунта |
| provider | TEXT NOT NULL | Провайдер (google) |
| providerAccountId | TEXT NOT NULL | ID в системе провайдера |
| refresh_token | TEXT | Refresh token |
| access_token | TEXT | Access token |
| expires_at | INTEGER | Время истечения токена |
| token_type | TEXT | Тип токена |
| scope | TEXT | Скоупы доступа |
| id_token | TEXT | ID токен |
| session_state | TEXT | Состояние сессии |

**Primary Key:** (provider, providerAccountId)  
**Foreign Key:** userId → users(id) ON DELETE CASCADE

---

#### `sessions`
**Назначение:** Активные сессии пользователей

| Поле | Тип | Описание |
|------|-----|----------|
| sessionToken | TEXT PRIMARY KEY | Уникальный токен сессии |
| userId | TEXT NOT NULL | ID пользователя |
| expires | INTEGER NOT NULL | Время истечения сессии |

**Foreign Key:** userId → users(id) ON DELETE CASCADE

---

#### `verificationTokens`
**Назначение:** Токены верификации email

| Поле | Тип | Описание |
|------|-----|----------|
| identifier | TEXT NOT NULL | Идентификатор (email) |
| token | TEXT NOT NULL | Токен верификации |
| expires | INTEGER NOT NULL | Время истечения |

**Primary Key:** (identifier, token)

---

### 2.2. Основные таблицы приложения

#### `competitors`
**Назначение:** Список отслеживаемых конкурентов (каналов)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| userId | TEXT NOT NULL | ID владельца |
| platform | TEXT DEFAULT 'youtube' | Платформа (пока только YouTube) |
| channelId | TEXT NOT NULL | Уникальный ID канала YouTube |
| handle | TEXT NOT NULL | Handle канала (@channel) |
| title | TEXT NOT NULL | Название канала |
| avatarUrl | TEXT | URL аватара канала |
| subscriberCount | INTEGER DEFAULT 0 | Количество подписчиков |
| videoCount | INTEGER DEFAULT 0 | Количество видео |
| viewCount | INTEGER DEFAULT 0 | Общие просмотры |
| lastSyncedAt | INTEGER NOT NULL | Последняя синхронизация (timestamp) |
| createdAt | INTEGER NOT NULL | Дата добавления |

**Foreign Key:** userId → users(id) ON DELETE CASCADE

**Использование:**
- Главная таблица для хранения информации о конкурентах
- Обновляется при каждой синхронизации метрик
- Используется для отображения списка и детальной информации

---

#### `channel_metrics`
**Назначение:** Исторические метрики каналов для построения графиков

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| userId | TEXT NOT NULL | ID владельца |
| channelId | TEXT NOT NULL | ID канала YouTube |
| subscriberCount | INTEGER DEFAULT 0 | Подписчики на момент записи |
| videoCount | INTEGER DEFAULT 0 | Видео на момент записи |
| viewCount | INTEGER DEFAULT 0 | Просмотры на момент записи |
| date | TEXT NOT NULL | Дата в формате YYYY-MM-DD |
| fetchedAt | INTEGER NOT NULL | Timestamp загрузки |

**Индексы:**
- `idx_channel_metrics_lookup` на (channelId, date)

**Foreign Key:** userId → users(id) ON DELETE CASCADE

**Использование:**
- Хранит snapshots метрик канала для построения графиков роста
- По умолчанию разрешено до 10 записей в день (для тестирования)
- В продакшене — 1 запись в день

---

#### `channel_videos`
**Назначение:** Топ-видео каналов

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| channelId | TEXT NOT NULL | ID канала |
| videoId | TEXT NOT NULL UNIQUE | ID видео YouTube |
| title | TEXT NOT NULL | Название видео |
| thumbnailUrl | TEXT | URL миниатюры |
| viewCount | INTEGER DEFAULT 0 | Просмотры |
| likeCount | INTEGER DEFAULT 0 | Лайки |
| commentCount | INTEGER DEFAULT 0 | Комментарии |
| publishedAt | TEXT NOT NULL | Дата публикации |
| fetchedAt | INTEGER NOT NULL | Timestamp загрузки |
| data | TEXT | Дополнительные данные (JSON) |

**Индексы:**
- `idx_channel_videos_lookup` на (channelId, videoId)

**Использование:**
- Хранит до ~150 топовых видео канала (5 страниц)
- Используется для отображения топ-видео и AI-анализа
- Обновляется при синхронизации видео

---

#### `video_details`
**Назначение:** Детальная информация о видео (расширенные метрики)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| videoId | TEXT NOT NULL UNIQUE | ID видео |
| url | TEXT NOT NULL | Полный URL видео |
| likeCount | INTEGER DEFAULT 0 | Лайки |
| commentCount | INTEGER DEFAULT 0 | Комментарии |
| viewCount | INTEGER DEFAULT 0 | Просмотры |
| durationMs | INTEGER | Длительность в миллисекундах |
| keywordsJson | TEXT | Keywords (JSON array) |
| transcriptShort | TEXT | Короткий транскрипт |
| updatedAt | INTEGER NOT NULL | Timestamp обновления |

**Индексы:**
- `idx_video_details_lookup` на (videoId)

**Использование:**
- Хранит расширенную информацию о видео
- Может использоваться для получения транскриптов
- В текущей версии используется редко

---

#### `video_comments`
**Назначение:** Комментарии к видео

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| videoId | TEXT NOT NULL | ID видео |
| commentId | TEXT NOT NULL UNIQUE | ID комментария |
| content | TEXT NOT NULL | Текст комментария |
| publishedTime | TEXT NOT NULL | Дата публикации |
| replyLevel | INTEGER DEFAULT 0 | Уровень ответа (0 = top-level) |
| likes | INTEGER DEFAULT 0 | Лайки |
| replies | INTEGER DEFAULT 0 | Количество ответов |
| authorName | TEXT NOT NULL | Имя автора |
| authorChannelId | TEXT NOT NULL | ID канала автора |
| isVerified | INTEGER DEFAULT 0 | Верифицирован? (0/1) |
| isCreator | INTEGER DEFAULT 0 | Автор канала? (0/1) |
| fetchedAt | INTEGER NOT NULL | Timestamp загрузки |
| channelId | TEXT | ID канала владельца |
| data | TEXT | Дополнительные данные (JSON) |

**Индексы:**
- `idx_video_comments_video` на (videoId, publishedTime DESC)
- `idx_video_comments_author` на (authorChannelId)

**Использование:**
- Хранит до 300 топ-комментариев на видео
- Используется для AI-анализа комментариев
- Загружается для топ-15 видео канала

---

### 2.3. Таблицы AI-анализа

#### `content_intelligence`
**Назначение:** AI-анализ контента канала (темы, форматы, паттерны)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| channelId | TEXT NOT NULL | ID канала |
| data | TEXT NOT NULL | JSON с анализом |
| data_ru | TEXT | JSON с переводом на русский |
| generatedAt | INTEGER NOT NULL | Timestamp генерации |

**Индексы:**
- `idx_content_intelligence_lookup` на (channelId, generatedAt DESC)

**Формат JSON (data):**
```json
{
  "themes": ["тема 1", "тема 2"],
  "formats": ["формат 1", "формат 2"],
  "patterns": ["паттерн 1", "паттерн 2"],
  "opportunities": ["возможность 1"],
  "recommendations": ["рекомендация 1"]
}
```

**Кэширование:** 7 дней

---

#### `momentum_insights`
**Назначение:** AI-анализ трендовых видео (momentum analysis)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| channelId | TEXT NOT NULL | ID канала |
| data | TEXT NOT NULL | JSON с анализом |
| data_ru | TEXT | JSON с переводом на русский |
| generatedAt | INTEGER NOT NULL | Timestamp генерации |

**Индексы:**
- `idx_momentum_insights_lookup` на (channelId, generatedAt DESC)

**Формат JSON (data):**
```json
{
  "highMomentumVideos": [
    {
      "videoId": "xxx",
      "title": "...",
      "viewCount": 100000,
      "viewsPerDay": 5000,
      "momentumScore": 1.2,
      "publishedAt": "2024-01-01"
    }
  ],
  "stats": {
    "totalAnalyzed": 150,
    "highMomentum": 20,
    "rising": 30,
    "medianViewsPerDay": 1000
  },
  "hotThemes": ["тема 1", "тема 2"],
  "hotFormats": ["формат 1"],
  "hotIdeas": ["идея 1"],
  "explanation": "..."
}
```

**Кэширование:** 3 дня

---

#### `audience_insights`
**Назначение:** AI-анализ аудитории канала

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| channelId | TEXT NOT NULL | ID канала |
| data | TEXT NOT NULL | JSON с анализом |
| data_ru | TEXT | JSON с переводом на русский |
| generatedAt | INTEGER NOT NULL | Timestamp генерации |

**Индексы:**
- `idx_audience_insights_lookup` на (channelId, generatedAt DESC)

**Формат JSON (data):**
```json
{
  "audienceProfile": ["характеристика 1", "характеристика 2"],
  "contentPreferences": ["предпочтение 1"],
  "engagementPatterns": ["паттерн 1"],
  "recommendations": ["рекомендация 1"]
}
```

**Кэширование:** 7 дней

**ПРИМЕЧАНИЕ:** Текущая версия генерирует generic инсайты без реальных данных комментариев

---

#### `comment_insights`
**Назначение:** Простой анализ комментариев (legacy)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| videoId | TEXT NOT NULL | ID видео |
| channelId | TEXT NOT NULL | ID канала |
| data | TEXT NOT NULL | JSON с анализом |
| data_ru | TEXT | JSON с переводом на русский |
| generatedAt | INTEGER NOT NULL | Timestamp генерации |

**Индексы:**
- `idx_comment_insights_lookup` на (videoId, generatedAt DESC)
- `idx_comment_insights_channel` на (channelId, generatedAt DESC)

**Использование:**
- Legacy версия анализа комментариев
- В текущей версии используется редко
- Заменена на `channel_ai_comment_insights`

---

#### `channel_ai_comment_insights`
**Назначение:** Глубокий AI-анализ комментариев (v2.0)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| channelId | TEXT NOT NULL | ID канала |
| resultJson | TEXT NOT NULL | JSON с результатами |
| createdAt | INTEGER NOT NULL | Timestamp создания |
| progress_current | INTEGER DEFAULT 0 | Текущий прогресс (чанки) |
| progress_total | INTEGER DEFAULT 0 | Всего чанков |
| status | TEXT DEFAULT 'pending' | Статус (pending/processing/done/error) |

**Индексы:**
- `idx_channel_ai_insights_channelId` на (channelId)
- `idx_channel_ai_insights_createdAt` на (createdAt DESC)

**Формат JSON (resultJson):**
```json
{
  "themes": ["тема 1", "тема 2"],
  "painPoints": ["боль 1", "боль 2"],
  "requests": ["запрос 1"],
  "praises": ["похвала 1"],
  "audienceSegments": ["сегмент 1"],
  "sentimentSummary": {
    "positive": 60,
    "negative": 10,
    "neutral": 30
  },
  "topQuotes": ["цитата 1"],
  "hiddenPatterns": ["паттерн 1"],
  "actionableIdeas": ["идея 1"],
  "totalAnalyzed": 1000,
  "language": "ru"
}
```

**Особенности:**
- Анализирует до 1000 комментариев с топ-30 видео
- Нормализует текст (удаляет emoji, URL, хештеги)
- Разбивает на чанки по 3500 символов
- Поддерживает прогресс-бар с отслеживанием состояния

---

#### `deep_audience`
**Назначение:** Глубокий анализ аудитории

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| channelId | TEXT NOT NULL | ID канала |
| data | TEXT NOT NULL | JSON с анализом |
| data_ru | TEXT | JSON с переводом на русский |
| createdAt | INTEGER NOT NULL | Timestamp создания |

**Индексы:**
- `idx_deep_audience_channelId` на (channelId)
- `idx_deep_audience_createdAt` на (createdAt DESC)

**Использование:**
- Расширенная версия audience_insights
- В текущей версии используется редко

---

#### `ai_insights` (Legacy)
**Назначение:** Старый SWOT-анализ

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| competitorId | INTEGER NOT NULL | ID конкурента |
| summary | TEXT NOT NULL | Краткое описание |
| strengths | TEXT NOT NULL | JSON: сильные стороны |
| weaknesses | TEXT NOT NULL | JSON: слабые стороны |
| opportunities | TEXT NOT NULL | JSON: возможности |
| threats | TEXT NOT NULL | JSON: угрозы |
| recommendations | TEXT NOT NULL | JSON: рекомендации |
| createdAt | INTEGER NOT NULL | Timestamp создания |

**Foreign Key:** competitorId → competitors(id) ON DELETE CASCADE

**Использование:**
- Legacy функция, не используется в текущей версии
- Может отображаться на странице канала, если есть данные
- Планируется удаление или переработка

---

#### `channels` (Legacy)
**Назначение:** Старая таблица каналов

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID |
| userId | TEXT | ID пользователя |
| channelId | TEXT | ID канала |
| title | TEXT | Название |
| createdAt | INTEGER | Timestamp создания |

**Использование:**
- Legacy таблица, НЕ используется
- Заменена на `competitors`
- Можно удалить

---

## 🎯 EXECUTIVE SUMMARY

YouTube Competitors Analytics — это полнофункциональная SaaS-платформа для глубокого анализа конкурентов на YouTube с использованием AI/GPT технологий. Проект базируется на Next.js 15 с App Router, использует SQLite для хранения данных, интегрирован с ScrapeCreators API для сбора данных YouTube и OpenAI GPT-4o-mini для генерации аналитических инсайтов.

**Текущее состояние:**
- ✅ **Competitors Module** — полностью реализован (добавление/удаление каналов, синхронизация метрик)
- ✅ **Channel Analytics** — полностью реализован (5 типов AI-анализа, графики, топ-видео)
- ✅ **AI Analysis Engine** — реализовано 5 модулей анализа на GPT
- ⏳ **Trending Module** — заглушка (готов к реализации)
- ⏳ **Reports Module** — заглушка (готов к реализации)
- ✅ **Authentication** — NextAuth с Google OAuth
- ✅ **Subscription Plans** — логика лимитов реализована

---

## 📑 СОДЕРЖАНИЕ

1. [Подробное описание текущего функционала](#1-подробное-описание-текущего-функционала)
2. [Описание таблиц SQLite](#2-описание-таблиц-sqlite)
3. [Обзор всех API endpoints](#3-обзор-всех-api-endpoints)
4. [Текущие возможности анализа](#4-текущие-возможности-анализа)
5. [Анализ GPT-промптов](#5-анализ-gpt-промптов)
6. [Варианты расширения функционала](#6-варианты-расширения-функционала)
7. [Идеи для Trending и Reports](#7-идеи-для-trending-и-reports)
8. [Сравнительный анализ конкурентов](#8-сравнительный-анализ-конкурентов)
9. [Генератор виральных сценариев](#9-генератор-виральных-сценариев)
10. [Итоговая таблица](#10-итоговая-таблица)

---

## 1. ПОДРОБНОЕ ОПИСАНИЕ ТЕКУЩЕГО ФУНКЦИОНАЛА

### 1.1. Раздел Competitors

**Страница:** `/competitors` (`src/app/(dashboard)/competitors/page.tsx`)

#### Основной функционал:
1. **Добавление конкурентов:**
   - Принимает ручку канала (`@channelname`) или полный URL YouTube
   - Нормализует ввод через `normalizeYoutubeInput()` из `src/lib/youtube/normalize`
   - Проверяет лимиты тарифного плана (free: 3, basic: 3, professional: 20, enterprise: 200)
   - Делает запрос к ScrapeCreators API через `getYoutubeChannelByHandle()`
   - Сохраняет в таблицу `competitors` с полными метриками

2. **Отображение списка:**
   - Таблица со всеми добавленными каналами
   - Показывает: аватар, название, handle, подписчики, видео, просмотры, дату последней синхронизации
   - Клик по строке → переход на детальную страницу канала

3. **Удаление конкурентов:**
   - Модальное окно подтверждения
   - API DELETE запрос на `/api/competitors/[id]`
   - Каскадное удаление связанных данных через SQLite Foreign Keys

4. **Ограничения:**
   - Блокирует добавление при достижении лимита плана
   - Показывает текущее использование (X/Y используется)
   - Предлагает upgrade при достижении лимита

#### Связи с другими частями:
- **API:** `/api/competitors` (GET, POST), `/api/competitors/[id]` (DELETE)
- **База данных:** таблица `competitors`, `users`
- **Внешний API:** ScrapeCreators `/v1/youtube/channel`
- **Маршрутизация:** переход на `/channel/[id]` при клике

#### Формат данных (input/output):
```typescript
// INPUT (POST /api/competitors)
{
  handle: string // "@channelname" или URL
}

// OUTPUT (GET /api/competitors)
[{
  id: number
  userId: string
  platform: "youtube"
  channelId: string
  handle: string
  title: string
  avatarUrl: string | null
  subscriberCount: number
  videoCount: number
  viewCount: number
  lastSyncedAt: number // timestamp
  createdAt: number // timestamp
}]
```

---

### 1.2. Детальная страница канала

**Страница:** `/channel/[id]` (`src/app/(dashboard)/channel/[id]/page.tsx`)

#### Основной функционал:

**1. Overview Block:**
- Отображает основные метрики канала
- Аватар, название, handle, ссылка на YouTube
- Три ключевые метрики: Subscribers, Total Views, Avg Views per Video
- Дата последней синхронизации

**2. Кнопки синхронизации:**
- **Sync All Data** — синхронизирует всё (метрики + видео + комментарии) последовательно
- **Sync Metrics** — обновляет подписчиков/просмотры/видео через ScrapeCreators API
- **Sync Videos** — загружает топ-видео канала (до 5 страниц по ~30 видео)
- **Sync Comments** — загружает комментарии для топ-15 видео (по 300 комментариев на видео)

**3. AI Insights (Legacy SWOT Analysis):**
- Старая версия AI-анализа (если существует запись в `ai_insights`)
- Показывает: Summary, Strengths, Weaknesses, Opportunities, Threats, Recommendations
- В текущей реализации НЕ генерируется автоматически (заготовка для будущих версий)

**4. AI Analytics Module (`ChannelAnalytics` component):**

Включает 7 блоков аналитики:

a) **Channel Growth Chart**
   - График роста метрик (подписчики, просмотры, видео) по времени
   - Использует данные из таблицы `channel_metrics`
   - Библиотека: Recharts

b) **Top Videos Grid**
   - Отображает топ-видео канала
   - Сортировка по просмотрам
   - Показывает: миниатюра, название, просмотры, лайки, комментарии
   - Клик → открывает видео на YouTube

c) **Content Intelligence Block**
   - AI-анализ контента канала (темы, форматы, паттерны)
   - Генерируется через OpenAI GPT-4o-mini
   - Кэширование на 7 дней
   - Требует: наличие синхронизированных видео

d) **Momentum Insights**
   - Анализ видео с высоким momentum (растущие видео)
   - Вычисляет momentum_score = (views_per_day / median_views_per_day) - 1
   - Категории: High Momentum (+50%+), Rising (+10%), Normal, Underperforming (-30%)
   - Генерирует AI-инсайты о трендовых темах
   - Кэширование на 3 дня

e) **Audience Insights**
   - AI-анализ профиля аудитории канала
   - Генерирует характеристики, предпочтения, паттерны взаимодействия
   - Кэширование на 7 дней
   - **ПРИМЕЧАНИЕ:** Текущая версия генерирует generic инсайты без реальных данных

f) **Comment Insights (Simple)**
   - Простой анализ комментариев (если есть данные)
   - Показывает статистику по комментариям
   - Требует: синхронизированные комментарии

g) **Deep Comment Analysis (AI v2.0)**
   - Глубокий AI-анализ комментариев через чанки
   - Извлекает: темы, боли аудитории, запросы, похвалы, сегменты, sentiment
   - Использует продвинутый алгоритм нормализации текста
   - Чанкинг комментариев по 3500 символов
   - Прогресс-бар с отслеживанием состояния
   - Требует: синхронизированные комментарии для топ-30 видео

#### Связи с другими частями:
- **API:** 8 эндпоинтов для синхронизации и анализа
- **База данных:** 15+ таблиц для хранения метрик, видео, комментариев, AI-инсайтов
- **Внешние API:** ScrapeCreators (метрики, видео, комментарии), OpenAI (AI-анализ)
- **Компоненты:** 13 специализированных React-компонентов

---

### 1.3. Раздел Trending

**Страница:** `/trending` (`src/app/(dashboard)/trending/page.tsx`)

#### Текущее состояние:
- **Статус:** Заглушка "Coming Soon"
- **Реализованный функционал:** Нет
- **UI:** Пустая карточка с сообщением "Trending content analysis will be available soon"

#### Что подготовлено в инфраструктуре:
- Маршрут зарегистрирован в навигации
- Страница существует в структуре проекта
- Layout настроен корректно

#### Что НЕ реализовано:
- API endpoints для трендов
- Таблицы БД для трендовых данных
- Логика определения трендов
- UI-компоненты для отображения

---

### 1.4. Раздел Reports

**Страница:** `/reports` (`src/app/(dashboard)/reports/page.tsx`)

#### Текущее состояние:
- **Статус:** Заглушка "Coming Soon"
- **Реализованный функционал:** Нет
- **UI:** Пустая карточка с сообщением "Report generation features will be available soon"

#### Что подготовлено в инфраструктуре:
- Маршрут зарегистрирован в навигации
- Страница существует в структуре проекта
- Layout настроен корректно

#### Что НЕ реализовано:
- API endpoints для генерации отчётов
- Логика экспорта данных (PDF/Excel/JSON)
- Шаблоны отчётов
- UI для конфигурации отчётов

---
