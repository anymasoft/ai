# 📋 Документация проекта CardMaker

**Дата обновления:** 01.01.2026
**Версия:** 1.0.0
**Язык интерфейса:** Русский
**Статус:** Активная разработка

---

## 🎯 Назначение проекта

**CardMaker** — веб-приложение для создания SEO-оптимизированных карточек товаров для маркетплейсов (Ozon, Wildberries и др.).

**Основной функционал:**
- Создание карточек товаров (название, описание, SEO-ключи)
- Массовая подготовка карточек (CSV/XLSX импорт)
- История и редактирование созданных карточек
- Управление подписками и тарифами
- Admin-панель для управления пользователями и платежами

---

## 🏗️ Архитектура и технологический стек

### Backend
```
- Next.js 15.4.7 (App Router + API Routes)
- SQLite (libsql/client@0.15.15) - база данных
- NextAuth.js v4.24.11 - аутентификация (Google OAuth)
- YooKassa - платежи (Яндекс Касса)
- OpenAI @6.9.1 - AI функции
- TypeScript
```

### Frontend
```
- React 19.1.0 + React DOM 19.1.0
- TypeScript
- Tailwind CSS 4.1.11 + PostCSS
- Radix UI (базовые UI компоненты)
  - Accordion, Alert Dialog, Avatar, Badge, Button
  - Card, Checkbox, Collapsible, Command, Dialog
  - Dropdown Menu, Hover Card, Input, Label, Popover
  - Progress, Radio Group, Scroll Area, Select, Separator
  - Sidebar, Skeleton, Switch, Table, Tabs, Toggle
  - Tooltip и другие
- Lucide React (иконки)
- next-themes (переключение тем light/dark/system)
```

### Состояние приложения
```
- React Context API (SidebarContext, ThemeContext, I18nContext)
- NextAuth Session (данные пользователя)
- React Hooks (useState, useEffect, useContext)
- Custom Hooks (useUser, useCheckPaymentStatus, useSidebarConfig)
- Zustand (установлен, но не используется активно)
```

### Утилиты и библиотеки
```
- react-hook-form @7.62.0 - управление формами
- zod @4.0.15 - валидация данных
- date-fns @4.1.0 - работа с датами
- react-markdown @10.1.0 - отображение markdown
- @react-pdf/renderer @4.3.1 - генерация PDF
- html2canvas @1.4.1 - скриншоты
- pdf-lib @1.17.1 - работа с PDF
- sonner @2.0.7 - toast уведомления
- recharts 2.15.4 - графики и диаграммы
- class-variance-authority @0.7.1 - стилизация вариантов
- clsx @2.1.1 - условные CSS классы
- tailwind-merge @3.3.1 - слияние Tailwind классов
- @dnd-kit/* - drag and drop функциональность
- @tanstack/react-table @8.21.3 - управление таблицами
```

---

## 📂 Структура папок проекта

```
CardMaker/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── (auth)/                   # Группа аутентификации
│   │   │   ├── sign-in/              # Вход (3 версии)
│   │   │   ├── sign-up/              # Регистрация (3 версии)
│   │   │   ├── forgot-password/      # Восстановление (3 версии)
│   │   │   └── errors/               # Страницы ошибок
│   │   │
│   │   ├── (dashboard)/              # Основной функционал (защищенные)
│   │   │   ├── card-generator/       # 🔴 ГЛАВНАЯ СТРАНИЦА - создание карточек
│   │   │   ├── cards/[id]/           # Просмотр и редактирование карточки
│   │   │   ├── cards-history/        # История всех карточек
│   │   │   ├── batch/                # Массовая обработка
│   │   │   ├── validate/             # Проверка описания на требования маркетплейса
│   │   │   ├── settings/
│   │   │   │   ├── billing/          # Тарифы и платежи
│   │   │   │   ├── account/          # Аккаунт
│   │   │   │   ├── appearance/       # Оформление
│   │   │   │   ├── user/             # Профиль пользователя
│   │   │   │   ├── notifications/    # Уведомления
│   │   │   │   └── connections/      # Подключения
│   │   │   ├── admin/
│   │   │   │   ├── messages/         # Сообщения от пользователей
│   │   │   │   ├── messages/[id]/    # Детали сообщения
│   │   │   │   ├── payments/         # Управление платежами
│   │   │   │   ├── users/            # Управление пользователями
│   │   │   │   ├── limits/           # Управление лимитами
│   │   │   │   ├── styles/           # Стили карточек
│   │   │   │   └── marketplace-rules/# Правила маркетплейсов
│   │   │   ├── feedback/             # Обратная связь
│   │   │   ├── mail/                 # Почта
│   │   │   ├── faqs/                 # Часто задаваемые вопросы
│   │   │   ├── calendar/             # Календарь
│   │   │   └── layout.tsx            # Layout для dashboard
│   │   │
│   │   ├── api/                      # API endpoints
│   │   │   ├── auth/[...nextauth]/   # NextAuth
│   │   │   ├── auth/check-user-status
│   │   │   ├── user/                 # Получение данных пользователя
│   │   │   ├── user/language/        # Сохранение языка
│   │   │   ├── payments/yookassa/    # YooKassa платежи (create, check, confirm, webhook)
│   │   │   ├── payments/user-history/# История платежей
│   │   │   ├── billing/script-usage/ # Информация об использовании
│   │   │   ├── admin/messages/*      # CRUD сообщений
│   │   │   ├── admin/payments/       # Платежи
│   │   │   ├── admin/users/          # Пользователи
│   │   │   ├── admin/limits/         # Лимиты
│   │   │   └── feedback/             # Обратная связь
│   │   │
│   │   ├── page.tsx                  # Главная (редирект)
│   │   ├── landing/                  # Лендинг страница
│   │   ├── auth-callback/            # OAuth callback
│   │   ├── auth/google-signin/       # Google вход
│   │   ├── auth/check-disabled/      # Проверка статуса
│   │   ├── layout.tsx                # Root layout
│   │   ├── metadata.ts               # Метаданные
│   │   └── globals.css               # Глобальные стили
│   │
│   ├── components/                   # React компоненты
│   │   ├── ui/                       # shadcn/ui компоненты (Button, Input, Card и т.д.)
│   │   ├── layouts/                  # Layout компоненты
│   │   │   ├── app-sidebar.tsx       # Боковая панель навигации
│   │   │   ├── site-header.tsx       # Верхняя навигация
│   │   │   ├── site-footer.tsx       # Подвал
│   │   │   ├── dashboard-client-wrapper.tsx  # Client wrapper для dashboard
│   │   │   └── nav-*.tsx             # Компоненты навигации
│   │   ├── landing/                  # Компоненты лендинга
│   │   ├── theme-customizer/         # Кастомизация темы
│   │   ├── PricingPlans.tsx          # Карточки тарифов
│   │   ├── UpgradeToProButton.tsx    # Кнопка upgrade
│   │   ├── CommandSearch.tsx         # Поиск (Cmd+K)
│   │   ├── ModeToggle.tsx            # Переключатель темы
│   │   ├── AdminMessagesMenuButton.tsx # Кнопка для admin сообщений
│   │   ├── SidebarNotification.tsx   # Уведомления в sidebar
│   │   └── другие компоненты...
│   │
│   ├── contexts/                     # React Context
│   │   ├── sidebar-context.tsx       # Управление сайдбаром
│   │   └── theme-context.ts          # Управление темой
│   │
│   ├── providers/                    # Context Providers
│   │   ├── I18nProvider.tsx          # Локализация (EN/RU)
│   │   └── AuthProvider.tsx          # NextAuth SessionProvider
│   │
│   ├── lib/                          # Утилиты и функции
│   │   ├── auth.ts                   # NextAuth конфигурация
│   │   ├── db.ts                     # SQLite инициализация и функции БД
│   │   ├── payments.ts               # Логика YooKassa платежей
│   │   ├── user-plan.ts              # Получение плана пользователя
│   │   ├── script-usage.ts           # Управление лимитами
│   │   ├── subscription-downgrade.ts # Понижение подписки
│   │   ├── date-formatting.ts        # Форматирование дат
│   │   ├── momentum-formatting.ts    # Форматирование метрик
│   │   ├── fonts.ts                  # Настройка шрифтов
│   │   ├── i18n.ts                   # Конфиг локализации
│   │   ├── utils.ts                  # Общие утилиты
│   │   ├── admin-api.ts              # Admin API функции
│   │   ├── admin-config.ts           # Admin конфигурация
│   │   ├── get-user-language-server.ts # Получение языка пользователя
│   │   └── pdf-generator.ts          # Генерация PDF
│   │
│   ├── i18n/                         # Локализация
│   │   ├── ru.ts                     # Русский язык
│   │   └── en.ts                     # Английский язык
│   │
│   ├── assets/                       # Статические ресурсы
│   │   └── изображения, иконки...
│   │
│   ├── middleware.ts                 # Next.js middleware
│   ├── instrumentation.ts            # Instrumentation
│   └── providers.tsx                 # Глобальные providers
│
├── migrations/                       # Миграции БД
├── scripts/
│   ├── migrate.ts                    # Скрипт миграции
│   ├── migrate.js                    # JS версия миграции
│   └── check-db-structure.ts         # Проверка структуры БД
│
├── public/                           # Публичные файлы
│   └── favicon.ico, логотипы и т.д.
│
├── next.config.ts                    # Next.js конфигурация
├── tsconfig.json                     # TypeScript конфигурация
├── tailwind.config.ts                # Tailwind CSS конфигурация
├── postcss.config.mjs                # PostCSS конфигурация
├── components.json                   # shadcn/ui конфигурация
├── package.json                      # NPM зависимости
├── pnpm-lock.yaml                    # pnpm lockfile
└── PROJECT_STRUCTURE.md              # ЭТА ДОКУМЕНТАЦИЯ
```

---

## 🗄️ База данных (SQLite)

### Таблицы

#### `users`
```sql
- id: string (PRIMARY KEY)
- email: string (UNIQUE)
- name: string | NULL
- emailVerified: timestamp | NULL
- image: string | NULL
- role: 'user' | 'admin' (DEFAULT 'user')
- plan: 'free' | 'basic' | 'professional' | 'enterprise'
- language: 'ru' | 'en'
- disabled: boolean (DEFAULT false)
- expiresAt: timestamp | NULL
- paymentProvider: string | NULL
- createdAt: timestamp
- updatedAt: timestamp
```

#### `product_cards`
```sql
- id: string (PRIMARY KEY)
- userId: string (FOREIGN KEY -> users.id)
- title: string
- description: text
- marketplace: 'ozon' | 'wildberries' | 'yandex_market'
- category: string | NULL
- seoKeywords: string | NULL
- style: string | NULL
- competitors: string | NULL (JSON)
- createdAt: timestamp
- updatedAt: timestamp
```

#### `payments`
```sql
- id: string (PRIMARY KEY)
- userId: string (FOREIGN KEY -> users.id)
- planId: string
- externalPaymentId: string (YooKassa payment ID)
- amount: number
- currency: string
- status: 'pending' | 'succeeded' | 'failed'
- billingCycle: 'monthly' | 'yearly'
- expiresAt: timestamp
- createdAt: timestamp
```

#### `admin_messages`
```sql
- id: string (PRIMARY KEY)
- email: string
- firstName: string | NULL
- lastName: string | NULL
- subject: string
- message: text
- isRead: boolean (DEFAULT false)
- createdAt: timestamp
```

#### Другие таблицы
- `accounts` - OAuth аккаунты (NextAuth)
- `sessions` - Сессии (NextAuth)
- `verificationTokens` - Токены верификации
- `user_usage_daily` - Суточное использование лимитов
- `admin_subscriptions` - Ручное управление подписками
- `system_flags` - Флаги системы

---

## 🔐 Аутентификация

### Провайдер
- **Google OAuth** через NextAuth.js v4

### Процесс
1. Пользователь кликает "Войти с Google"
2. Редирект на Google Sign-In
3. NextAuth обрабатывает callback
4. **signIn callback:**
   - Проверяет наличие пользователя в БД
   - Создает пользователя если не существует
   - Проверяет `users.disabled` статус
   - Если disabled - отказывает доступ
5. **jwt callback:**
   - Добавляет `id` и `role` в JWT token
6. **session callback:**
   - Передает `id` и `role` в session

### Сессия
```javascript
{
  user: {
    id: string,
    email: string,
    name: string,
    image: string,
    role: 'user' | 'admin'
  },
  expires: string // ISO date
}
```

### Защита маршрутов
- Все страницы в `/(dashboard)` требуют аутентификации
- Middleware проверяет `session.user` перед доступом
- Если нет сессии → редирект на `/sign-in`

---

## 💳 Платежи (YooKassa)

### Система платежей
- **Провайдер:** YooKassa (Яндекс Касса)
- **Таблица:** `payments`
- **Биллинг:** Monthly/Yearly

### Тарифы

| План | Цена | Карточек в месяц | Описание |
|------|------|------------------|---------|
| Free | Бесплатно | 3 | Базовый функционал |
| Basic | N руб/мес | 50 | Стандартный функционал |
| Professional | N руб/мес | 500 | Расширенный функционал |
| Enterprise | По запросу | Неограниченно | Полный функционал + поддержка |

### Процесс платежа
1. Пользователь выбирает тариф на `/settings/billing`
2. `POST /api/payments/yookassa/create` - создание платежа
3. YooKassa возвращает URL для оплаты
4. Пользователь переходит на YooKassa и платит
5. `POST /api/payments/yookassa/webhook` - YooKassa уведомляет сервер
6. `updateUserPlan()` - обновляет `users.plan` и `users.expiresAt`
7. Сброс суточных лимитов в `user_usage_daily`

### API Endpoints платежей
```
POST   /api/payments/yookassa/create      Создание платежа
GET    /api/payments/yookassa/check       Проверка статуса
POST   /api/payments/yookassa/confirm     Подтверждение платежа
POST   /api/payments/yookassa/webhook     Webhook от YooKassa
POST   /api/payments/yookassa/webhook-test Тестирование webhook
GET    /api/payments/user-history         История платежей пользователя
GET    /api/billing/script-usage          Информация об использовании лимитов
```

---

## 🎛️ Управление состоянием (State Management)

### React Context API

#### 1. SidebarContext
```typescript
{
  variant: "sidebar" | "floating" | "inset"  // текущее: "inset"
  collapsible: "offcanvas" | "icon" | "none" // текущее: "offcanvas"
  side: "left" | "right"                     // текущее: "left"
  updateConfig(partial: Partial<SidebarConfig>): void
}
```
**Файл:** `/src/contexts/sidebar-context.tsx`
**Использование:** Управление состоянием и стилем боковой панели

#### 2. ThemeContext
```typescript
{
  theme: "dark" | "light" | "system"
  setTheme(theme: string): void
}
```
**Файл:** `/src/contexts/theme-context.ts`
**Использование:** Переключение темы приложения

#### 3. I18nContext
```typescript
{
  lang: Language  // 'en' | 'ru'
  dict: Record<string, any>
}
```
**Файл:** `/src/providers/I18nProvider.tsx`
**Использование:** Локализация интерфейса

#### 4. NextAuth Session
```typescript
{
  user: {
    id: string
    email: string
    name: string
    image: string
    role: 'user' | 'admin'
  }
  expires: string
}
```
**Использование:** Данные аутентифицированного пользователя

### Custom Hooks
- `useUser()` - получение текущего пользователя
- `useCheckPaymentStatus()` - проверка статуса платежа
- `useSidebarConfig()` - получение конфиги сайдбара
- `useRouter()` - Next.js routing

---

## 🎨 Дизайн и UI

### Тема (Light/Dark)
- **Provider:** next-themes
- **Переключатель:** ModeToggle компонент в header
- **Хранилище:** localStorage (ключ: `theme`)
- **Значения:** `light`, `dark`, `system`

### Компоненты UI
Все компоненты из **shadcn/ui** + **Radix UI**:

**Основные:**
- Button, Input, Textarea, Select, Badge, Card
- Table, Separator, Skeleton, Spinner

**Диалоги:**
- Dialog, Drawer, AlertDialog, Popover, Hover Card

**Форма:**
- Label, Checkbox, Radio, Switch, Toggle, Toggle Group

**Навигация:**
- Sidebar, Tabs, Breadcrumb, Navigation Menu

**Другое:**
- Accordion, Collapsible, Scroll Area, Tooltip, Progress

### Иконки
- **Библиотека:** Lucide React
- **Количество:** 500+
- **Использование:** Импорт `import { IconName } from 'lucide-react'`

### CSS Framework
- **Tailwind CSS 4.1.11**
- **PostCSS для обработки**
- **Кастомные переменные в globals.css**

---

## 📱 Основные страницы и функционал

### 🔴 Главная функция - Генератор карточек

**URL:** `/card-generator`
**Тип:** Client Component
**Назначение:** Создание карточки товара

**Форма содержит:**
- Input - название товара
- Textarea - описание товара
- Select - маркетплейс (Ozon, Wildberries)
- Input - категория товара
- Input - SEO-ключи
- Select - стиль карточки
- Textarea - конкуренты

**Результат:**
- Отображение сгенерированной карточки
- Возможность копирования в буфер обмена
- Сохранение в историю (`product_cards`)

---

### Проверка описания

**URL:** `/validate`
**Тип:** Client Component
**Назначение:** Валидация описания товара перед публикацией на маркетплейсе

**Дизайн:**
- IDE-like двухколоночный layout (как Visual Studio Code)
- Левая колонка (~68%): Textarea для ввода
- Правая колонка (~32%): Панель результатов
- Все на одном экране (без необходимости скроллить)
- Согласован по стилю с `/card-generator`

**Функционал:**
- Вставка описания в большое textarea (min-height: 560px, resize-y)
- Проверка на соответствие требованиям Ozon/Wildberries
- Валидация:
  - Длина описания (100-4000 символов)
  - Отсутствие запрещённых маркетинговых слов
  - Наличие абзацев для читаемости
  - Наличие основных характеристик товара
  - Отсутствие лишних символов

**Результаты валидации (в правой колонке):**
- **ДО проверки:** Placeholder "Здесь появятся результаты проверки вашего описания"
- **✅ Успех:** зелёная иконка CheckCircle2 + сообщение "Проверка пройдена"
- **❌ Ошибка:** красная иконка AlertCircle + список проблем + запрещённые слова в виде Badge
- Правая колонка всегда видна (overflow-y-auto если контент больше)
- Тестовый переключатель "Demo: ошибка" для целей разработки (в левой колонке)

---

### История карточек

**URL:** `/cards-history`
**Функционал:**
- Таблица всех созданных карточек
- Фильтр по маркетплейсу
- Сортировка по дате
- Кнопка копирования
- Кнопка перегенерации
- Удаление карточки

---

### Массовая обработка

**URL:** `/batch`
**Функционал:**
- Загрузка CSV/XLSX файла
- Вставка списка товаров (текст)
- Обработка пакета
- Экспорт результатов
- Просмотр статуса обработки

---

### Биллинг и тарифы

**URL:** `/settings/billing`
**Функционал:**
- Отображение текущего плана
- Progress bar использования карточек
- История платежей в таблице
- Выбор и смена тарифа
- Интеграция с YooKassa

---

### Admin-панель

#### Сообщения
**URL:** `/admin/messages`
**Функционал:**
- Таблица сообщений от пользователей
- Фильтр по email, статусу (прочитано/не прочитано)
- Пагинация
- Отметить как прочитано
- Удаление сообщения
- Просмотр деталей сообщения

#### Платежи
**URL:** `/admin/payments`
**Функционал:**
- История всех платежей
- Статус платежа (pending, succeeded, failed)
- Информация о пользователе
- Продление подписки
- Фильтр и сортировка

#### Пользователи
**URL:** `/admin/users`
**Функционал:**
- Список всех пользователей
- Статус (активный/отключен)
- Текущий тариф
- Смена тарифа пользователю
- Сброс лимитов

#### Лимиты
**URL:** `/admin/limits`
**Функционал:**
- Конфигурация лимитов по тарифам
- Редактирование количества карточек в месяц
- Редактирование других параметров лимитов

---

## 📊 API Endpoints полный список

### Аутентификация (2)
```
POST   /api/auth/[...nextauth]           NextAuth callback
GET    /api/auth/check-user-status       Проверка статуса
```

### Пользователь (2)
```
GET    /api/user                         Получить данные пользователя
POST   /api/user/language                Сохранить язык
```

### Платежи (7)
```
POST   /api/payments/yookassa/create     Создать платеж
GET    /api/payments/yookassa/check      Проверить статус
POST   /api/payments/yookassa/confirm    Подтвердить платеж
POST   /api/payments/yookassa/webhook    Webhook YooKassa
POST   /api/payments/yookassa/webhook-test Тест webhook
GET    /api/payments/user-history        История платежей
GET    /api/billing/script-usage         Использование лимитов
```

### Admin - Сообщения (7)
```
GET    /api/admin/messages               Список сообщений
POST   /api/admin/messages               Создать сообщение
GET    /api/admin/messages/[id]          Получить сообщение
PATCH  /api/admin/messages/[id]          Обновить сообщение
DELETE /api/admin/messages/[id]          Удалить сообщение
PATCH  /api/admin/messages/[id]/read     Отметить как прочитано
GET    /api/admin/messages/unread-count  Количество непрочитанных
```

### Admin - Платежи (3)
```
GET    /api/admin/payments               Все платежи
GET    /api/admin/payments/[id]          Платеж
POST   /api/admin/payments/extend        Продлить подписку
```

### Admin - Пользователи (3)
```
GET    /api/admin/users                  Все пользователи
POST   /api/admin/users/change-plan      Смена тарифа
POST   /api/admin/users/reset-limits     Сброс лимитов
```

### Другое (2)
```
POST   /api/feedback                     Отправить обратную связь
GET    /api/admin/limits                 Конфигурация лимитов
```

**Итого:** 23 API endpoint

---

## 🚀 Как запустить проект

### Требования
- Node.js 18+ (текущая версия в .nvmrc)
- npm / pnpm

### Установка зависимостей
```bash
pnpm install
```

### Переменные окружения
Создать файл `.env.local`:
```
# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key

# Google OAuth
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

# Database
DATABASE_URL=your-sqlite-db-url

# YooKassa
YOOKASSA_SHOP_ID=...
YOOKASSA_API_KEY=...

# OpenAI
OPENAI_API_KEY=...
```

### Запуск dev сервера
```bash
pnpm dev
```
Откроется: http://localhost:3000

### Build
```bash
pnpm build
pnpm start
```

### Linting
```bash
pnpm lint
```

---

## 📝 Разработка и изменения

### Когда добавляешь новую страницу
1. Создай папку в `src/app/(dashboard)/[page-name]/`
2. Создай `page.tsx` файл
3. Если требуется аутентификация - уже защищено (в `(dashboard)`)
4. Обновить sidebar навигацию в `components/layouts/nav-main.tsx`
5. **Обновить этот файл документации!**

### Когда добавляешь новый API endpoint
1. Создай маршрут в `src/app/api/[path]/route.ts`
2. Добавить обработчик (GET, POST, etc.)
3. Добавить проверку аутентификации если нужна
4. Добавить валидацию данных (zod)
5. **Обновить раздел "API Endpoints" в этом файле!**

### Когда меняешь БД
1. Обновить схему в `src/lib/db.ts` (`initializeDatabase()`)
2. Создать миграцию в `migrations/`
3. Запустить: `pnpm ts-node scripts/migrate.ts`
4. **Обновить раздел "База данных" в этом файле!**

### Когда добавляешь новый компонент
1. Создать файл в `src/components/`
2. Использовать `'use client'` если требуется interactivity
3. Типизировать props с TypeScript
4. **При добавлении основного компонента - обновить этот файл!**

### Когда меняешь состояние приложения
1. Обновить соответствующий context файл
2. Обновить типы в `.d.ts` файлах если нужно
3. **Обновить раздел "State Management" в этом файле!**

---

## 🔗 Важные файлы

| Файл | Назначение |
|------|-----------|
| `src/lib/auth.ts` | Конфигурация NextAuth |
| `src/lib/db.ts` | SQLite инициализация и запросы |
| `src/lib/payments.ts` | Логика YooKassa платежей |
| `src/contexts/sidebar-context.tsx` | Состояние сайдбара |
| `src/contexts/theme-context.ts` | Состояние темы |
| `src/providers/I18nProvider.tsx` | Локализация |
| `src/components/layouts/dashboard-client-wrapper.tsx` | Layout dashboard |
| `src/components/layouts/app-sidebar.tsx` | Боковая панель |
| `next.config.ts` | Next.js конфиги |
| `tailwind.config.ts` | Tailwind конфиги |
| `tsconfig.json` | TypeScript конфиги |

---

## 🐛 Известные проблемы и TODO

*Этот раздел обновляется при обнаружении проблем*

---

## 📞 Контакты и поддержка

- **Обратная связь:** `/feedback`
- **Admin сообщения:** `/admin/messages`
- **Email поддержка:** (настроить)

---

**Последнее обновление:** 01.01.2026
**Следующая проверка:** При каждом значительном изменении кода
