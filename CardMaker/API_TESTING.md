# Тестирование API генерации и валидации CardMaker

## ⚠️ Предварительные условия

1. **Запустить сервер в режиме разработки:**
   ```bash
   npm run dev
   ```
   Сервер будет доступен на http://localhost:3000

2. **Установить переменные окружения в `.env.local`:**
   ```bash
   OPENAI_API_KEY=sk-...your-api-key...
   OPENAI_MODEL=gpt-4.1-mini
   ```
   - `OPENAI_API_KEY` — обязательно, API ключ OpenAI
   - `OPENAI_MODEL` — опционально, по умолчанию `gpt-4.1-mini`

3. **Убедиться что вы авторизованы в приложении**
   - Откройте http://localhost:3000/sign-in
   - Авторизуйтесь через Google
   - Система должна создать вашего пользователя в БД

4. **Получить cookie сессии для curl запросов**
   - Откройте DevTools (F12) → Network → перезагрузите страницу
   - Найдите запрос к http://localhost:3000/
   - В "Cookies" скопируйте значение `next-auth.session-token`
   - Сохраните его в переменную, например: `SESSION_COOKIE=...`

---

## 🚀 Endpoint 1: Генерация описания товара

### URL
```
POST http://localhost:3000/api/generate-card
```

### Headers
```
Content-Type: application/json
Cookie: next-auth.session-token=YOUR_SESSION_COOKIE
```

### Request Body

```json
{
  "productTitle": "Смарт-часы с AMOLED экраном",
  "productCategory": "Электроника",
  "marketplace": "ozon",
  "style": "selling",
  "additionalNotes": "Водонепроницаемые, GPS, батарея 7 дней"
}
```

### Параметры

| Параметр | Тип | Обязательно | Описание |
|----------|-----|-------------|---------|
| `productTitle` | string | ✅ | Название товара (макс. 500 символов) |
| `productCategory` | string | ✅ | Категория товара (макс. 200 символов) |
| `marketplace` | enum | ✅ | `ozon` или `wildberries` |
| `style` | enum | ✅ | `selling`, `expert` или `brief` |
| `additionalNotes` | string | ❌ | Дополнительная информация (макс. 1000 символов) |

### Стили описания

- **`selling`** - Продажный стиль с акцентом на преимущества
- **`expert`** - Экспертный стиль с техническими подробностями
- **`brief`** - Краткий стиль с самой важной информацией

### Успешный ответ (200 OK)

```json
{
  "success": true,
  "data": {
    "title": "Смарт-часы с AMOLED экраном - лучший выбор для активного образа жизни",
    "description": "Наши смарт-часы с передовым AMOLED экраном обеспечивают четкое изображение при любом освещении. Встроенный GPS позволит вам отслеживать маршруты пробежек и прогулок...",
    "usage": {
      "inputTokens": 542,
      "outputTokens": 328,
      "totalTokens": 870
    },
    "generatedAt": "2025-01-01T12:34:56.789Z"
  }
}
```

### Ошибки

**401 Unauthorized** - Требуется авторизация
```json
{
  "error": "Требуется авторизация"
}
```

**400 Bad Request** - Ошибка валидации
```json
{
  "error": "Ошибка валидации: productTitle: Название товара обязательно"
}
```

**429 Too Many Requests** - Превышен лимит генераций
```json
{
  "error": "Превышен месячный лимит генераций",
  "code": "LIMIT_EXCEEDED"
}
```

**500 Internal Server Error** - Ошибка при генерации
```json
{
  "error": "OpenAI API ошибка: message"
}
```

---

### 📝 Примеры curl команд

#### Пример 1: Генерация в стиле "selling" для Озона

```bash
curl -X POST http://localhost:3000/api/generate-card \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
  -d '{
    "productTitle": "Смарт-часы с AMOLED экраном",
    "productCategory": "Электроника",
    "marketplace": "ozon",
    "style": "selling",
    "additionalNotes": "Водонепроницаемые, GPS, батарея 7 дней"
  }'
```

#### Пример 2: Генерация в стиле "expert" для WildBerries

```bash
curl -X POST http://localhost:3000/api/generate-card \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
  -d '{
    "productTitle": "Мобильный процессор Snapdragon 8 Gen 3",
    "productCategory": "Компоненты ПК",
    "marketplace": "wildberries",
    "style": "expert"
  }'
```

#### Пример 3: Быстрая генерация в стиле "brief"

```bash
curl -X POST http://localhost:3000/api/generate-card \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
  -d '{
    "productTitle": "USB-C кабель 2м",
    "productCategory": "Аксессуары",
    "marketplace": "ozon",
    "style": "brief"
  }'
```

---

## 🔍 Endpoint 2: Валидация описания товара

### URL
```
POST http://localhost:3000/api/validate-text
```

### Headers
```
Content-Type: application/json
Cookie: next-auth.session-token=YOUR_SESSION_COOKIE
```

### Request Body

```json
{
  "description": "Смарт-часы с AMOLED экраном. Водонепроницаемые, GPS, батарея 7 дней. Идеальны для спортсменов и активных людей.",
  "marketplace": "ozon"
}
```

### Параметры

| Параметр | Тип | Обязательно | Описание |
|----------|-----|-------------|---------|
| `description` | string | ✅ | Описание товара для валидации (макс. 5000 символов) |
| `marketplace` | enum | ✅ | `ozon` или `wildberries` |

### Успешный ответ (200 OK)

```json
{
  "success": true,
  "data": {
    "isValid": true,
    "score": 92,
    "issues": [
      {
        "type": "clarity",
        "severity": "info",
        "message": "Описание может быть более детальным для категории Электроника",
        "suggestion": "Добавьте информацию о совместимости и технических характеристиках"
      }
    ],
    "summary": "Найдено 1 замечание общего характера",
    "validatedAt": "2025-01-01T12:34:56.789Z"
  }
}
```

### Ответ с ошибками валидации

```json
{
  "success": true,
  "data": {
    "isValid": false,
    "score": 45,
    "issues": [
      {
        "type": "forbidden_words",
        "severity": "error",
        "message": "Найдены запрещённые маркетинговые слова",
        "suggestion": "Замените 'идеальны' на 'хорошие' или 'надежные'"
      },
      {
        "type": "grammar",
        "severity": "warning",
        "message": "Найдена ошибка пунктуации",
        "suggestion": "Добавьте точку в конце предложения"
      }
    ],
    "summary": "Найдено 1 критических проблем(ы). Найдено 1 предупреждения(я)",
    "validatedAt": "2025-01-01T12:34:56.789Z"
  }
}
```

### Типы проблем при валидации

| Тип | Severity | Описание |
|-----|----------|---------|
| `forbidden_words` | error/warning | Найдены запрещённые слова из stop-words |
| `grammar` | warning/info | Грамматические или пунктуационные ошибки |
| `requirements` | error | Нарушение требований маркетплейса |
| `exaggeration` | error/warning | Неоправданное преувеличение |
| `clarity` | warning/info | Недостаточная ясность описания |
| `other` | warning/info | Другие проблемы |

### 📝 Примеры curl команд

#### Пример 1: Валидация для Озона

```bash
curl -X POST http://localhost:3000/api/validate-text \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
  -d '{
    "description": "Смарт-часы AMOLED. Водонепроницаемые. GPS, батарея 7 дней. Совместимы с iOS и Android.",
    "marketplace": "ozon"
  }'
```

#### Пример 2: Валидация для WildBerries

```bash
curl -X POST http://localhost:3000/api/validate-text \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
  -d '{
    "description": "Премиум мобильный процессор. Архитектура Cortex-X4. Частота до 3.4 ГГц. 4нм техпроцесс. Поддержка 5G.",
    "marketplace": "wildberries"
  }'
```

---

## 🧪 End-to-End тестирование

### Сценарий 1: Полный цикл генерации и валидации

1. **Шаг 1: Сгенерировать описание**
   ```bash
   RESPONSE=$(curl -s -X POST http://localhost:3000/api/generate-card \
     -H "Content-Type: application/json" \
     -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
     -d '{
       "productTitle": "Беспроводные наушники",
       "productCategory": "Аудиотехника",
       "marketplace": "ozon",
       "style": "selling"
     }')

   echo $RESPONSE | jq '.'
   ```

2. **Шаг 2: Извлечь сгенерированное описание и провалидировать**
   ```bash
   DESCRIPTION=$(echo $RESPONSE | jq -r '.data.description')

   curl -s -X POST http://localhost:3000/api/validate-text \
     -H "Content-Type: application/json" \
     -H "Cookie: next-auth.session-token=YOUR_SESSION_COOKIE" \
     -d "{
       \"description\": \"$DESCRIPTION\",
       \"marketplace\": \"ozon\"
     }" | jq '.'
   ```

### Сценарий 2: Тестирование разных стилей

```bash
#!/bin/bash

SESSION_COOKIE="your_session_cookie"
PRODUCT="iPhone 16 Pro"
CATEGORY="Смартфоны"

for STYLE in selling expert brief; do
  echo "=== Тестирование стиля: $STYLE ==="

  curl -s -X POST http://localhost:3000/api/generate-card \
    -H "Content-Type: application/json" \
    -H "Cookie: next-auth.session-token=$SESSION_COOKIE" \
    -d "{
      \"productTitle\": \"$PRODUCT\",
      \"productCategory\": \"$CATEGORY\",
      \"marketplace\": \"ozon\",
      \"style\": \"$STYLE\"
    }" | jq '.data | {title, descriptionLength: (.description | length)}'

  echo ""
done
```

### Сценарий 3: Тестирование обоих маркетплейсов

```bash
#!/bin/bash

SESSION_COOKIE="your_session_cookie"

for MARKETPLACE in ozon wildberries; do
  echo "=== Тестирование маркетплейса: $MARKETPLACE ==="

  curl -s -X POST http://localhost:3000/api/generate-card \
    -H "Content-Type: application/json" \
    -H "Cookie: next-auth.session-token=$SESSION_COOKIE" \
    -d "{
      \"productTitle\": \"Смарт-часы\",
      \"productCategory\": \"Электроника\",
      \"marketplace\": \"$MARKETPLACE\",
      \"style\": \"selling\"
    }" | jq '.data | {title, marketplace: "'$MARKETPLACE'"}'

  echo ""
done
```

---

## 🔧 Отладка

### Проверить переменную окружения OPENAI_API_KEY

```bash
# Linux/Mac
echo $OPENAI_API_KEY

# Windows PowerShell
$env:OPENAI_API_KEY
```

### Проверить логи сервера

В терминале где запущен `npm run dev` вы должны увидеть логи типа:
```
[generateProductCard] Генерируем описание для "Смарт-часы"...
[callOpenAI] Вызываем OpenAI API с моделью gpt-4o-mini
[validateProductDescription] Валидируем описание для маркетплейса ozon
```

### Проверить что БД инициализирована

Перейдите в админ-панель:
- http://localhost:3000/admin/system-prompts
- http://localhost:3000/admin/styles
- http://localhost:3000/admin/marketplace-rules
- http://localhost:3000/admin/stop-words

Все конфигурационные таблицы должны быть заполнены.

---

## 📊 Мониторинг использования

Использование токенов можно отслеживать из ответа API:

```json
{
  "usage": {
    "inputTokens": 542,      // Токены на входе
    "outputTokens": 328,     // Токены на выходе
    "totalTokens": 870       // Общее количество токенов
  }
}
```

Для оценки стоимости используйте [OpenAI Pricing](https://openai.com/pricing):
- gpt-4o-mini: $0.15 per 1M input tokens, $0.60 per 1M output tokens

**Примерная стоимость одного запроса:**
- Input: 542 tokens × ($0.15 / 1M) = $0.00008
- Output: 328 tokens × ($0.60 / 1M) = $0.0002
- **Итого: ~$0.0003 per generation**

---

## ⚠️ Возможные ошибки

### 1. "OPENAI_API_KEY не установлен"
**Решение:** Добавьте `OPENAI_API_KEY` в файл `.env.local`

### 2. "Требуется авторизация"
**Решение:** Убедитесь что передали корректный cookie `next-auth.session-token`

### 3. "OpenAI API ошибка: 401 Authentication error"
**Решение:** Проверьте что API ключ правильный и не истёк

### 4. "OpenAI API ошибка: 429 Rate limit exceeded"
**Решение:** Подождите несколько секунд перед следующим запросом

### 5. "Превышен месячный лимит генераций"
**Решение:** Проверьте тариф пользователя в админ-панели `/admin/users`

---

## 📚 Дополнительная информация

### Системные промпты
System prompts хранятся в таблице `system_prompts` и могут быть изменены в админ-панели:
- `gen_base` - базовый промпт для генерации
- `validate_base` - базовый промпт для валидации

### Стили описаний
Стили хранятся в таблице `styles`:
- `selling` - акцент на преимущества и продажи
- `expert` - технические подробности
- `brief` - краткость и лаконичность

### Правила маркетплейсов
Правила хранятся в таблице `marketplace_rules`:
- `ozon` - требования Озона
- `wildberries` - требования WildBerries

### Стоп-слова
Стоп-слова хранятся в таблице `stop_words` с категориями:
- `marketing` - маркетинговые преувеличения
- `health` - медицинские обещания
- `prohibited` - общие запреты
- `custom` - пользовательские слова
