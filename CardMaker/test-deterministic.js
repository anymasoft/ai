/**
 * ШАГ 6: ПРОВЕРКА ПОВТОРЯЕМОСТИ
 * Простой тест для проверки что валидация работает одинаково
 */

const testDescriptions = [
  {
    marketplace: 'ozon',
    text: 'Отличные наушники для музыки'
  },
  {
    marketplace: 'wb',
    text: 'Лучший товар на всех маркетплейсах, гарантия вечная, звоните в ватсап'
  },
  {
    marketplace: 'ozon',
    text: 'Качественный браслет для фитнеса с водозащитой IP67 и GPS'
  }
]

console.log('ШАГ 6: ПРОВЕРКА ДЕТЕРМИНИРОВАННОСТИ')
console.log('=====================================')
console.log(`
Тесты для проверки:

1. ДЕТЕРМИНИРОВАННОСТЬ:
   - Один и тот же текст
   - Один маркетплейс
   - Несколько вызовов подряд
   ➜ РЕЗУЛЬТАТ ДОЛЖЕН быть идентичным

2. ЛЕНДИНГ vs /VALIDATE:
   - Один текст
   - Один маркетплейс
   - Вызов /api/validate-text/free
   - Вызов /api/validate-text
   ➜ isValid ДОЛЖНА совпадать
   ➜ score ДОЛЖЕН совпадать
   ➜ В free версии НЕ должны быть детали
   ➜ В /validate версии ДОЛЖНЫ быть детали (если есть ошибки)

3. MARKETPLACE SELECTOR:
   - Переключение селектора в /validate
   - БЕЗ выполнения новой валидации
   ➜ РЕЗУЛЬТАТ должен оставаться старым (BUG, который мы не исправляли)
   ➜ Когда нажимается "Check" - результат обновляется

СЛЕДУЮЩИЕ ШАГИ:
1. Убедиться что БД имеет system_prompts
2. Проверить через curl или Postman
3. Убедиться что temperature = 0 использует система

=====================================
`)
