# 🚨 INCIDENT RECOVERY DOCUMENT
## Восстановление утраченных данных админки

**Дата инцидента**: 2026-01-04
**Статус**: READ-ONLY анализ (БД НЕ ТРОГАЛИ)
**Режим**: Восстановление текстов и спецификаций из кода и git history

---

# 📋 СОДЕРЖАНИЕ

1. [Архитектура системы](#архитектура-системы)
2. [Пайплайн генерации описаний](#пайплайн-генерации)
3. [Пайплайн валидации](#пайплайн-валидации)
4. [Системные промпты](#системные-промпты)
5. [Стили описаний](#стили-описаний)
6. [Категории товаров](#категории-товаров)
7. [Стоп-слова и запреты](#стоп-слова)
8. [Правила маркетплейсов](#правила-маркетплейсов)
9. [Структура БД](#структура-БД)
10. [Анализ инцидента](#анализ-инцидента)

---

# 🏗️ АРХИТЕКТУРА СИСТЕМЫ

## Схема взаимодействия

```
┌─────────────────────────────────────────────────────────────────┐
│                    CARDMAKER СИСТЕМА                             │
└─────────────────────────────────────────────────────────────────┘

ПОЛЬЗОВАТЕЛЬ
    ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React)                              │
├─────────────────────────────────────────────────────────────────┤
│  - Генератор описаний (card-generator)                          │
│  - Валидатор описаний (validate page)                           │
│  - Лендинг (free validator)                                     │
└──────────────────────────────────────────────────────────────────┘
    ↓
┌──────────────────┬──────────────────┬──────────────────────┐
│   GENERATION     │   VALIDATION     │   ADMIN PANEL        │
│   PIPELINE       │   PIPELINE       │   MANAGEMENT         │
└──────────────────┴──────────────────┴──────────────────────┘
    ↓                  ↓                    ↓
    │                  │                    │
    ├─ DATABASE ──────┼────────────────────┤
    │  (SQLite)        │                    │
    │                  │                    │
    └──────────────────┴────────────────────┘

```

---

# 🎯 ПАЙПЛАЙН ГЕНЕРАЦИИ

## Общая структура

**Процесс**: Пользователь заполняет форму → система генерирует описание товара → возвращает результат

**Компоненты**:
- Frontend: `/src/app/card-generator/page.tsx`
- Backend: `/src/lib/ai-services/generation.ts`, `/src/lib/prompts/builders.ts`
- Database: `styles`, `category_prompts`, `system_prompts`, `marketplace_rules`, `stop_words`

## Входные параметры

```javascript
{
  productTitle: string         // Название товара
  productCategory: string      // Категория (electronics, fashion, home, sports, beauty, toys, books)
  marketplace: 'ozon' | 'wb'  // Маркетплейс
  style: string               // Стиль (selling, expert, brief)
  seoKeywords?: string[]      // SEO-ключи (опционально)
  competitors?: string[]      // Описания конкурентов (опционально)
  additionalNotes?: string    // Дополнительные замечания (опционально)
}
```

## Пошаговый процесс

### ЭТАП 1: Загрузка конфигурации из БД

```
loadSystemPrompts()
  → SELECT key, content FROM system_prompts WHERE is_active = 1
  → Получаем: gen_base, validate_base

loadStyles()
  → SELECT id, key, title, prompt, is_active FROM styles
  → Получаем: selling, expert, brief (с их промптами)

loadMarketplaceRules(marketplace)
  → SELECT content FROM marketplace_rules WHERE marketplace = ? AND is_active = 1
  → Получаем: правила для Ozon или WB

loadStopWords(marketplace)
  → SELECT category, words FROM stop_words
    WHERE is_active = 1 AND (marketplace IS NULL OR marketplace = ?)
  → Получаем: маркетинговые слова, запреты, медицинские обещания

loadCategoryPrompt(productCategory)
  → SELECT prompt FROM category_prompts WHERE key = ? AND is_active = 1
  → Получаем: специальная инструкция для категории
```

### ЭТАП 2: Построение системного промпта

```
BASE PROMPT (gen_base из БД)
    ↓
+ STYLE PROMPT (выбранный стиль)
    ↓
+ MARKETPLACE RULES (правила маркетплейса)
    ↓
+ CATEGORY INSTRUCTIONS (инструкции по категории)
    ↓
+ STOP WORDS (запрещённые слова)
    ↓
+ REQUIREMENTS (требования к структуре)
    ↓
= FINAL SYSTEM PROMPT
```

### ЭТАП 3: Построение пользовательского промпта

```
"Создай описание для товара:

ТОВАР:
- Название: {productTitle}
- Категория: {productCategory}

[ЕСЛИ есть SEO-ключи]
SEO-ключи для оптимизации поиска:
{seoKeywords.join(', ')}

[ЕСЛИ есть описания конкурентов]
Для вдохновения (описания товаров конкурентов):
{competitors.map((c, i) => `${i + 1}. ${c}`)}

[ЕСЛИ есть дополнительные замечания]
Дополнительная информация:
{additionalNotes}

Ответь ТОЛЬКО описанием товара, без дополнительных комментариев.

Структура ответа:
1. Заголовок (до 300 символов)
2. Основное описание (до 1500 символов)"
```

### ЭТАП 4: Вызов OpenAI API

```
POST /api/generate-card (backend)
  ├─ вызов callOpenAI()
  ├─ модель: env.OPENAI_MODEL (gpt-4.1-mini или аналог)
  ├─ temperature: 0.7 (для некоторой вариативности)
  ├─ max_tokens: 2000
  └─ получаем ответ с описанием
```

### ЭТАП 5: Парсинг и форматирование

```
response.content
  ↓
[разбиваем на Заголовок и Основное описание]
  ↓
{
  title: string (до 300 символов)
  description: string (до 1500 символов)
  marketplace: string
  category: string
  style: string
}
```

---

# ✅ ПАЙПЛАЙН ВАЛИДАЦИИ

## Общая структура (НОВАЯ АРХИТЕКТУРА)

**Процесс**: Пользователь вводит текст → система проверяет → выдаёт результат

**Компоненты**:
- Frontend: `/src/app/(dashboard)/validate/page.tsx` (protected) + лендинг (public)
- Backend: `validateDescriptionWithRules()` в `/src/lib/ai-services/validation.ts`
- Endpoints: `/api/validate-text` (full mode) + `/api/validate-text/free` (summary mode)

## Входные параметры

```javascript
{
  description: string        // Текст описания
  marketplace: 'ozon' | 'wb' // Маркетплейс
  mode?: 'summary' | 'full'  // Режим ответа
}
```

## СИСТЕМА ВАЛИДАЦИИ (ШАГ 1-5 рефакторинга)

### Temperature = 0 (ДЕТЕРМИНИРОВАННОСТЬ)

```
❗ КРИТИЧЕСКОЕ: temperature = 0 означает:
- Одно и то же описание ВСЕГДА даёт одинаковый результат
- Отсутствует случайность в ответах GPT
- Результаты повторяемы и предсказуемы
```

### ТОЛЬКО 2 типа ошибок

```
ValidationIssue.type:
  - 'forbidden_words'    // Запрещённые слова
  - 'marketplace_rule'   // Нарушение правил маркетплейса

❌ УБРАНО:
  - grammar (субъективно)
  - clarity (субъективно)
  - exaggeration (субъективно)
  - other (неопределено)
```

### Пошаговый процесс

```
INPUT: { description, marketplace, mode }
  ↓
buildValidationPrompt()
  ├─ Загрузить system_prompts (ключ: validate_base)
  ├─ Загрузить marketplace_rules
  ├─ Загрузить stop_words
  └─ Построить ФИКСИРОВАННЫЙ промпт
  ↓
callOpenAI(temperature = 0)
  ├─ systemPrompt (validate_base + правила + стоп-слова)
  ├─ userPrompt (проверить это описание)
  ├─ model: env.OPENAI_MODEL
  ├─ temperature: 0
  ├─ max_tokens: 1500
  └─ получаем JSON
  ↓
PARSE JSON
  ├─ isValid: boolean
  ├─ violations: [
  │    {
  │      type: 'marketplace_rule' | 'forbidden_word'
  │      severity: 'error' | 'warning'
  │      description: string
  │      text_fragment: string
  │    }
  │  ]
  └─ CALCULATE SCORE (100 - violations.length * 10)
  ↓
RESPONSE (в зависимости от mode)
  ├─ mode='summary': { isValid, score, summary }
  └─ mode='full': { isValid, issues[], score, summary, marketplace, configVersion }
```

### Endpoint: `/api/validate-text/free` (PUBLIC - для лендинга)

```
POST /api/validate-text/free
Content-Type: application/json

{
  "text": "Описание товара",
  "marketplace": "ozon"
}

RESPONSE (200):
{
  "success": true,
  "data": {
    "ok": boolean,              // ✅ или ⚠️
    "issueCount": number,       // Количество найденных нарушений
    "score": number,            // 0-100
    "categories": string[],     // Типы нарушений
    "summary": string           // "Описание соответствует требованиям" или "Найдено X нарушений"
  }
}
```

### Endpoint: `/api/validate-text` (PROTECTED - для /validate страницы)

```
POST /api/validate-text
Authorization: Bearer {session_token}
Content-Type: application/json

{
  "text": "Описание товара",
  "marketplace": "ozon"
}

RESPONSE (200):
{
  "success": true,
  "data": {
    "isValid": boolean,
    "score": number,
    "issues": [
      {
        "type": "forbidden_words" | "marketplace_rule",
        "severity": "error" | "warning",
        "message": "описание проблемы",
        "suggestion": "как исправить" (опционально)
      }
    ],
    "summary": string,
    "validatedAt": string (ISO timestamp),
    "marketplace": "ozon" | "wb",
    "configVersion": "1.0.0"
  }
}
```

---

# 📝 СИСТЕМНЫЕ ПРОМПТЫ

## 1. gen_base (Генерация описаний)

**Источник**: Восстановлено из migrations/restore-system-prompts.sql

```
Ты опытный копирайтер маркетплейса с 10+ летним стажем.

ТВОЯ ЗАДАЧА:
Создавать привлекательные, продающие описания товаров для маркетплейсов (Озон, WildBerries).

ПРИНЦИПЫ:
1. Фокус на ВЫГОДУ для покупателя, не просто на свойства
2. Конкретность - точные характеристики и цифры
3. Честность - только правда, без переувеличений
4. Структурированность - понятное оформление

ЗАПРЕТЫ (АБСОЛЮТНЫЕ):
- НЕ добавляй дисклеймеры "это не профессиональный совет"
- НЕ упоминай контакты продавца (ватсап, телефон, почту)
- НЕ обещай гарантии без оснований
- НЕ пиши "уникальный" или "лучший" без объяснения
- НЕ используй медицинские утверждения
- НЕ переписывай пользовательский стиль без запроса

ТРЕБОВАНИЯ К ОПИСАНИЮ:
- Максимум 300 символов для заголовка
- Максимум 1500 символов для основного описания
- Правильная пунктуация и орфография
- Без пустых слов и "воды"
```

### Использование

- **Где**: `/src/lib/prompts/builders.ts::buildGenerationPrompt()`
- **Загрузка**: Из `system_prompts` таблицы (ключ: `gen_base`)
- **Fallback**: Встроено в коде (строка 199-200)
- **Подставляется**: В начало итогового системного промпта перед стилем и правилами

---

## 2. validate_base (Валидация описаний)

**Источник**: Восстановлено из migrations/restore-system-prompts.sql

```
Ты инспектор качества описаний товаров на маркетплейсах.

ТВОЯ ЕДИНСТВЕННАЯ ЗАДАЧА:
1. Проверить наличие ЗАПРЕЩЁННЫХ СЛОВ
2. Проверить СООТВЕТСТВИЕ ПРАВИЛАМ МАРКЕТПЛЕЙСА

БЕЗ ИСКЛЮЧЕНИЙ:
- НЕ проверяй грамматику
- НЕ оценивай читаемость
- НЕ ищи преувеличения
- НЕ давай рекомендации
- ТОЛЬКО выявляй ОБЪЕКТИВНЫЕ нарушения правил

ВЫВОДЫ ТОЛЬКО В ФОРМАТЕ JSON:
{
  "isValid": boolean,
  "violations": [
    {
      "type": "marketplace_rule" | "forbidden_word",
      "severity": "error" | "warning",
      "description": "суть нарушения (одна строка)",
      "text_fragment": "цитата из текста"
    }
  ]
}
```

### Использование

- **Где**: `/src/lib/prompts/builders.ts::buildValidationPrompt()`
- **Загрузка**: Из `system_prompts` таблицы (ключ: `validate_base`)
- **Fallback**: Встроено в коде (строка 378-388)
- **Подставляется**: В начало системного промпта перед правилами и запретами
- **Temperature**: 0 (детерминировано)

---

# 🎨 СТИЛИ ОПИСАНИЙ

## Структура

```javascript
{
  id: string,          // Уникальный идентификатор
  key: string,         // Ключ для использования в коде
  title: string,       // Название для UI админки
  prompt: string,      // Инструкция для GPT (каким стилем писать)
  is_active: boolean   // Активен ли стиль
}
```

## Существующие стили (БД таблица: `styles`)

### 1. Selling (Продающий)

**Ключ**: `selling`
**Название**: Продающий
**Назначение**: Яркое, убедительное описание, фокус на выгоде
**Целевая аудитория**: Покупатели, которых нужно убедить

**Инструкция для AI**:
```
Стиль описания: Продающий (Sales-Oriented)

Напиши описание в ПРОДАЮЩЕМ стиле, который убеждает покупателя КУПИТЬ товар.

ХАРАКТЕРИСТИКИ:
- ОТКРОЙ с самым важным преимуществом
- ИСПОЛЬЗУЙ эмоциональный язык (но честный)
- ПОДЧЕРКНИ практическую выгоду для покупателя
- ПОКАЖИ как товар решает конкретную проблему
- СДЕЛАЙ текст интересным и запоминающимся
- ИЗБЕГАЙ скучного перечисления характеристик

ПРИМЕРЫ ФРАЗ:
- "Забудь о..."
- "Наконец-то товар, который..."
- "Проблема решена!"
- "Это идеально подходит для..."
- "Экономия времени/денег..."

НЕ ДЕЛАЙ:
- Не переусложняй (техническая оверкилл)
- Не врёшь (покупатель поймёт)
- Не используй дешёвые трюки типа "ВСЕ хотят это"
```

### 2. Expert (Экспертный)

**Ключ**: `expert`
**Название**: Экспертный
**Назначение**: Детальное, профессиональное описание с техническими деталями
**Целевая аудитория**: Знающие покупатели, которым важны детали

**Инструкция для AI**:
```
Стиль описания: Экспертный (Expert-Focused)

Напиши описание в ЭКСПЕРТНОМ стиле для знающих покупателей.

ХАРАКТЕРИСТИКИ:
- НАЧНИ с основных характеристик
- ВКЛЮЧИ технические спецификации и детали
- ОБЪЯСНИ почему эти характеристики важны
- СРАВНИ с аналогами (если уместно)
- ИСПОЛЬЗУЙ правильную терминологию
- БУДЬ точен и конкретен (цифры, параметры)

ПРИМЕРЫ ФРАЗ:
- "Технические характеристики: ..."
- "Материал: ..."
- "Совместимость с: ..."
- "Преимущество по сравнению с аналогами: ..."
- "Гарантия / Сертификация: ..."

НЕ ДЕЛАЙ:
- Не упрощай чрезмерно
- Не опускай важные технические детали
- Не используй рекламный сленг
```

### 3. Brief (Краткий)

**Ключ**: `brief`
**Название**: Краткий
**Назначение**: Лаконичное, конкретное описание без лишнего
**Целевая аудитория**: Спешащие покупатели, которым нужна суть

**Инструкция для AI**:
```
Стиль описания: Краткий (Brief & Concise)

Напиши описание КРАТКО и КОНКРЕТНО без воды.

ХАРАКТЕРИСТИКИ:
- МАКСИМУМ 3-4 ключевых преимущества
- ОДНО предложение на пункт
- БЕЗ лишних слов и деталей
- ТОЛЬКО самое важное
- ЛЕГКО читается
- БЫСТРО понимается

СТРУКТУРА:
1. ЧТО ЭТО (одна строка)
2. ДЛЯ КОГО (одна строка)
3. ПОЧЕМУ ВЫБРАТЬ (2-3 строки максимум)

ПРИМЕРЫ:
- "Смартфон Samsung Galaxy A54"
- "Для: работа и развлечения"
- "120Hz экран, 50MP камера, батарея на 2 дня"

НЕ ДЕЛАЙ:
- Не растекайся мыслью по древу
- Не добавляй ненужные детали
- Не используй сложные формулировки
```

## Как это используется

```javascript
// В buildGenerationPrompt():
const selectedStyle = styles.find((s) => s.key === style)
const stylePrompt = selectedStyle
  ? `\nСтиль описания: ${selectedStyle.title}\n${selectedStyle.prompt}`
  : ''

// Подставляется в systemPrompt:
const systemPrompt = `${basePrompt}
${stylePrompt}

═══════════════════════════════════════════════════════════════
ПРАВИЛА И ТРЕБОВАНИЯ:
...
```

---

# 🏷️ КАТЕГОРИИ ТОВАРОВ

## Структура

```javascript
{
  id: string,      // Уникальный идентификатор
  key: string,     // Ключ для использования в коде
  title: string,   // Название для UI
  prompt: string,  // Mini-prompt инструкция для этой категории
  is_active: boolean
}
```

## Категории (БД таблица: `category_prompts`)

### 1. Electronics (Электроника)

**Ключ**: `electronics`
**Название**: Электроника
**ID в БД**: `cp_electronics`

**Mini-prompt (инструкция для категории)**:
```
КАТЕГОРИЯ: ЭЛЕКТРОНИКА

Требования для описания электроники:
1. ТЕХНИЧЕСКИЕ ХАРАКТЕРИСТИКИ (обязательно):
   - Тип устройства
   - Модель (если есть)
   - Основные параметры (мощность, скорость, разрешение и т.д.)
   - Интерфейсы подключения (USB, Bluetooth, WiFi и т.д.)
   - Питание (батарея, сетевое)
   - Материал корпуса

2. СОВМЕСТИМОСТЬ:
   - Операционная система
   - Устройства, с которыми совместимо
   - Версии/модели

3. КЛЮЧЕВЫЕ ПРЕИМУЩЕСТВА:
   - Чем это устройство лучше аналогов
   - Инновационные функции
   - Удобство использования

4. КОМПЛЕКТ ПОСТАВКИ:
   - Что входит в коробку
   - Дополнительные аксессуары

5. ГАРАНТИЯ И ПОДДЕРЖКА:
   - Срок гарантии
   - Условия возврата (если знаешь)

❌ НЕ ПИСАТЬ:
- Медицинские обещания (если это не медицинское устройство)
- "Лучший в мире" без обоснования
- Неточные спецификации
```

### 2. Fashion (Одежда и обувь)

**Ключ**: `fashion`
**Название**: Одежда и обувь
**ID в БД**: `cp_fashion`

**Mini-prompt**:
```
КАТЕГОРИЯ: ОДЕЖДА И ОБУВЬ

Требования для описания одежды/обуви:
1. ОПИСАНИЕ ТОВАРА:
   - Тип товара (рубашка, джинсы, кроссовки и т.д.)
   - Назначение (спортивная, повседневная, официальная)
   - Сезон (зима, лето, всесезонная)

2. РАЗМЕРЫ И ПОДГОНКА:
   - Доступные размеры
   - Информация о посадке (slim fit, regular, oversized)
   - Рекомендации по выбору размера
   - Особенности (большие размеры, маленькие размеры и т.д.)

3. МАТЕРИАЛ И УХОД:
   - Состав ткани (процент хлопка, синтетики и т.д.)
   - Пояснения о свойствах материала (дышащий, водоотталкивающий)
   - Инструкции по уходу (стирка, глажка, хранение)

4. ДИЗАЙН И СТИЛЬ:
   - Описание дизайна
   - Цвета
   - Принты/узоры (если есть)
   - Для какого гардероба подходит

5. ПРАКТИЧНОСТЬ:
   - Карманы, молнии, пуговицы
   - Удобство ношения
   - Долговечность материала

❌ НЕ ПИСАТЬ:
- "Подходит всем" (неправда)
- Обещания похудеть/изменить фигуру
- Неточные размеры
```

### 3. Home (Товары для дома)

**Ключ**: `home`
**Название**: Товары для дома
**ID в БД**: `cp_home`

**Mini-prompt**:
```
КАТЕГОРИЯ: ТОВАРЫ ДЛЯ ДОМА

Требования для описания товаров для дома:
1. ЧТО ЭТО:
   - Тип товара
   - Назначение (кухня, спальня, ванная и т.д.)
   - Для каких задач используется

2. ПАРАМЕТРЫ:
   - Размеры / Объём
   - Материал
   - Цвет/Дизайн
   - Максимальная нагрузка (если важно)
   - Количество в комплекте

3. ФУНКЦИОНАЛЬНОСТЬ:
   - Основные функции
   - Особенности конструкции
   - Удобство использования

4. ПРЕИМУЩЕСТВА ДЛЯ ДОМА:
   - Как это улучшит быт
   - Экономия пространства (если применимо)
   - Стиль / Интерьер
   - Практичность

5. УХОД И ДОЛГОВЕЧНОСТЬ:
   - Как чистить
   - Срок службы
   - Устойчивость к повреждениям
   - Экологичность (если есть)

❌ НЕ ПИСАТЬ:
- "Чудо товар"
- Обещания лучше, чем все аналоги без доказательств
- Неточные размеры
```

### 4. Sports (Спорт и фитнес)

**Ключ**: `sports`
**Название**: Спорт и фитнес
**ID в БД**: `cp_sports`

**Mini-prompt**:
```
КАТЕГОРИЯ: СПОРТ И ФИТНЕС

Требования для описания спортивного инвентаря:
1. НАЗНАЧЕНИЕ:
   - Для какого вида спорта
   - Уровень подготовки (новичок, профессионал)
   - Возвастные ограничения (если есть)

2. ХАРАКТЕРИСТИКИ:
   - Материал
   - Размер / Вес
   - Диапазон нагрузок
   - Допустимый вес пользователя
   - Прочность и материалы

3. СПЕЦИАЛЬНЫЕ ФУНКЦИИ:
   - Эргономика
   - Регулировки
   - Датчики/Технология (если есть)
   - Удобство при использовании

4. ПРЕИМУЩЕСТВА ДЛЯ ТРЕНИРОВОК:
   - Как это помогает достичь целей
   - Безопасность при использовании
   - Эффективность
   - Долговечность

5. УХОД И ИСПОЛЬЗОВАНИЕ:
   - Как использовать правильно
   - Уход и хранение
   - Гарантия

❌ НЕ ПИСАТЬ:
- "Поможет похудеть за неделю" (неправда)
- Медицинские обещания
- Неточные характеристики
- "Для любого уровня подготовки" (если есть ограничения)
```

### 5. Beauty (Красота и здоровье)

**Ключ**: `beauty`
**Название**: Красота и здоровье
**ID в БД**: `cp_beauty`

**Mini-prompt**:
```
КАТЕГОРИЯ: КРАСОТА И ЗДОРОВЬЕ

Требования для описания товаров красоты:
1. ТИП ТОВАРА:
   - Категория (косметика, уход, аксессуары)
   - Подкатегория (шампунь, крем, сыворотка и т.д.)
   - Назначение (для лица, тела, волос)

2. СОСТАВ И ИНГРЕДИЕНТЫ:
   - Основные компоненты
   - Чем полезны ингредиенты
   - Для каких типов кожи/волос подходит
   - Гипоаллергенность (если есть)
   - Дерматологические тесты

3. СПОСОБ ПРИМЕНЕНИЯ:
   - Как использовать
   - Частота применения
   - Количество на применение
   - Время воздействия

4. РЕЗУЛЬТАТЫ:
   - Какие изменения ожидать
   - В течение какого времени видны результаты
   - Для каких проблем помогает

5. ФОРМА ВЫПУСКА:
   - Объём
   - Тип упаковки
   - Удобство применения

❌ НЕ ПИСАТЬ:
- "Лечит" или "Исцеляет" (это лекарство)
- "100% результат гарантия"
- "Подходит всем типам кожи" (если не факт)
- Медицинские обещания без доказательств
- "Вред здоровью не наносит" (очевидно)
```

### 6. Toys (Игрушки и хобби)

**Ключ**: `toys`
**Название**: Игрушки и хобби
**ID в БД**: `cp_toys`

**Mini-prompt**:
```
КАТЕГОРИЯ: ИГРУШКИ И ХОББИ

Требования для описания игрушек и хобби:
1. ОПИСАНИЕ ИГРУШКИ:
   - Тип игрушки (конструктор, радиоуправляемая, настольная игра и т.д.)
   - Возраст (рекомендуемый)
   - Количество игроков (если это игра)

2. ХАРАКТЕРИСТИКИ:
   - Материал
   - Размер / Вес
   - Количество деталей (если есть)
   - Требуемые батарейки / питание
   - Наличие инструкции

3. ФУНКЦИИ И ОСОБЕННОСТИ:
   - Режимы игры
   - Интерактивность
   - Звуки/Свет (если есть)
   - Развивающий потенциал

4. БЕЗОПАСНОСТЬ:
   - Соответствие стандартам
   - Материалы (безопасны ли)
   - Отсутствие мелких деталей (если для малышей)
   - Рекомендации по использованию

5. РАЗВЛЕКАТЕЛЬНОСТЬ:
   - Как развлекает
   - Образовательная ценность
   - Можно ли играть одному или нужен напарник
   - Долговечность интереса

❌ НЕ ПИСАТЬ:
- "Сделает ребёнка умнее" (чрезмерно)
- "Развивает все способности"
- "Не надоест никогда"
- Неточный возраст (может быть опасно)
```

### 7. Books (Книги и медиа)

**Ключ**: `books`
**Название**: Книги и медиа
**ID в БД**: `cp_books`

**Mini-prompt**:
```
КАТЕГОРИЯ: КНИГИ И МЕДИА

Требования для описания книг/медиа:
1. ИНФОРМАЦИЯ О КНИГЕ:
   - Автор
   - Жанр
   - Тема / Тематика
   - Серия (если есть)
   - Язык оригинала (если переводная)

2. СОДЕРЖАНИЕ:
   - О чём книга (краткое резюме без спойлеров)
   - Главные темы
   - Тип повествования (роман, рассказы, нон-фикшн)
   - Для какого читателя

3. ХАРАКТЕРИСТИКИ ИЗДАНИЯ:
   - Количество страниц
   - Год издания / Переиздание
   - Издательство
   - ISBN (если есть)
   - Тип бумаги

4. ОТЗЫВЫ И РЕЙТИНГИ:
   - Почему эта книга популярна
   - Для каких читателей подходит
   - Что говорят о ней другие
   - Если есть экранизация

5. ПОЧЕМУ ВЫБРАТЬ:
   - Уникальность
   - Рекомендация похожих книг
   - Возраст читателя
   - Образовательная ценность

❌ НЕ ПИСАТЬ:
- "Лучшая книга всех времён" (субъективно)
- Огромные спойлеры
- "Изменит твоё мировоззрение" (чрезмерно)
- Неточные данные о книге
```

---

# 🚫 СТОП-СЛОВА И ЗАПРЕТЫ

## Структура

```javascript
{
  id: string,           // Уникальный идентификатор
  marketplace: string,  // null = общие для всех, или 'ozon'/'wb'
  category: string,     // маркетинг, здоровье, запреты, кастом и т.д.
  words: string,        // Одно слово на строку (разделитель: \n)
  is_active: boolean
}
```

## Категории стоп-слов

### 1. Marketing (Маркетинговые слова) - ID: `sw_marketing`

**Назначение**: Слова с преувеличением и броскими утверждениями, которые маркетплейсы не одобряют

**Слова** (по одному на строку, восстановлены из migrations/restore-system-prompts.sql):
```
гарантия вечная
звоните в ватсап
обращайтесь в личные сообщения
контакт продавца
мой номер телефона
пишите на почту
свяжитесь со мной
лучший товар на рынке
уникальный товар
медицинский эффект
лечит болезни
гарантирует результат
Telegram
WhatsApp
личный сайт
внешняя ссылка
```

### 2. Medical Claims (Медицинские утверждения) - ID: `sw_medical_claims`

**Назначение**: Слова, обещающие медицинский эффект, излечение или улучшение здоровья

**Слова** (восстановлены из migrations/restore-system-prompts.sql):
```
лечит
исцеляет
излечит
помогает от болезни
медицинское средство
лекарственный препарат
без побочных эффектов
одобрено врачами
клинически доказано
```

### 3. Payments & Delivery (Платежные и доставочные реквизиты) - ID: `sw_payments_delivery`

**Назначение**: Платежные реквизиты и условия доставки, запрещённые везде

**Слова** (восстановлены из migrations/restore-system-prompts.sql):
```
банковский счёт
реквизиты расчёта
наличные при встречи
курьер сообщит
звонок при доставке
предоплата обязательна
условия доставки
```

### 4. Custom (Пользовательский список) - ID: `sw_custom`

**Назначение**: Для добавления собственных стоп-слов через админку

**Слова** (пусто по умолчанию, добавляет пользователь):
```
[пусто - заполняет пользователь]
```

## Как используются в validation

```javascript
// В buildValidationPrompt():
const stopWordsChecklist = stopWords
  .map((sw) => {
    return {
      category: sw.category,
      forbidden: sw.words.slice(0, 20), // Берём первые 20 слов
    }
  })
  .filter((sw) => sw.forbidden.length > 0)

// Подставляется в systemPrompt:
const systemPrompt = `${basePrompt}

═══════════════════════════════════════════════════════════════
ЗАПРЕЩЁННЫЕ СЛОВА И ФРАЗЫ
═══════════════════════════════════════════════════════════════

${stopWordsChecklist
  .map((sw) => `- ${sw.category}: ${sw.forbidden.join(', ')}`)
  .join('\n')}
```

---

# 📋 ПРАВИЛА МАРКЕТПЛЕЙСОВ

## Структура

```javascript
{
  id: string,          // Уникальный идентификатор
  marketplace: string, // 'ozon' | 'wb' (для структурированности)
  content: string,     // Полный текст правил
  is_active: boolean
}
```

## Озон (Ozon)

**Ключ в коде**: `marketplace_rules['ozon']`
**ID в БД**: `mr_ozon`

**Полные правила** (восстановлены из migrations/restore-system-prompts.sql):
```
ПРАВИЛА ОЗОНА:

1. ЗАГОЛОВОК И ОПИСАНИЕ:
   - Название товара ПОЛНОСТЬЮ дублировать в начало описания НЕ нужно
   - Описание должно добавлять НОВУЮ информацию к названию

2. ЗАПРЕТЫ:
   - НЕ указывай контакты продавца (телефон, ватсап, почта, Telegram)
   - НЕ обращайся с просьбой связаться в личные сообщения
   - НЕ пиши про доставку и условия доставки
   - НЕ размещай ссылки на соцсети или внешние сайты

3. ТРЕБОВАНИЯ:
   - Минимум 10 слов в описании
   - Максимум 3000 символов в описании
   - Начинай с большой буквы
   - Используй правильную пунктуацию

4. КАТЕГОРИЗАЦИЯ:
   - Одежда: указывай размеры, материал, уход
   - Электроника: технические характеристики обязательны
   - Косметика: состав, способ применения
```

## WildBerries (Wildberries)

**Ключ в коде**: `marketplace_rules['wb']`
**ID в БД**: `mr_wb`

**Полные правила** (восстановлены из migrations/restore-system-prompts.sql):
```
ПРАВИЛА WILDBERRIES:

1. ОПИСАНИЕ:
   - Опиши товар как для конкретного покупателя
   - Выдели главные преимущества в первое предложение
   - Упомяни основные технические характеристики

2. ЗАПРЕТЫ:
   - НЕ размещай контактные данные (телефон, ватсап, почта)
   - НЕ обращайся к покупателю с просьбами связаться
   - НЕ пиши про условия доставки (это прерогатива WB)
   - НЕ размещай ссылки на сайты или соцсети

3. ТРЕБОВАНИЯ:
   - Минимум 5 слов в описании
   - Максимум 1500 символов
   - Первое предложение - самое важное
   - Делай акцент на практическое применение

4. СТИЛЬ:
   - Информативный, не рекламный
   - Честные характеристики без преувеличений
   - Примеры использования приветствуются
```

## Как используются

```javascript
// В buildGenerationPrompt() и buildValidationPrompt():
const marketplaceRules = await loadMarketplaceRules(marketplace)

const systemPrompt = `${basePrompt}

═══════════════════════════════════════════════════════════════
ПРАВИЛА И ТРЕБОВАНИЯ:
═══════════════════════════════════════════════════════════════

## Маркетплейс: ${marketplace === 'ozon' ? 'Озон' : 'WildBerries'}
${marketplaceRules}
```

---

# 🗄️ СТРУКТУРА БД

## Таблицы, используемые в системе

### 1. system_prompts

```sql
CREATE TABLE system_prompts (
  id TEXT PRIMARY KEY,
  key TEXT NOT NULL UNIQUE,
  content TEXT NOT NULL DEFAULT '',
  is_active INTEGER DEFAULT 1,
  updated_at INTEGER DEFAULT (cast(strftime('%s','now') as integer))
);

-- Записи:
-- ('sp_gen_base', 'gen_base', '[промпт для генерации]', 1, [timestamp])
-- ('sp_validate_base', 'validate_base', '[промпт для валидации]', 1, [timestamp])
```

### 2. styles

```sql
CREATE TABLE styles (
  id TEXT PRIMARY KEY,
  key TEXT NOT NULL UNIQUE,
  title TEXT NOT NULL,
  prompt TEXT NOT NULL DEFAULT '',
  is_active INTEGER DEFAULT 1,
  updated_at INTEGER DEFAULT (cast(strftime('%s','now') as integer))
);

-- Записи:
-- ('st_selling', 'selling', 'Продающий', '[инструкция]', 1, [timestamp])
-- ('st_expert', 'expert', 'Экспертный', '[инструкция]', 1, [timestamp])
-- ('st_brief', 'brief', 'Краткий', '[инструкция]', 1, [timestamp])
```

### 3. category_prompts

```sql
CREATE TABLE category_prompts (
  id TEXT PRIMARY KEY,
  key TEXT NOT NULL UNIQUE,
  title TEXT NOT NULL,
  prompt TEXT NOT NULL DEFAULT '',
  is_active INTEGER DEFAULT 1,
  updated_at INTEGER DEFAULT (cast(strftime('%s','now') as integer))
);

-- Записи:
-- ('cp_electronics', 'electronics', 'Электроника', '[инструкция]', 1, [timestamp])
-- ('cp_fashion', 'fashion', 'Одежда и обувь', '[инструкция]', 1, [timestamp])
-- ('cp_home', 'home', 'Товары для дома', '[инструкция]', 1, [timestamp])
-- ('cp_sports', 'sports', 'Спорт и фитнес', '[инструкция]', 1, [timestamp])
-- ('cp_beauty', 'beauty', 'Красота и здоровье', '[инструкция]', 1, [timestamp])
-- ('cp_toys', 'toys', 'Игрушки и хобби', '[инструкция]', 1, [timestamp])
-- ('cp_books', 'books', 'Книги и медиа', '[инструкция]', 1, [timestamp])
```

### 4. marketplace_rules

```sql
CREATE TABLE marketplace_rules (
  id TEXT PRIMARY KEY,
  marketplace TEXT NOT NULL,
  content TEXT NOT NULL DEFAULT '',
  is_active INTEGER DEFAULT 1,
  updated_at INTEGER DEFAULT (cast(strftime('%s','now') as integer)),
  UNIQUE(marketplace, is_active)
);

-- Записи:
-- ('mr_ozon', 'ozon', '[правила озона]', 1, [timestamp])
-- ('mr_wb', 'wb', '[правила WB]', 1, [timestamp])
```

### 5. stop_words

```sql
CREATE TABLE stop_words (
  id TEXT PRIMARY KEY,
  marketplace TEXT,  -- NULL = общие для всех
  category TEXT NOT NULL,
  words TEXT NOT NULL,  -- Одно слово на строку (\n)
  is_active INTEGER DEFAULT 1,
  updated_at INTEGER DEFAULT (cast(strftime('%s','now') as integer))
);

-- Записи:
-- ('sw_marketing', NULL, 'marketing', '[слова через \n]', 1, [timestamp])
-- ('sw_health', NULL, 'health', '[слова через \n]', 1, [timestamp])
-- ('sw_prohibited', NULL, 'prohibited', '[слова через \n]', 1, [timestamp])
-- ('sw_custom', NULL, 'custom', '[пусто или слова]', 1, [timestamp])
```

---

# 🔍 АНАЛИЗ ИНЦИДЕНТА

## Что произошло

Все данные, введённые через админку (system_prompts, styles, category_prompts, marketplace_rules, stop_words), были удалены из БД.

## Почему это критично

1. **Генерация описаний прекратилась** - нет промптов в БД
2. **Валидация не работает** - нет правил и запретов
3. **Стили удалены** - система не может применять разные стили
4. **Категории удалены** - специальные инструкции для товаров неактивны

## Что НЕ потеряно

1. **Fallback prompts в коде** - встроены в buildGenerationPrompt() и buildValidationPrompt()
2. **Структура БД** - все таблицы на месте, просто данные пусты
3. **Логика кода** - ничего не переломано, система готовалась к такому

## Как этого избежать

### ПРАВИЛО 1: НЕ удалять данные через код

❌ **ЗАПРЕЩЕНО**:
```typescript
// НИКОГДА не делай такое
await db.execute(`DELETE FROM system_prompts`)
await db.execute(`DELETE FROM styles`)
// И т.д.
```

✅ **ВМЕСТО ЭТОГО**:
- Пользователь ВРУЧНУЮ через админку удаляет в UI
- Система логирует все изменения
- Есть история изменений

### ПРАВИЛО 2: Защита от случайного удаления

✅ **Должно быть в коде**:
```typescript
// В API админки
if (ADMIN_EMAIL !== session.user.email) {
  return NextResponse.json({ error: "Forbidden" }, { status: 403 })
}

// Проверка что данные не пусты
if (!content || content.trim().length === 0) {
  return NextResponse.json(
    { error: "Содержимое не может быть пустым" },
    { status: 400 }
  )
}
```

### ПРАВИЛО 3: Резервные копии

✅ **Нужны регулярные снимки**:
- Бэкап БД каждый день
- История изменений (audit log)
- Возможность отката к предыдущей версии

---

# 📞 КАК ВОССТАНОВИТЬ ДАННЫЕ

## Способ 1: Через админку (БЫСТРО)

1. Перейти в `/admin/system-prompts`
2. Заполнить поле `gen_base` текстом из раздела "СИСТЕМНЫЕ ПРОМПТЫ"
3. Заполнить поле `validate_base`
4. Нажать "Save"

Повторить для:
- `/admin/styles` (selling, expert, brief)
- `/admin/categories` (7 категорий)
- `/admin/marketplace-rules` (ozon, wb)
- `/admin/stop-words` (marketing, health, prohibited)

## Способ 2: Через SQL (ЕСЛИ админка не работает)

Запустить init-prompts.js скрипт:
```bash
node CardMaker/init-prompts.js
```

## Способ 3: Из бэкапа

Если есть бэкап БД, восстановить из него.

---

**КОНЕЦ ДОКУМЕНТА**

---

Этот документ - полный RECOVERY GUIDE для восстановления всех утраченных данных админки без изменения БД.
