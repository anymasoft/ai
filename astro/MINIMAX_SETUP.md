# 🚀 Интеграция MiniMax - Руководство по настройке

## Что было сделано

Проект Astrolus теперь использует **реальную генерацию видео через MiniMax API** вместо демо.mp4.

### Основные изменения:

#### 1. **Backend (Node.js)**
- ✅ `/src/lib/minimax/` - функции для работы с MiniMax API
  - `imageToBase64DataUrl.ts` - конвертация изображения
  - `callMinimaxAPI.ts` - отправка задачи на генерацию
  - `downloadVideoFromMinimax.ts` - скачивание готового видео

- ✅ `/src/pages/api/upload-image.ts` - загрузка изображений (jpg only)
- ✅ `/src/pages/api/generate.ts` - обновлен для вызова MiniMax
- ✅ `/src/pages/api/minimax_callback.ts` - webhook для обработки результатов

#### 2. **Frontend (Astro)**
- ✅ `/src/pages/app.astro` - обновлен для работы с реальными видео
  - Функция `uploadImageToServer()` - загрузка на сервер
  - Использование `videoUrl` из API вместо demo.mp4
  - Увеличен таймаут polling с 30 сек до 300 сек (5 минут)

#### 3. **База данных**
- ✅ Новые колонки в таблице `generations`:
  - `minimax_job_id` - ID задачи в MiniMax
  - `video_url` - URL сохраненного видео
  - `minimax_status` - статус обработки

#### 4. **Хранилище файлов**
- ✅ `/uploads/image.jpg` - текущее изображение (перезапись)
- ✅ `/videos/output.mp4` - текущее видео (перезапись)

---

## Быстрый старт

### Шаг 1: Получить API ключ MiniMax

1. Перейди на https://www.minimaxi.com/
2. Зарегистрируйся и получи API ключ
3. Скопируй ключ вида `sk-api-xxxxx`

### Шаг 2: Настроить переменные окружения

Создай файл `.env.local` в корне проекта:

```bash
# Копируем пример
cp .env.example .env.local
```

Отредактируй `.env.local`:

```env
# MiniMax API
MINIMAX_API_KEY=sk-api-xxxxx   # Твой ключ от MiniMax

# Callback URL (ВАЖНО!)
MINIMAX_CALLBACK_URL=https://yourdomain.com/api/minimax_callback
```

### Шаг 3: Настроить callback URL

#### Для локальной разработки (использовать ngrok):

```bash
# 1. Установить ngrok
brew install ngrok
# или скачать с https://ngrok.com/

# 2. Запустить ngrok (на том же порту что Astro, обычно 3000)
ngrok http 3000

# 3. Скопировать URL (вида https://12345abc.ngrok-free.app)
# и добавить в .env.local:
MINIMAX_CALLBACK_URL=https://12345abc.ngrok-free.app/api/minimax_callback
```

#### Для production (на своем домене):

```env
MINIMAX_CALLBACK_URL=https://yourdomain.com/api/minimax_callback
```

### Шаг 4: Запустить проект

```bash
npm install  # Если первый раз
npm run dev
```

Проверь логи - должны быть сообщения типа:
```
✅ Database initialized
✅ MiniMax integration ready
```

---

## Как это работает

### Флоу генерации видео:

```
1. ПОЛЬЗОВАТЕЛЬ
   └─ Выбирает изображение (jpg)
   └─ Пишет описание
   └─ Нажимает "Generate"

2. ФРОНТЕНД
   └─ POST /api/upload-image
      └─ Сохраняет /uploads/image.jpg на сервере

3. POST /api/generate
   ├─ Проверка баланса
   ├─ Создание записи в БД
   └─ Запуск фоновой задачи

4. ФОНОВАЯ ОБРАБОТКА
   ├─ Чтение /uploads/image.jpg
   ├─ Конвертация в base64
   └─ POST https://api.minimax.io/v1/video_generation
      └─ MiniMax возвращает task_id

5. МИНИMAX ОБРАБАТЫВАЕТ
   └─ Генерирует видео (1-5 минут)
   └─ Отправляет callback на /api/minimax_callback?generationId=...

6. CALLBACK ENDPOINT
   ├─ Получает file_id от MiniMax
   ├─ Скачивает видео с MiniMax CDN
   ├─ Сохраняет в /videos/output.mp4
   ├─ Обновляет БД (video_url, status=success)
   └─ chargeGeneration() - списывает кредиты

7. ФРОНТЕНД
   ├─ Polling GET /api/generate?generationId=...
   ├─ Получает videoUrl: /videos/output.mp4
   └─ Показывает видео в плеере

8. ПОЛЬЗОВАТЕЛЬ
   └─ Видит готовое видео ✅
   └─ Может скачать или поделиться
```

---

## Переменные окружения

| Переменная | Обязательна | Описание |
|-----------|-----------|---------|
| `MINIMAX_API_KEY` | ✅ Да | API ключ от MiniMax |
| `MINIMAX_CALLBACK_URL` | ✅ Да | URL callback'а (для MiniMax) |
| `DATABASE_URL` | ❌ Нет | Путь к SQLite БД (по умолчанию vr_ai.db) |

Все остальные переменные (Google OAuth, YooKassa и т.д.) - для других функций, не обязательны для MiniMax.

---

## API Endpoints

### POST /api/upload-image
**Загружает изображение товара**

```bash
curl -F "image=@photo.jpg" http://localhost:3000/api/upload-image
```

**Параметры:**
- `image` - файл (multipart/form-data)
- Только JPEG (image/jpeg)
- Макс 50 MB

**Ответ:**
```json
{
  "success": true,
  "imagePath": "/uploads/image.jpg",
  "size": 102400
}
```

### POST /api/generate
**Запускает генерацию видео**

```bash
curl -X POST http://localhost:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "товар движется слева направо",
    "duration": 6
  }'
```

**Параметры:**
- `prompt` - описание движения (обязателен)
- `duration` - 6 или 10 секунд (обязателен)

**Ответ:**
```json
{
  "success": true,
  "generationId": "gen_1234567890_userID",
  "cost": 1,
  "balanceBefore": 5,
  "balanceAfter": 4
}
```

### GET /api/generate?generationId=...
**Проверка статуса генерации**

```bash
curl http://localhost:3000/api/generate?generationId=gen_1234567890_userID
```

**Ответ (при успехе):**
```json
{
  "success": true,
  "generationId": "gen_1234567890_userID",
  "status": "success",
  "videoUrl": "/videos/output.mp4",
  "charged": true
}
```

### POST /api/minimax_callback?generationId=...
**Webhook от MiniMax (автоматический)**

MiniMax отправляет сюда результат генерации. Обрабатывается автоматически.

---

## Отладка

### Проверка логов

```bash
# Смотри консоль при npm run dev

# Ищи сообщения:
[UPLOAD] ✅ Image uploaded: /uploads/image.jpg
[GEN] Отправляем в MiniMax: generation=gen_...
[MINIMAX] ✅ Task created: task_xyz
[CALLBACK] ✅ MiniMax success: generationId=gen_...
[DOWNLOAD] ✅ Видео сохранено: /videos/output.mp4
[CHARGE] ✅ Successfully charged credits
```

### Частые проблемы

#### 1. "MINIMAX_API_KEY не установлен"
```
Решение: Проверь .env.local - добавь MINIMAX_API_KEY
```

#### 2. "Callback от MiniMax не приходит"
```
Решение:
- Убедись что MINIMAX_CALLBACK_URL правильный
- Используй ngrok для локальной разработки
- MiniMax должен иметь доступ к твоему URL
```

#### 3. "Видео не сохраняется"
```
Решение:
- Проверь права доступа на папку /videos
- Убедись что место на диске не закончилось
- Посмотри ошибку в логах [DOWNLOAD]
```

---

## Структура проекта (новое)

```
/home/user/ai/astro/
├── src/
│   ├── lib/
│   │   ├── minimax/
│   │   │   ├── imageToBase64.ts
│   │   │   ├── callMinimaxAPI.ts
│   │   │   └── downloadVideoFromMinimax.ts
│   │   ├── billing/
│   │   │   └── chargeGeneration.ts (обновлен)
│   │   └── db.ts (обновлен - миграция)
│   ├── pages/
│   │   ├── api/
│   │   │   ├── upload-image.ts (новый)
│   │   │   ├── generate.ts (обновлен)
│   │   │   └── minimax_callback.ts (новый)
│   │   └── app.astro (обновлен)
├── uploads/ (папка)
│   └── image.jpg (перезапись)
├── videos/ (папка)
│   └── output.mp4 (перезапись)
├── .env.local (создай сам)
├── .env.example (шаблон)
└── MINIMAX_INTEGRATION.md (документация протокола)
```

---

## Тестирование

### Тест 1: Upload изображения

```bash
# Загрузи изображение
curl -F "image=@test.jpg" http://localhost:3000/api/upload-image

# Проверь /uploads/image.jpg создался
ls -la uploads/image.jpg
```

### Тест 2: POST /api/generate

```bash
curl -X POST http://localhost:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "товар вращается",
    "duration": 6
  }'
```

Должен вернуть generationId.

### Тест 3: Проверка статуса

```bash
# Замени generationId на реальный
curl http://localhost:3000/api/generate?generationId=gen_1234567890_userID
```

Статус будет:
- `processing` - еще обрабатывается в MiniMax
- `success` - готово, видео скачано
- `failed` - ошибка при генерации

---

## Безопасность

⚠️ **ВАЖНО:**

1. **Никогда не коммитй `.env.local`**
   ```bash
   # .gitignore должен содержать:
   .env.local
   .env.*.local
   ```

2. **Защищай API ключ**
   - Используй разные ключи для разных окружений (dev, staging, prod)
   - Периодически ротируй ключи

3. **Используй HTTPS в production**
   - MiniMax callback'ы должны идти по HTTPS
   - Используй сертификаты Let's Encrypt

4. **Rate limiting**
   - Добавь rate limiting на /api/upload-image если нужно
   - Защити от spam генераций

---

## Что дальше

- Добавить отслеживание истории генераций (опционально)
- Добавить retry логику при ошибках MiniMax
- Оптимизировать хранение видео (S3, CDN и т.д.)
- Добавить качество обработки (разные разрешения)

---

**Готово! 🎉 Твой проект теперь использует реальную MiniMax генерацию видео.**
