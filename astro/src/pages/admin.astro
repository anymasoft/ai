---
import { getUserFromSession, isAdmin } from '../lib/auth';
import AppLayout from '../layouts/AppLayout.astro';

// Эта страница должна быть динамичной для работы с cookies
export const prerender = false;

// Получаем текущего пользователя из сессии
const sessionToken = Astro.cookies.get('session_token')?.value;
const user = sessionToken ? getUserFromSession(sessionToken) : null;

// Если пользователь не авторизован, редиректим на sign-in
if (!user) {
  return Astro.redirect('/sign-in');
}

// Проверяем, является ли пользователь админом
if (!isAdmin(user.email)) {
  return Astro.redirect('/app');
}
---

<AppLayout title="Admin Settings - Video Reader AI">
  <main class="flex-1 overflow-auto">
    <div class="max-w-4xl mx-auto p-4 md:p-6">
      <!-- Back Button -->
      <a href="/app" class="inline-flex items-center justify-center p-2 rounded-full hover:bg-gray-100 dark:hover:bg-gray-800 transition cursor-pointer mb-4">
        <svg class="w-6 h-6 text-gray-700 dark:text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
      </a>

      <!-- Header -->
      <div class="mb-4">
        <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-1">Admin Settings</h1>
        <p class="text-sm text-gray-600 dark:text-gray-400">Manage system and user data</p>
      </div>

      <!-- Admin Info Card -->
      <div class="bg-white dark:bg-gray-900 rounded-2xl border border-gray-200 dark:border-gray-800 p-4 mb-4">
        <div class="flex items-start gap-3 mb-4">
          <div class="w-12 h-12 rounded-full bg-gradient-to-br from-purple-500 to-blue-600 flex items-center justify-center text-white font-bold text-sm">
            A
          </div>
          <div class="flex-1">
            <h2 class="text-lg font-bold text-gray-900 dark:text-white mb-0.5">Administrator</h2>
            <p class="text-xs text-gray-600 dark:text-gray-400">{user.email}</p>
          </div>
        </div>

        <!-- Admin Features -->
        <div class="space-y-2">
          <div class="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800">
            <h3 class="font-semibold text-blue-900 dark:text-blue-300 text-sm mb-1">👥 User Management</h3>
            <p class="text-xs text-blue-800 dark:text-blue-400">View and manage all users in the system</p>
          </div>

          <div class="p-3 rounded-lg bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800">
            <h3 class="font-semibold text-green-900 dark:text-green-300 text-sm mb-1">💰 Credits Management</h3>
            <p class="text-xs text-green-800 dark:text-green-400">Manage user credits and subscription plans</p>
          </div>

          <div class="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/20 border border-purple-200 dark:border-purple-800">
            <h3 class="font-semibold text-purple-900 dark:text-purple-300 text-sm mb-1">⚙️ System Settings</h3>
            <p class="text-xs text-purple-800 dark:text-purple-400">Configure global application settings</p>
          </div>
        </div>
      </div>

      <!-- Coming Soon Notice -->
      <div class="bg-yellow-50 dark:bg-yellow-900/20 rounded-2xl border border-yellow-200 dark:border-yellow-800 p-4">
        <h3 class="text-sm font-semibold text-yellow-900 dark:text-yellow-300 mb-1">🚀 Coming Soon</h3>
        <p class="text-xs text-yellow-800 dark:text-yellow-400">Admin panel features are being developed. More management tools will be available soon.</p>
      </div>
    </div>
  </main>
</AppLayout>
