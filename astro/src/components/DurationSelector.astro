---
// DurationSelector.astro - Duration Selector
// Переключатель длительности видео: 6s vs 10s
// Первая кнопка выбрана по умолчанию
---

<div class="flex items-center justify-center gap-2 w-full">
  <!-- 6s Button -->
  <button
    id="duration-6"
    data-duration="6"
    class="duration-toggle-btn duration-toggle-active flex-1 flex items-center justify-center gap-2
           px-4 py-2 rounded-lg
           font-medium text-sm
           transition-all duration-200"
  >
    <span>6s (1 кредит)</span>
  </button>

  <!-- 10s Button -->
  <button
    id="duration-10"
    data-duration="10"
    class="duration-toggle-btn flex-1 flex items-center justify-center gap-2
           px-4 py-2 rounded-lg
           font-medium text-sm
           transition-all duration-200"
  >
    <span>10s (2 кредита)</span>
  </button>
</div>

<style>
  /* NO-VIDEO MODE: Contrast buttons */
  .no-video .duration-toggle-btn {
    border: 1px solid #d1d5db !important;
    background-color: #ffffff !important;
    color: #374151 !important;
    backdrop-filter: none !important;
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05) !important;
  }

  .dark .no-video .duration-toggle-btn {
    border-color: #4b5563 !important;
    background-color: #374151 !important;
    color: #e5e7eb !important;
  }

  .no-video .duration-toggle-btn:hover:not(.duration-toggle-active) {
    background-color: #f3f4f6 !important;
    border-color: #9ca3af !important;
  }

  .dark .no-video .duration-toggle-btn:hover:not(.duration-toggle-active) {
    background-color: #4b5563 !important;
    border-color: #6b7280 !important;
  }

  .no-video .duration-toggle-active {
    background: #dbeafe !important;
    border-color: #3b82f6 !important;
    color: #1e40af !important;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1) !important;
  }

  .dark .no-video .duration-toggle-active {
    background: #1e3a8a !important;
    border-color: #3b82f6 !important;
    color: #93c5fd !important;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2) !important;
  }

  /* HAS-VIDEO MODE: Glass pill buttons */
  .has-video .duration-toggle-btn {
    border: 1px solid rgba(255, 255, 255, 0.3);
    background-color: rgba(255, 255, 255, 0.1);
    background-image: none;
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    color: #475569;
    cursor: pointer;
  }

  .dark .has-video .duration-toggle-btn {
    border-color: rgba(255, 255, 255, 0.2);
    background-color: rgba(255, 255, 255, 0.05);
    color: #cbd5e1;
  }

  .has-video .duration-toggle-btn:hover:not(.duration-toggle-active) {
    background-color: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.4);
  }

  .dark .has-video .duration-toggle-btn:hover:not(.duration-toggle-active) {
    background-color: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .has-video .duration-toggle-active {
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.4);
    color: #1e40af;
    box-shadow: 0 0 12px rgba(59, 130, 246, 0.3), inset 0 0 8px rgba(59, 130, 246, 0.1);
  }

  .dark .has-video .duration-toggle-active {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
    color: #60a5fa;
    box-shadow: 0 0 12px rgba(59, 130, 246, 0.3), inset 0 0 8px rgba(59, 130, 246, 0.1);
  }

  /* Base button styles */
  .duration-toggle-btn {
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .duration-toggle-btn:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
  }
</style>

<script>
  // Duration toggle logic
  const durationButtons = document.querySelectorAll('.duration-toggle-btn');

  durationButtons.forEach(button => {
    button.addEventListener('click', () => {
      // Remove active class from all buttons
      durationButtons.forEach(btn => btn.classList.remove('duration-toggle-active'));

      // Add active class to clicked button
      button.classList.add('duration-toggle-active');

      const duration = button.getAttribute('data-duration');

      // Dispatch custom event for parent component
      window.dispatchEvent(new CustomEvent('durationChanged', {
        detail: { duration }
      }));

      console.log(`[DURATION] Switched to: ${duration}s`);
    });
  });
</script>
