# 🚀 AI Site Builder - Быстрый старт (Windows 10)

## ⚡ Требования

- Node.js 18+ (https://nodejs.org)
- Git
- OpenAI API ключ (https://platform.openai.com/api-keys)

## 📋 Шаг 1: Клонирование и подготовка

```bash
git clone https://github.com/anymasoft/ai.git
cd ai
cd ai-site-builder
```

## 🔧 Шаг 2: Установка зависимостей

### Сервер (Backend)

```bash
cd server

# Очистите старые node_modules если есть
rmdir /s /q node_modules
del package-lock.json

# Установите зависимости
npm install
```

### Клиент (Frontend)

В отдельной PowerShell вкладке:

```bash
cd client

rmdir /s /q node_modules
del package-lock.json

npm install
```

## 🔑 Шаг 3: Конфигурация

### В папке `server/`

Создайте файл `.env`:

```
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxx
NODE_ENV=development
```

Замените `sk-proj-xxxxxxxxxxxxxx` на ваш реальный OpenAI API ключ.

### В папке `client/`

Создайте файл `.env`:

```
VITE_API_URL=http://localhost:3000/api
VITE_APP_URL=http://localhost:5173
```

## 🚀 Шаг 4: Запуск приложения

### Запустите Сервер

В PowerShell в папке `server/`:

```bash
npm run server
```

Вы должны увидеть:
```
✅ Server is running at http://localhost:3000
```

### Запустите Клиент

В отдельной PowerShell в папке `client/`:

```bash
npm run dev
```

Откроется браузер на **http://localhost:5173**

## ✨ Готово!

Теперь вы можете:

1. **Создать проект**: Нажмите "Create Project"
2. **Ввести промпт**: Например: "Create a beautiful landing page for a tech startup"
3. **Дождитесь генерации**: AI будет генерировать сайт
4. **Предпросмотр**: Посмотрите результат
5. **Редактирование**: Попросите AI внести изменения

## 🛠️ Что изменилось?

- ✅ **Prisma** → **SQLite** (база данных)
- ✅ **Better Auth** → Простая авторизация через заголовки
- ✅ **Stripe** → Удалена (платежи)
- ✅ **OpenRouter** → **OpenAI GPT-4o-mini**

## 📊 Как это работает?

1. **База данных**: SQLite (файл `server/data/app.db`)
   - Автоматически создается при первом запуске
   - Хранит проекты, пользователей, версии кода, историю сообщений

2. **Авторизация**: Просто укажите `x-user-id` в заголовке запроса
   - Разработка: используется `default-user`
   - UI автоматически отправляет этот заголовок

3. **Кредиты**:
   - Новый пользователь получает 20 кредитов
   - Создание проекта: 5 кредитов
   - Редактирование: 5 кредитов

4. **Генерация сайтов**:
   - Промпт → OpenAI GPT-4o-mini
   - HTML с Tailwind CSS
   - Готовый к использованию

## ⚠️ Если что-то не работает

### Сервер не стартует

```bash
# Проверьте Node версию
node --version

# Очистите npm кэш
npm cache clean --force

# Переустановите
rmdir /s /q node_modules
npm install
```

### OPENAI_API_KEY не найден

Убедитесь что файл `.env` в папке `server/` содержит:
```
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxx
```

### Клиент не подключается к серверу

1. Проверьте что сервер запущен: http://localhost:3000
2. Посмотрите консоль браузера (F12) на ошибки
3. Проверьте что CORS включен (должен быть по умолчанию)

### Ошибка "Database is locked"

Закройте все экземпляры сервера и попробуйте снова:
```bash
npm run server
```

## 📁 Структура проекта

```
ai-site-builder/
├── server/              # Node.js + Express + SQLite
│   ├── data/
│   │   └── app.db      # SQLite база (автоматически создается)
│   ├── lib/db.ts       # Инициализация БД
│   ├── controller/      # Бизнес-логика
│   ├── routes/          # API маршруты
│   ├── config/          # OpenAI конфигурация
│   └── .env             # Ключи и переменные
│
├── client/              # React + Vite + Tailwind
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы
│   │   └── App.tsx      # Главный компонент
│   └── .env             # API URLs
│
└── README.md
```

## 🎯 API эндпоинты

- `POST /api/user/project` - Создать проект
- `GET /api/user/projects` - Получить проекты пользователя
- `GET /api/user/credits` - Получить кредиты
- `POST /api/project/revision/:projectId` - Редактировать сайт
- `PUT /api/project/save/:projectId` - Сохранить код
- `GET /api/project/preview/:projectId` - Получить превью
- `GET /api/project/published` - Получить опубликованные проекты

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте лог-файлы в консоли
2. Убедитесь что OPENAI_API_KEY правильный
3. Перезагрузите сервер и клиент
4. Очистите кэш браузера (Ctrl+Shift+Delete)

---

**Готово к работе! 🎉**
