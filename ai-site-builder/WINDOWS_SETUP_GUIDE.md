# 🚀 Руководство по запуску AI Site Builder на Windows 10

Полное пошаговое руководство для запуска проекта на компьютере с Windows 10.

---

## 📋 Предварительные требования

Перед началом убедитесь, что у вас установлены:

1. **Node.js** (версия 18.0 или выше)
2. **PostgreSQL** (база данных)
3. **Git** (для клонирования репозитория)

### ✅ Проверка установки

Откройте **PowerShell** или **Command Prompt** и выполните:

```bash
node --version
npm --version
postgres --version
git --version
```

Если какая-то команда не найдена, установите соответствующее ПО.

---

## 📦 Шаг 1: Установка необходимого ПО

### 1.1 Установка Node.js

1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Запустите установщик и следуйте инструкциям
4. Убедитесь, что установлены npm и Node.js

### 1.2 Установка PostgreSQL

1. Перейдите на https://www.postgresql.org/download/windows/
2. Скачайте установщик
3. Запустите установщик
4. **Важно**: запомните пароль для пользователя `postgres`
5. Оставьте порт по умолчанию: **5432**

### 1.3 Установка Git

1. Перейдите на https://git-scm.com/
2. Скачайте установщик для Windows
3. Запустите установщик с параметрами по умолчанию

---

## 🔧 Шаг 2: Подготовка проекта

### 2.1 Клонирование репозитория

Откройте **PowerShell** в папке, где хотите сохранить проект, и выполните:

```bash
git clone https://github.com/anymasoft/ai.git
cd ai
cd ai-site-builder
```

### 2.2 Структура папок

После клонирования ваша структура должна быть:

```
ai-site-builder/
├── server/          ← Бэкенд (Node.js + Express)
├── client/          ← Фронтенд (React + Vite)
├── .env.example
└── README.md
```

---

## 💾 Шаг 3: Конфигурация переменных окружения

### 3.1 Создание базы данных PostgreSQL

Откройте **pgAdmin** (установился вместе с PostgreSQL) или используйте PowerShell:

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE ai_site_builder;

# Выход
\q
```

**Или через pgAdmin:**
1. Откройте pgAdmin (обычно `http://localhost:5050`)
2. Нажмите правой кнопкой на **Databases** → **Create** → **Database**
3. Введите имя: `ai_site_builder`
4. Нажмите **Save**

### 3.2 Конфигурация SERVER

Откройте папку **`server`** и создайте файл **`.env`** с конфигурацией:

```bash
cd server
```

Создайте файл `.env` (копируйте из `.env.example`):

```
# OpenAI Configuration
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxx

# Database Configuration
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/ai_site_builder

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# Better Auth Configuration
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=your_generated_secret_here

# CORS Configuration
TRUSTED_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment
NODE_ENV=development
```

**Замените значения на ваши:**
- `OPENAI_API_KEY` - получите на https://platform.openai.com/api-keys
- `YOUR_PASSWORD` - пароль PostgreSQL, который вы установили
- `STRIPE_SECRET_KEY` и `STRIPE_WEBHOOK_SECRET` - получите на https://stripe.com

### 3.3 Конфигурация CLIENT

Откройте папку **`client`** (в другой PowerShell вкладке):

```bash
cd client
```

Создайте файл `.env` (копируйте из `.env.example`):

```
VITE_API_URL=http://localhost:3000/api
VITE_APP_URL=http://localhost:5173
```

---

## 📚 Шаг 4: Установка зависимостей

### 4.1 Установка зависимостей SERVER

В PowerShell в папке **`server`**:

```bash
npm install
```

Это установит все необходимые пакеты (express, prisma, openai и т.д.)

### 4.2 Установка зависимостей CLIENT

В отдельной PowerShell вкладке в папке **`client`**:

```bash
npm install
```

Это установит React, Vite, Tailwind и другие зависимости.

---

## 🗄️ Шаг 5: Инициализация базы данных

### 5.1 Применение миграций Prisma

В папке **`server`** выполните:

```bash
npx prisma migrate deploy
```

Или, если нужно создать новую миграцию:

```bash
npx prisma migrate dev --name init
```

Это создаст все необходимые таблицы в базе данных.

### 5.2 Проверка базы данных

Можете проверить через pgAdmin, что таблицы созданы:

```bash
npx prisma studio
```

Откроется веб-интерфейс для просмотра данных.

---

## 🚀 Шаг 6: Запуск приложения

### 6.1 Запуск SERVER

В PowerShell в папке **`server`** выполните:

```bash
npm run server
```

Вы должны увидеть сообщение:
```
Server is running at http://localhost:3000
```

Сервер работает на **http://localhost:3000**

### 6.2 Запуск CLIENT

В отдельной PowerShell вкладке в папке **`client`** выполните:

```bash
npm run dev
```

Вы должны увидеть сообщение:
```
VITE v7.2.4  ready in xxx ms

➜  Local:   http://localhost:5173/
```

Клиент работает на **http://localhost:5173**

---

## ✨ Шаг 7: Проверка работоспособности

1. Откройте браузер и перейдите на **http://localhost:5173**
2. Должна загрузиться главная страница приложения
3. Зарегистрируйтесь или войдите
4. Попробуйте создать новый проект

Если видите ошибки в консоли браузера, проверьте:
- Запущен ли сервер на http://localhost:3000
- Правильны ли переменные окружения в `.env`
- Подключена ли база данных PostgreSQL

---

## 📁 Структура команд для запуска

| Что делать | Где | Команда |
|-----------|-----|---------|
| Установить зависимости SERVER | `server/` | `npm install` |
| Установить зависимости CLIENT | `client/` | `npm install` |
| Применить миграции БД | `server/` | `npx prisma migrate deploy` |
| Запустить сервер | `server/` | `npm run server` |
| Запустить клиент | `client/` | `npm run dev` |
| Просмотреть БД | `server/` | `npx prisma studio` |

---

## 🛠️ Полезные команды

### Построить проект для production

**Сервер:**
```bash
cd server
npm run build
npm start
```

**Клиент:**
```bash
cd client
npm run build
```

### Просмотр логов базы данных

```bash
cd server
npx prisma studio
```

### Очистка кэша Node

Если возникают проблемы с установкой:
```bash
npm cache clean --force
```

---

## ⚠️ Частые проблемы и решения

### Проблема: "port 3000 is already in use"
**Решение:** Другое приложение использует порт 3000. Закройте его или измените порт в `server.ts`

### Проблема: "OPENAI_API_KEY is required"
**Решение:** Проверьте что в `.env` в папке `server/` прописан `OPENAI_API_KEY`

### Проблема: "Connection refused" при подключении к БД
**Решение:**
- Проверьте что PostgreSQL запущен
- Проверьте правильность пароля в `DATABASE_URL`
- Проверьте что база `ai_site_builder` существует

### Проблема: "npm ERR! code ERESOLVE"
**Решение:**
```bash
npm install --legacy-peer-deps
```

### Проблема: Клиент не подключается к серверу
**Решение:** Проверьте что `TRUSTED_ORIGINS` в `.env` на сервере содержит `http://localhost:5173`

---

## 📞 Получение API ключей

### OpenAI API Key
1. Перейдите на https://platform.openai.com
2. Нажмите на иконку профиля → API keys
3. Нажмите Create new secret key
4. Скопируйте ключ и вставьте в `.env`

### Stripe Keys (опционально)
1. Перейдите на https://stripe.com
2. Регистрируйтесь и создайте аккаунт
3. Перейдите в Developers → API Keys
4. Скопируйте Secret Key и Webhook Secret

---

## 🎉 Готово!

Ваше приложение должно быть запущено и работать на:
- **Фронтенд:** http://localhost:5173
- **Бэкенд:** http://localhost:3000
- **База данных:** http://localhost:5432

Теперь вы можете:
- Создавать проекты
- Генерировать веб-сайты с помощью AI
- Управлять версиями кода
- Просматривать и редактировать сгенерированные сайты

Успехов! 🚀
