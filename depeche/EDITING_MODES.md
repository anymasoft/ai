# 📝 Система редактирования с 3 режимами

Редактор автоматически определяет режим работы по **состоянию текста**, а не по ключевым словам в промпте.

---

## 🔹 РЕЖИМ 1: ГЕНЕРАЦИЯ ПЛАНА

**Когда активируется:** Текст статьи **ПУСТОЙ**

**Что передать в промпт:**
- Любой текст, который можно интерпретировать как тему или идею
- Примеры:
  - `"Обезьяны в Африке"`
  - `"Хочу написать о климатических изменениях"`
  - `"Вот материал из Википедии: [текст]"`
  - `"Лекция про квантовую физику"`

**Что происходит:**
1. Весь текст промпта передается в LLM как **исходный материал/тема**
2. LLM генерирует **структурированный план** (5-7 пунктов)
3. План появляется в редакторе с эффектом печатной машинки

**Результат:** Статья получает четкую структуру для дальнейшего написания

---

## 🔹 РЕЖИМ 2: РЕДАКТИРОВАНИЕ ВСЕГО ТЕКСТА

**Когда активируется:**
- Текст статьи **СУЩЕСТВУЕТ**
- **НЕ ВЫДЕЛЕНО** никакого текста курсором

**Что передать в промпт:**
- **Инструкция редактирования** для всего текста
- Примеры:
  - `"Сделай текст более научным"`
  - `"Улучши стиль и грамматику"`
  - `"Убери повторы и лишние слова"`
  - `"Адаптируй для читателей 12-14 лет"`
  - `"Сократи вдвое без потери смысла"`

**Что происходит:**
1. **ВСЕ** содержимое статьи передается в LLM
2. Инструкция применяется ко **всему тексту**
3. LLM возвращает **полностью переписанный текст**
4. Новый текст заменяет старый в редакторе

**Внимание:** Это глобальная операция! Весь текст может значительно измениться.

---

## 🔹 РЕЖИМ 3: РЕДАКТИРОВАНИЕ ВЫДЕЛЕННОГО ФРАГМЕНТА

**Когда активируется:**
- Текст статьи **СУЩЕСТВУЕТ**
- **ВЫДЕЛЕН** фрагмент текста (мышью или клавиатурой)

**Что передать в промпт:**
- **Инструкция редактирования** для фрагмента
- Примеры:
  - `"Раскрой подробнее"`
  - `"Сделай более очень ясным"`
  - `"Добавь конкретный пример"`
  - `"Упростить формулировку"`
  - `"Переформулируй более научно"`

**Что происходит:**
1. Система захватывает:
   - **Выделенный фрагмент** (то, что нужно отредактировать)
   - **Предыдущий абзац** (для контекста)
   - **Следующий абзац** (для связности)
2. Инструкция применяется **ТОЛЬКО К ФРАГМЕНТУ**
3. LLM возвращает **только отредактированный фрагмент**
4. Фрагмент заменяется в тексте, всё остальное остаётся неизменным

**Преимущества:**
- Точное редактирование без риска потерять остальной текст
- Сохраняется стиль и логика статьи
- Быстрые локальные правки

---

## 📋 Порядок определения режима (СТРОГИЙ)

Система проверяет в таком порядке:

```
1. Если текст пуст → РЕЖИМ 1
2. Иначе, если выделен текст → РЕЖИМ 3
3. Иначе → РЕЖИМ 2
```

**Примечание:** Режим определяется автоматически, пользователю не нужно указывать его явно.

---

## 💡 Примеры использования

### Пример 1: Создание статьи про кошек
1. Откройте пустую статью
2. В prompt bar введите: `"Домашние кошки и их особенности"`
3. Нажмите Enter
4. → **РЕЖИМ 1**: LLM создаст план из 5-7 пунктов

### Пример 2: Улучшить весь текст
1. У вас есть написанная статья
2. Ничего не выделяйте
3. В prompt bar введите: `"Сделай текст более живым и интересным"`
4. Нажмите Enter
5. → **РЕЖИМ 2**: LLM переписать ВСЮ статью с учетом инструкции

### Пример 3: Улучшить одно предложение
1. У вас есть написанная статья
2. **Выделите** одно предложение, которое нужно исправить
3. В prompt bar введите: `"Упростить, чтобы было понятно детям"`
4. Нажмите Enter
5. → **РЕЖИМ 3**: LLM изменит ТОЛЬКО это предложение

---

## ⚙️ Технические детали

### Backend endpoints

- **Режим 1:** `POST /api/articles/{id}/plan`
  - Тело: `{ "topic": "Тема для плана" }`

- **Режим 2:** `POST /api/articles/{id}/edit-full`
  - Тело: `{ "instruction": "Инструкция для правки" }`

- **Режим 3:** `POST /api/articles/{id}/edit-fragment`
  - Тело: `{ "before_context": "...", "fragment": "...", "after_context": "...", "instruction": "..." }`

### Frontend функции

- `detectMode()` — определить текущий режим
- `getSelectionWithContext()` — получить выделение с контекстом
- `applyPrompt()` — главная функция обработки промпта (использует режимы)

### LLM функции

- `generate_article_plan(topic)` — Режим 1
- `edit_full_text(text, instruction)` — Режим 2
- `edit_fragment(before, fragment, after, instruction)` — Режим 3

---

## 🎯 Принципы дизайна

✅ **Пользователь не думает о режимах** — они определяются автоматически

✅ **Контекстная интерпретация** — промпт понимается исходя из состояния редактора

✅ **Предсказуемость** — одни и те же действия дают одинаковые результаты

✅ **Безопасность фрагмента** — в режиме 3 остальной текст не меняется

✅ **Гибкость** — любой текст в промпте интерпретируется как инструкция/тема

---

## 🐛 Отладка

Все действия логируются в консоли браузера с префиксами:
- `[PROMPT]` — обработка промпта
- `[EDIT_FULL_API]` — режим 2
- `[EDIT_FRAGMENT_API]` — режим 3
- `[SELECTION]` — работа с выделением

В серверных логах:
- `[GENERATE_PLAN]` — режим 1
- `[EDIT_FULL]` — режим 2
- `[EDIT_FRAGMENT]` — режим 3
