# Open Lovable - Полное руководство

Документация по установке, конфигурации и использованию Open Lovable с OpenAI.

## Что такое Open Lovable?

**Open Lovable** - это революционное приложение на Next.js, которое позволяет создавать полнофункциональные React приложения через диалог с AI. Вы просто описываете, что вам нужно, а AI автоматически:

- Генерирует React компоненты
- Устанавливает необходимые зависимости
- Применяет стили с Tailwind CSS
- Запускает приложение в облачном sandbox
- Показывает превью в реальном времени

## Быстрый старт (3 шага)

### 1️⃣ Клонирование и установка

```bash
cd open-lovable
pnpm install
```

### 2️⃣ Конфигурация .env.local

Получите API ключи:

- **OpenAI** → https://platform.openai.com/api-keys
- **Firecrawl** → https://firecrawl.dev
- **Vercel** → https://vercel.com/account/tokens (для sandbox)

Создайте `.env.local`:

```bash
# Обязательно
FIRECRAWL_API_KEY=your_key_here
OPENAI_API_KEY=your_key_here

# Sandbox
SANDBOX_PROVIDER=vercel
VERCEL_OIDC_TOKEN=auto_generated_by_vercel_env_pull
```

Получить Vercel token автоматически:

```bash
vercel link
vercel env pull
```

### 3️⃣ Запуск

```bash
pnpm dev
```

Откройте http://localhost:3000

## Полное описание файлов

### Файлы в корне проекта (anymasoft/ai)

| Файл | Описание |
|------|----------|
| `QUICKSTART_OPEN_LOVABLE_OPENAI.md` | 📚 **НАЧНИТЕ ОТСЮДА** - Краткая инструкция запуска |
| `OPEN_LOVABLE_QUICK_REFERENCE.md` | 📋 Справочник команд и параметров |
| `OPEN_LOVABLE_ARCHITECTURE.md` | 🏗️ Полная архитектура приложения |
| `OPEN_LOVABLE_README.md` | 📖 Это файл - полное руководство |

### Папка open-lovable/

```
open-lovable/
├── app/                    # Next.js приложение
│   ├── api/               # REST API endpoints
│   ├── page.tsx           # Главная страница
│   └── layout.tsx         # Layout приложения
├── lib/                    # Утилиты и логика
│   ├── ai/                # AI провайдеры
│   ├── sandbox/           # Sandbox логика
│   └── ...                # Другие утилиты
├── components/            # React компоненты
├── config/               # Конфигурация
│   └── app.config.ts     # Главная конфиг
├── public/               # Статические файлы
├── .env.example          # Пример переменных
├── package.json          # Зависимости
└── README.md             # Оригинальный README
```

## Архитектура

### Основные компоненты

```
┌─────────────────────────────────────────────┐
│        React Chat Interface (Frontend)       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│        Next.js API Routes (Backend)          │
│  - apply-ai-code-stream                     │
│  - analyze-edit-intent                      │
│  - create-ai-sandbox                        │
│  - sandbox-status                           │
│  - install-packages                         │
└──────────────────┬──────────────────────────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
    ┌────────┐ ┌────────┐ ┌─────────┐
    │ OpenAI │ │ Vercel │ │Firecrawl│
    │  (AI)  │ │(Sandbox)│ │(WebAPI) │
    └────────┘ └────────┘ └─────────┘
```

### Поток работы

1. **Пользователь пишет** - описывает желаемое приложение в чате
2. **Анализ** - система определяет тип изменения (create/update/delete)
3. **AI генерирует** - OpenAI создаёт React код с примерами
4. **Применение** - код применяется в песочнице (Vercel Sandbox)
5. **Установка** - NPM пакеты устанавливаются в sandbox
6. **Запуск** - приложение автоматически запускается
7. **Превью** - результат показывается в реальном времени

## Поддерживаемые модели AI

### OpenAI (рекомендуется)

```
openai/gpt-5              ← Рекомендуется для сложных задач
openai/gpt-4o             ← Хороший баланс скорости и качества
openai/gpt-4              ← Базовая модель
openai/gpt-3.5-turbo      ← Самая быстрая
```

### Другие провайдеры

```
anthropic/claude-sonnet-4      ← Claude (Anthropic)
google/gemini-3-pro            ← Gemini (Google)
moonshotai/kimi-k2-instruct    ← Kimi K2 (Groq)
```

## Примеры использования

### Создание todo приложения

```
Запрос:
"Создай todo список с возможностью добавления,
удаления и отметки как выполненной.
Сохраняй данные в localStorage.
Используй красивый дизайн с Tailwind CSS."

Результат:
✅ React компонент с функциональностью
✅ localStorage для сохранения
✅ Tailwind стили
✅ Полностью рабочее приложение
```

### Изменение стилей

```
Запрос:
"Добавь тёмную тему с переключателем день/ночь"

Результат:
✅ Компонент обновлён
✅ Добавлена поддержка dark mode
✅ Переключатель темы в интерфейсе
✅ CSS переменные для тем
```

### Добавление новой функции

```
Запрос:
"Добавь фильтр по статусу и категориям"

Результат:
✅ Новые фильтры добавлены
✅ Логика фильтрации работает
✅ UI компоненты обновлены
✅ Всё синхронизировано
```

## Переменные окружения

### Полный набор для .env.local

```env
# =================== ОБЯЗАТЕЛЬНЫЕ ===================

# Firecrawl API (для веб-скрепинга)
FIRECRAWL_API_KEY=fc_...
# Получить: https://firecrawl.dev

# =================== AI ПРОВАЙДЕРЫ ===================
# Выберите МИНИМУМ один

# OpenAI (рекомендуется)
OPENAI_API_KEY=sk-...
# Получить: https://platform.openai.com/api-keys

# Anthropic (альтернатива)
# ANTHROPIC_API_KEY=sk-ant-...
# Получить: https://console.anthropic.com

# Google Gemini (альтернатива)
# GEMINI_API_KEY=AIz...
# Получить: https://aistudio.google.com/app/apikey

# Groq (альтернатива)
# GROQ_API_KEY=gsk_...
# Получить: https://console.groq.com

# =================== SANDBOX PROVIDER ===================

# Выберите провайдер: vercel (по умолчанию) или e2b
SANDBOX_PROVIDER=vercel

# --- Vercel Sandbox (рекомендуется) ---

# Вариант 1: OIDC Token (рекомендуется)
VERCEL_OIDC_TOKEN=auto_generated_by_vercel_env_pull

# Вариант 2: Personal Access Token
# (используйте, если OIDC не работает)
# VERCEL_TEAM_ID=team_xxxxxxxxx
# VERCEL_PROJECT_ID=prj_xxxxxxxxx
# VERCEL_TOKEN=vercel_xxxxxxxxxxxx

# --- E2B Sandbox (альтернатива) ---
# Раскомментируйте для использования E2B
# SANDBOX_PROVIDER=e2b
# E2B_API_KEY=your_e2b_api_key

# =================== ОПЦИОНАЛЬНО ===================

# Vercel AI Gateway (для доступа к нескольким моделям)
# AI_GATEWAY_API_KEY=...
# Получить: https://vercel.com/dashboard/ai-gateway

# Morph Fast Apply (для ускоренного применения правок)
# MORPH_API_KEY=...
# Получить: https://morphllm.com/dashboard
```

## Команды разработки

```bash
# Установка зависимостей
pnpm install
npm install
yarn install

# Запуск dev сервера
pnpm dev          # http://localhost:3000 с Turbopack

# Сборка для production
pnpm build

# Запуск production сборки
pnpm start

# Линтинг кода
pnpm lint

# Запуск тестов
pnpm test:all     # Все тесты
pnpm test:api     # Тесты API endpoints
pnpm test:code    # Тесты генерации кода
```

## Получение Vercel OIDC Token

### Автоматический способ (рекомендуется)

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Свяжите проект с Vercel
vercel link

# 3. Загрузите переменные в .env.local
vercel env pull

# Готово! VERCEL_OIDC_TOKEN автоматически добавлена
```

### Ручной способ

1. Откройте https://vercel.com/account/tokens
2. Создайте Personal Access Token
3. Получите свой Team ID и Project ID из https://vercel.com/dashboard
4. Добавьте в `.env.local`:

```env
VERCEL_TEAM_ID=team_xxxxxxxxx
VERCEL_PROJECT_ID=prj_xxxxxxxxx
VERCEL_TOKEN=vercel_xxxxxxxxxxxx
```

## Системные требования

- **Node.js** ≥ 18
- **npm/pnpm/yarn** - пакетный менеджер
- **Верный доступ в интернет** - для API вызовов

### Рекомендуемая конфигурация

- **ОС**: Linux, macOS, Windows
- **Node.js**: 20.x LTS
- **Пакетный менеджер**: pnpm (быстрее, чем npm)
- **Память**: минимум 2GB RAM

## Решение проблем

### Ошибка: "OPENAI_API_KEY is undefined"

**Причина**: Переменная не задана в `.env.local`

**Решение**:
```bash
# Добавьте в .env.local
echo "OPENAI_API_KEY=sk-your_key_here" >> .env.local
```

### Ошибка: "VERCEL_OIDC_TOKEN undefined"

**Причина**: Vercel токен не получен

**Решение**:
```bash
# Загрузите токен автоматически
vercel link
vercel env pull
```

### Ошибка: "Sandbox timeout"

**Причина**: Операция заняла слишком много времени

**Решение**: Увеличьте timeout в `config/app.config.ts`:
```typescript
vercelSandbox: {
  timeoutMinutes: 20,  // Было 15
}
```

### Ошибка: "npm install failed"

**Причина**: Конфликт зависимостей npm

**Решение**: Используйте legacy peer deps (уже включено в конфиге)
```typescript
// config/app.config.ts
useLegacyPeerDeps: true
```

## Контрибьютинг

Проект принимает contributions. Процесс:

1. Fork репозитория
2. Создайте feature branch
3. Commit changes
4. Push и создайте Pull Request

## Лицензия

MIT - свободна для использования в коммерческих и личных проектах

## Полезные ссылки

- 🔗 [Open Lovable GitHub](https://github.com/firecrawl/open-lovable)
- 🌐 [Lovable.dev](https://lovable.dev/) - облачная версия
- 📚 [Next.js документация](https://nextjs.org/docs)
- 🎨 [Tailwind CSS](https://tailwindcss.com)
- 🤖 [OpenAI API](https://platform.openai.com/docs)
- 🔧 [Vercel документация](https://vercel.com/docs)
- 🕷️ [Firecrawl документация](https://docs.firecrawl.dev)

## FAQ

**Q: Можно ли использовать другого провайдера вместо OpenAI?**
A: Да! Поддерживаются Claude (Anthropic), Gemini (Google), Groq и другие. Просто добавьте нужный API ключ в `.env.local`.

**Q: Где хранятся созданные приложения?**
A: Они запускаются в облачном sandbox (Vercel по умолчанию). Код можно экспортировать в ZIP.

**Q: Сколько это стоит?**
A: Стоимость зависит от использования OpenAI API и Vercel Sandbox. Возможны бесплатные тарифы на оба сервиса.

**Q: Можно ли развернуть локально?**
A: Да, используйте E2B вместо Vercel Sandbox или развернитесь на своём сервере.

**Q: Какие фреймворки поддерживаются?**
A: Основной - React. Можно создавать Next.js приложения. Поддерживаются все NPM пакеты.

---

**Последнее обновление**: 2025-12-20
**Версия**: Open Lovable v0.1.0
**Статус**: ✅ Полностью функционально
