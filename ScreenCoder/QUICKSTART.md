# ScreenCoder - Быстрый Старт

## 🚀 Самый быстрый способ (5 минут)

### 1. Получить API ключ (выбрать ОДИН вариант)

#### ✅ Вариант A: GPT-4o (OpenAI) - РЕКОМЕНДУЕТСЯ
```bash
# 1. Перейти на https://platform.openai.com/api/keys
# 2. Нажать "Create new secret key"
# 3. Скопировать ключ (начинается с sk-)
# 4. Сохранить в файл:

echo "sk-ВАШ-КЛЮЧ-ЗДЕСЬ" > gpt_api.txt
```

#### Вариант B: Qwen (Alibaba) - ДЕШЕВЛЕ
```bash
# 1. Зарегистрироваться на https://dashscope.aliyun.com/
# 2. Создать API ключ в личном кабинете
# 3. Сохранить:

echo "sk-ВАШ-КЛЮЧ-ЗДЕСЬ" > qwen_api.txt
```

#### Вариант C: Doubao (Volcengine) - КИТАЙСКИЙ
```bash
# 1. Зарегистрироваться на https://console.volcengine.com
# 2. Создать API ключ для Doubao
# 3. Сохранить:

echo "ВАШ-КЛЮЧ-ЗДЕСЬ" > doubao_api.txt
```

### 2. Установить зависимости
```bash
pip install -r requirements_minimal.txt
```

### 3. Запустить проект

#### Способ A: С GPT-4o (по умолчанию)
```bash
# Отредактируйте block_parsor.py строка 75:
# client = Doubao(api_path) -> client = GPT("gpt_api.txt")

python block_parsor.py
python html_generator.py
```

#### Способ B: Простой тест (без API)
```bash
# Просто посмотреть структуру:
python image_box_detection.py
python UIED/run_single.py
```

## 📋 Полный workflow

```bash
# Часть 1: Генерация HTML с плейсхолдерами
python block_parsor.py          # Обнаружить блоки (header, sidebar, etc)
python html_generator.py        # Сгенерировать HTML/CSS

# Часть 2: Замена плейсхолдеров на реальные изображения
python image_box_detection.py   # Найти плейсхолдеры
python UIED/run_single.py       # Детектировать UI элементы
python mapping.py               # Маппить плейсхолдеры на изображения
python image_replacer.py        # Заменить плейсхолдеры

# ИЛИ все сразу:
python main.py
```

## 🔍 Тестовые изображения

В проекте уже есть 3 тестовых скриншота:
```
data/input/
  ├── test1.png (Instagram-like)
  ├── test2.png (меньший сайт)
  └── test3.png (большой сайт)
```

## 📁 Результаты

Сгенерированный HTML найдется в:
```
data/output/
```

## ⚠️ Частые ошибки

### Ошибка: "ModuleNotFoundError: No module named 'cv2'"
**Решение:**
```bash
pip install opencv-python
```

### Ошибка: "No module named '_cffi_backend'"
**Решение:** НЕ используйте Gemini, используйте GPT или Qwen

### Ошибка: "API key not found"
**Решение:**
```bash
# Убедитесь что файл существует и имеет ключ:
cat gpt_api.txt
# Должен вывести: sk-...
```

### Ошибка при запуске main.py
**Решение:** Запустите скрипты по отдельности чтобы найти ошибку:
```bash
python block_parsor.py
# Если сработает, то:
python html_generator.py
```

## 🎯 Минимальный тест (без API)

Если у вас нет API ключа, можно протестировать части, которые не требуют LLM:

```bash
# Обнаружение плейсхолдеров
python image_box_detection.py

# UI элементы детектирование
python UIED/run_single.py

# Маппинг
python mapping.py
```

## 📊 Ожидаемые результаты

1. **block_parsor.py** → Detects page structure (header, sidebar, main, nav)
2. **html_generator.py** → Generates HTML/CSS with gray image placeholders
3. **image_box_detection.py** → Finds placeholder coordinates
4. **UIED/run_single.py** → Detects UI components
5. **mapping.py** → Maps components to placeholders
6. **image_replacer.py** → Replaces placeholders with cropped images

**Финальный результат:** `data/output/output.html` - полный HTML с изображениями

## 🆘 Нужна помощь?

1. Проверьте что Python 3.9+ установлен:
   ```bash
   python --version
   ```

2. Проверьте что API ключ правильный:
   ```bash
   cat gpt_api.txt
   ```

3. Проверьте зависимости:
   ```bash
   python -c "import cv2; import openai; print('OK')"
   ```

4. Читайте файл `SCREENCODER_ANALYSIS.md` для полного анализа проблем
