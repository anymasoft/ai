# Запуск проекта Screen2code

## ⚡ Быстрый старт (одна команда)

```bash
npm run dev
```

Эта команда запустит:
- **Backend** на http://localhost:7001 (FastAPI + Python)
- **Frontend** на http://localhost:5173 (Vite + React)

---

## 🔧 Ручной запуск (раздельно)

### Backend
```bash
npm run dev:backend
# или
cd backend && poetry run uvicorn main:app --reload
```

### Frontend
```bash
npm run dev:frontend
# или
cd frontend && npm run dev
```

---

## 📋 Первоначальная настройка

### 1. Установить зависимости

**Корневая директория:**
```bash
npm install
```

**Backend:**
```bash
cd backend
poetry install
```

**Frontend:**
```bash
cd frontend
npm install
```

### 2. Создать .env файлы

**Frontend (.env.local):**
```bash
cd frontend
cp .env.example .env.local
```

Содержимое:
```env
VITE_API_BASE_URL=http://localhost:7001
```

**Backend (.env):**
```bash
cd backend
# Если есть .env.example - скопировать
# В противном случае - backend работает с дефолтными настройками
```

### 3. Создать admin пользователя (опционально)

```bash
cd backend
poetry run python create_admin.py
```

Для доступа к админ-панели добавить в браузере:
```javascript
localStorage.setItem("dev_admin_email", "admin@test.com")
```

---

## 🎯 Доступные скрипты

```bash
npm run dev           # Запустить backend + frontend одновременно
npm run dev:backend   # Запустить только backend (port 7001)
npm run dev:frontend  # Запустить только frontend (port 5173)
npm run test          # Запустить все тесты
npm run test:backend  # Тесты backend
npm run test:frontend # Тесты frontend
```

---

## 🌐 URLs

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:7001
- **API Health:** http://localhost:7001/api/health

---

## 📦 Технологии

**Frontend:**
- Vite
- React
- TypeScript
- TailwindCSS
- shadcn/ui

**Backend:**
- FastAPI
- Python 3.10+
- SQLite
- Poetry

---

## 🚨 Troubleshooting

### Backend не запускается

**Проверить Poetry:**
```bash
poetry --version
```

Если не установлен:
```bash
pip install poetry
```

### Frontend не запускается

**Проверить Node.js:**
```bash
node --version  # Должен быть >= 18
npm --version
```

**Переустановить зависимости:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### Port уже занят

**Освободить порт 7001 (Backend):**
```bash
# Linux/Mac
lsof -ti:7001 | xargs kill -9

# Windows
netstat -ano | findstr :7001
taskkill /PID <PID> /F
```

**Освободить порт 5173 (Frontend):**
```bash
# Linux/Mac
lsof -ti:5173 | xargs kill -9

# Windows
netstat -ano | findstr :5173
taskkill /PID <PID> /F
```

### concurrently не работает

**Переустановить:**
```bash
npm install concurrently@latest --save-dev
```

---

## 📚 Документация

- **Feedback + Admin система:** `FEEDBACK_ADMIN_CHECKLIST.md`
- **API документация:** После запуска - http://localhost:7001/docs (если включено)

---

## 🎉 Готово!

После `npm run dev` должны запуститься оба сервера:
```
[backend] INFO:     Uvicorn running on http://127.0.0.1:7001
[frontend] VITE v5.x.x ready in xxx ms
[frontend] ➜  Local:   http://localhost:5173/
```

Откройте http://localhost:5173 в браузере.
