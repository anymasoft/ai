# 👋 Добро пожаловать в Video Reader AI!

Этот документ поможет быстро войти в проект.

## 1. Архитектура

**Компоненты:**

- **Chrome Extension** - расширение для браузера
- **Flask Backend** (api.beem.ink) - основной API сервер
- **Astro Landing** (beem.ink) - лендинг на Astro
- **Admin Panel** - панель администратора
- **SQLite databases** - базы данных пользователей и переводов
- **Nginx reverse proxy** - веб-сервер
- **systemd service** - сервис для управления бэкендом

Все запросы идут через HTTPS.

## 2. Основные директории

| Компонент | Путь |
|-----------|------|
| Backend | `/var/www/api.beem.ink/` |
| Landing | `/var/www/beem.ink/dist/` |
| Admin | `/var/www/api.beem.ink/admin/` |
| Extension | `/var/www/api.beem.ink/extension/` |

## 3. Как обновлять backend

1. Внести изменения в `SERVER_TEMPLATE.py`
2. Загрузить на сервер
3. Выполнить команду:

```bash
sudo systemctl restart videoreader.service
```

## 4. Как обновлять лендинг

```bash
npm run build
scp -r dist/ root@server:/var/www/beem.ink/dist
```

## 5. База данных

Используется SQLite для хранения данных:

```bash
sqlite3 users.db
sqlite3 translations.db
```

## 6. Авторизация

Вся логика авторизации построена на:
- Google OAuth token → 
- Backend выдает собственный JWT

## 7. Тарифы

**Доступные тарифные планы:**

- **Free** - базовый функционал
- **Pro** (30 дней) - расширенные возможности
- **Premium** (30 дней) - полный доступ ко всем функциям

Сброс плана происходит автоматически по истечении периода подписки.

## 8. Разработка

### Backend (Flask)
- Основной файл: `SERVER_TEMPLATE.py`
- Виртуальное окружение: `venv/`
- Зависимости: `requirements.txt`

### Frontend (Astro)
- Директория: `astro-landing/`
- Dev-сервер: `npm run dev`
- Сборка: `npm run build`

### Chrome Extension
- Директория: `extension/`
- Сборка через стандартные инструменты Chrome

## 9. Мониторинг и логи

- Логи бэкенда: `journalctl -u videoreader.service`
- Логи Nginx: `/var/log/nginx/`
- Мониторинг через systemd

## 10. Контакты и поддержка

Для вопросов по разработке обращайтесь к техническому лиду проекта.