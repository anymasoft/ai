# JobRadar v0

**JobRadar v0** — это Telegram-бот для мониторинга публичных Telegram-каналов с поиском ключевых слов.

## Функциональность

### ✨ Основные возможности:

1. **📡 Управление источниками (каналы)**
   - ➕ Добавить канал (по username или ссылке)
   - 📋 Показать список добавленных каналов
   - ❌ Включить/отключить канал из мониторинга

2. **🔑 Управление ключевыми словами**
   - ➕ Добавить ключевое слово или фразу
   - 📋 Показать список всех слов
   - 🗑 Удалить ключевое слово

3. **📢 Статус мониторинга**
   - Количество активных каналов
   - Количество активных ключевых слов
   - Время последнего обработанного поста

### 🎯 Логика мониторинга:

- **Polling** каждые **10 секунд** (как в LeadScanner)
- Для каждого нового поста проверяется совпадение с ключевыми словами (case-insensitive substring match)
- При совпадении **выводит в консоль**:
  - Имя канала
  - Время поста
  - Автора
  - Найденные ключевые слова
  - Текст поста (первые 200 символов)

## Установка

### 1. Получить Telegram API учетные данные

1. Перейди на https://my.telegram.org/apps
2. Введи номер телефона и получи SMS
3. Скопируй **API ID** и **API Hash**

### 2. Создать Telegram Bot

1. Напиши @BotFather в Telegram
2. Команда `/newbot`
3. Следуй инструкциям
4. Скопируй **Bot Token**

### 3. Узнать свой Telegram ID

1. Напиши @userinfobot в Telegram
2. Скопируй свой **ID**

### 4. Установить зависимости

```bash
cd JobRadar
pip install -r requirements.txt
```

### 5. Конфигурация

Скопируй `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируй `.env` и вставь свои значения:

```env
TELEGRAM_BOT_TOKEN=xxxxxx
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=xxxxx
TELEGRAM_PHONE=+7999xxx
TELEGRAM_ADMIN_ID=123456789
```

## Запуск

```bash
python main.py
```

При первом запуске потребуется авторизация в Telegram (потребуется ввести код из SMS).

## Использование

### Команда /start

Откроет главное меню:

```
🤖 JobRadar v0 - Мониторинг каналов

[📡 Источники]
[🔑 Ключевые слова]
[📢 Статус]
```

### 📡 Источники (Каналы)

**Добавить канал:**
- Кликни "➕ Добавить канал"
- Введи имя канала (например: `@python_jobs` или `https://t.me/python_jobs`)
- Бот добавит канал в БД

**Список каналов:**
- Кликни "📋 Показать список"
- Увидишь все добавленные каналы
- Кликни на канал чтобы включить/отключить его (🟢 / 🔴)

**Удаление:**
- Пока что надо удалять вручную через БД (в v1 будет кнопка удаления)

### 🔑 Ключевые слова

**Добавить слово/фразу:**
- Кликни "➕ Добавить слово/фразу"
- Введи слово или фразу
- Примеры: `Python`, `Data Science`, `Senior Developer`

**Список слов:**
- Кликни "📋 Показать список"
- Все добавленные слова
- Кликни на слово чтобы удалить его (🗑)

### 📢 Статус

Показывает:
- 📡 Количество активных каналов
- 🔑 Количество активных ключевых слов
- ⏰ Время последнего обработанного поста
- 🟢 Статус мониторинга (АКТИВЕН)

## Примеры использования

### Пример 1: Мониторинг Python вакансий

1. Добавь каналы:
   - `@python_jobs`
   - `@python_vacancies`
   - `@dev_jobs_russia`

2. Добавь ключевые слова:
   - `Python`
   - `Senior`
   - `AWS`
   - `Backend`

3. Бот будет каждые 10 сек проверять новые посты в этих каналах

4. В консоли увидишь:

```
📡 Проверяю канал @python_jobs...
  🎯 СОВПАДЕНИЕ НАЙДЕНО!
     Канал: @python_jobs
     Время: 2026-01-23 15:30:45
     Автор: @recruiter_name
     Ключевые слова: python, senior
     Текст: Senior Python Developer (AWS, PostgreSQL, 5+ years)...
```

## Структура файлов

```
JobRadar/
├── main.py              # Telegram бот с inline-кнопками
├── monitor.py           # Polling логика мониторинга
├── config.py            # Конфигурация
├── models.py            # ORM модели (Channel, Keyword)
├── database.py          # Инициализация SQLAlchemy
├── requirements.txt     # Зависимости
├── .env.example         # Пример конфигурации
├── jobradar.db          # SQLite база данных (создается автоматически)
└── jobradar_session     # Telegram сессия (создается при первом запуске)
```

## Архитектура

### Технологический стек:

- **Telethon** — User Client для чтения публичных каналов (как LeadScanner)
- **python-telegram-bot** — Bot API для управления (inline-кнопки)
- **SQLAlchemy** — ORM для работы с БД
- **SQLite** — Легкая база данных
- **APScheduler** — Scheduling для polling каждые 10 сек

### Модели данных:

**Channel:**
- id (Primary Key)
- username (уникальный)
- enabled (boolean)
- last_message_id (для отслеживания новых постов)
- created_at (время добавления)

**Keyword:**
- id (Primary Key)
- word (уникальное)
- enabled (boolean)
- created_at (время добавления)

## Заметки разработчики

### Polling vs Event-based:

В v0 используется **Polling-модель** (как в LeadScanner):
- ✅ Простая реализация
- ✅ Надежная (не зависит от сессий)
- ❌ Задержка обнаружения (до 10 сек)
- ❌ Больше запросов к API

Если нужна **Event-based архитектура** (как LeadSensei, реал-тайм обнаружение) — это v1+ задача.

### Что НЕ реализовано в v0:

- ❌ Публикация в Telegram при совпадении
- ❌ AI-анализ постов
- ❌ Email/Webhook уведомления
- ❌ Веб-интерфейс дашборда
- ❌ Сохранение истории совпадений

Это намеренно! v0 — это **рабочий каркас**, основа для дальнейшего развития.

## Решение проблем

### Ошибка: "TELEGRAM_BOT_TOKEN not set"

Убедись что:
1. Создал файл `.env`
2. Вставил корректный Bot Token от @BotFather
3. Нет пробелов в начале значения

### Ошибка: "ChannelPrivateError"

Канал приватный или был удален. Бот автоматически отключит его.

### Медленная обработка

Если много каналов/ключевых слов, увеличь интервал `POLLING_INTERVAL_SECONDS` в `config.py`.

## Лицензия

MIT
