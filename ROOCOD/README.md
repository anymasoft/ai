# VPN Manager Pro

Полнофункциональное браузерное расширение VPN с админ-панелью для управления прокси и мониторингом пользователей.

## 📋 Особенности

### Для обычных пользователей:
- ✅ Простое включение/выключение VPN
- ✅ Выбор локации из доступных прокси
- ✅ Просмотр текущего IP и статуса подключения
- ✅ Детали текущего прокси
- ✅ Проверка скорости подключения
- ✅ Параметры (автоподключение, kill-switch)

### Для администраторов:
- ✅ Полное управление прокси (создание, редактирование, удаление)
- ✅ Управление пользователями
- ✅ Система статистики и аналитики
- ✅ Просмотр логов системы
- ✅ Конфигурирование параметров системы

## 🚀 Быстрый старт

### 1. Установка зависимостей (Backend)

```bash
cd backend
pip install -r requirements.txt
```

### 2. Конфигурация

```bash
cd backend
cp .env.example .env
# Отредактируйте .env при необходимости
```

### 3. Запуск Backend сервера

```bash
cd backend
python server.py
```

Backend будет доступен на `http://localhost:5000`

### 4. Установка расширения в браузер

**Chrome/Edge:**
1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `extension/`

**Firefox:**
1. Откройте `about:debugging#/runtime/this-firefox`
2. Нажмите "Загрузить временное дополнение"
3. Выберите файл `extension/manifest.json`

## 🏗️ Архитектура проекта

```
ROOCOD/
├── extension/
│   ├── manifest.json           # Манифест расширения
│   ├── popup.html              # Интерфейс юзера
│   ├── popup.css               # Стили интерфейса
│   ├── popup.js                # Логика интерфейса
│   ├── background.js           # Service Worker
│   ├── content.js              # Content Script
│   ├── assets/
│   │   ├── logo.png
│   │   └── icons/
│   │       ├── icon-16.png
│   │       ├── icon-48.png
│   │       └── icon-128.png
│   └── admin/
│       ├── admin.html          # Админ-панель
│       ├── admin.css
│       ├── admin.js
│       ├── admin-login.html    # Страница входа
│       ├── admin-login.css
│       └── admin-login.js
├── backend/
│   ├── server.py               # Flask приложение
│   ├── database.py             # Работа с БД
│   ├── config.py               # Конфигурация
│   ├── requirements.txt
│   └── .env.example
└── README.md
```

## 🔧 Развёртывание VPS с прокси

### Вариант 1: Использование готовой VPS с прокси

**Рекомендуемые провайдеры:**
- **Residential proxies:** Bright Data, Oxylabs, Smartproxy
- **Data center proxies:** Luminati, IPRoyal, 922.co

### Вариант 2: Развёртывание собственного прокси-сервера

#### Использование Tinyproxy на Linux:

```bash
# Установка
sudo apt update
sudo apt install tinyproxy

# Конфигурация
sudo nano /etc/tinyproxy/tinyproxy.conf

# Изменить строки:
# Port 8080
# Listen 0.0.0.0

# Запуск
sudo systemctl start tinyproxy
sudo systemctl enable tinyproxy

# Проверка
curl -x 127.0.0.1:8080 http://example.com
```

#### Использование Squid на Linux:

```bash
# Установка
sudo apt update
sudo apt install squid

# Конфигурация
sudo nano /etc/squid/squid.conf

# Добавить:
# http_port 3128

# Запуск
sudo systemctl start squid
sudo systemctl enable squid
```

#### Использование Docker:

```bash
# Dockerfile
FROM ubuntu:22.04
RUN apt-get update && apt-get install -y tinyproxy
EXPOSE 8080
CMD ["tinyproxy", "-d"]

# Сборка и запуск
docker build -t proxy-server .
docker run -p 8080:8080 proxy-server
```

## 🔑 Учётные данные по умолчанию

**Admin Panel:**
- Username: `admin`
- Password: `admin123`

⚠️ **Измените пароль в production!**

## 📡 API Endpoints

### Общие (без аутентификации):

```
GET    /api/health                 # Проверка статуса сервера
GET    /api/proxies                # Получить доступные прокси
GET    /api/proxies/<id>           # Получить прокси по ID
POST   /api/vpn/toggle             # Включить/выключить VPN
POST   /api/vpn/connect            # Подключиться к VPN
POST   /api/vpn/location           # Изменить локацию
GET    /api/speed-test             # Проверить скорость
```

### Админ (с JWT токеном):

```
POST   /api/admin/login                        # Вход администратора
GET    /api/admin/verify-token                 # Проверка токена
GET    /api/admin/proxies                      # Все прокси (с деталями)
GET    /api/admin/proxies/<id>                 # Прокси по ID
POST   /api/admin/proxies                      # Создать прокси
PUT    /api/admin/proxies/<id>                 # Обновить прокси
DELETE /api/admin/proxies/<id>                 # Удалить прокси
GET    /api/admin/users                        # Все пользователи
GET    /api/admin/users/<id>                   # Пользователь по ID
PUT    /api/admin/users/<id>                   # Обновить пользователя
GET    /api/admin/statistics                   # Статистика системы
GET    /api/admin/settings                     # Параметры системы
POST   /api/admin/settings                     # Сохранить параметры
GET    /api/admin/logs                         # Системные логи
DELETE /api/admin/logs                         # Очистить логи
```

## 🔐 Безопасность

### Рекомендации:

1. **В Production:**
   - Измените `JWT_SECRET_KEY` на длинный случайный ключ
   - Используйте HTTPS для всех соединений
   - Установите правильные CORS origins
   - Используйте rate limiting
   - Включите логирование всех действий админа

2. **Аутентификация:**
   - Используйте сильные пароли
   - Рассмотрите 2FA для админа
   - Регулярно меняйте пароли

3. **Прокси:**
   - Используйте аутентификацию для прокси
   - Мониторьте использование прокси
   - Блокируйте подозрительную активность

## 📊 Мониторинг

Все действия логируются в БД:
- Подключение/отключение пользователей
- Изменения в конфигурации админом
- Ошибки и проблемы с прокси
- Активность системы

Логи доступны в админ-панели → "Логи системы"

## 🛠️ Troubleshooting

### Расширение не подключается к Backend:

1. Проверьте, что Backend запущен на `localhost:5000`
2. Проверьте CORS настройки в `.env`
3. Посмотрите логи браузера (F12 → Console)

### Админ не может залогиниться:

1. Убедитесь, что Backend работает
2. Проверьте username/password
3. Очистите localStorage: `localStorage.clear()`

### Прокси не работает:

1. Проверьте, доступен ли хост прокси
2. Проверьте правильность порта
3. Убедитесь, что прокси требует аутентификацию

## 📦 Зависимости

### Backend:
- Flask 2.3.3
- Flask-CORS 4.0.0
- Flask-JWT-Extended 4.5.2
- Gunicorn 21.2.0

### Extension:
- Chrome/Edge (Manifest V3)
- Firefox (WebExtensions API)

## 🤝 Contributing

Все контрибьюции приветствуются! Пожалуйста, создавайте pull requests.

## 📄 Лицензия

MIT License - смотрите файл LICENSE для деталей

## 📚 Полезные ссылки и репозитории

Этот проект основан на следующих технологиях и примерах:

### Браузерные расширения:
- [Chrome Extension Documentation](https://developer.chrome.com/docs/extensions/)
- [Web Extensions MDN](https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions)
- [awesome-chrome-extensions](https://github.com/learn-anything/awesome-chrome-extensions)

### Backend и API:
- [Flask Official Documentation](https://flask.palletsprojects.com/)
- [Flask-JWT-Extended](https://flask-jwt-extended.readthedocs.io/)
- [Flask-CORS](https://flask-cors.readthedocs.io/)
- [Full Stack Python Example](https://github.com/miguelgrinberg/flasgger)

### Прокси и VPN:
- [Squid Proxy](http://www.squid-cache.org/)
- [Tinyproxy Documentation](https://tinyproxy.github.io/)
- [Mitmproxy - Interactive HTTPS proxy](https://mitmproxy.org/)
- [Shadowsocks - Network tool](https://shadowsocks.org/)

### Примеры проектов:
- [Chrome Extension Boilerplate](https://github.com/lxieyang/chrome-extension-boilerplate-react)
- [Browser Extension Template](https://github.com/fregante/browser-extension-template)
- [Flask API Template](https://github.com/codingforentrepreneurs/Starting-an-API-w-Django-REST-Framework)
- [VPN Client Example](https://github.com/ProtonVPN/proton-vpn-cli)

### Дополнительные ресурсы:
- [OWASP Top 10 Security](https://owasp.org/www-project-top-ten/)
- [REST API Best Practices](https://restfulapi.net/)
- [Database Security](https://en.wikipedia.org/wiki/Database_security)

## ✉️ Поддержка

Для вопросов и предложений:
- Создайте Issue в репозитории
- Отправьте email на support@vpnmanager.local

---

**Версия:** 1.0.0
**Последнее обновление:** 2024-12-19
**Автор:** VPN Manager Team
