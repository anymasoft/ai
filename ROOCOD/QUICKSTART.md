# ⚡ VPN Manager - Быстрый старт (5 минут)

## 🚀 Вариант 1: Локальный запуск (для тестирования)

### Шаг 1: Установка Backend (2 мин)

```bash
cd ROOCOD/backend
python3 -m venv venv

# Linux/Mac:
source venv/bin/activate
# Или Windows:
# venv\Scripts\activate

pip install -r requirements.txt
python server.py
```

**Ожидаемый вывод:**
```
Starting VPN Manager Backend Server...
API running on localhost:5000
```

### Шаг 2: Установка расширения в браузер (2 мин)

#### Chrome/Edge:
1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика" (верхний правый угол)
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `ROOCOD/extension/`

#### Firefox:
1. Откройте `about:debugging#/runtime/this-firefox`
2. Нажмите "Загрузить временное дополнение"
3. Выберите файл `ROOCOD/extension/manifest.json`

### Шаг 3: Использование (1 мин)

1. Нажмите на иконку расширения в браузере
2. Откроется всплывающее окно VPN Manager
3. Выберите локацию из списка
4. Нажмите на toggle для включения VPN

**Готово!** 🎉

---

## 📱 Тестирование функций

### Обычный пользователь:

```
popup.html
├─ Toggle VPN (включить/выключить)
├─ Выбор локации
├─ Просмотр статуса
├─ Проверка скорости
└─ Параметры
```

### Администратор:

1. Нажмите кнопку "Админ-панель" в расширении
2. Откроется админ-панель в новой вкладке
3. Вход:
   - **Username:** `admin`
   - **Password:** `admin123`

**Функции админа:**
- ✅ Добавить прокси
- ✅ Редактировать прокси
- ✅ Удалить прокси
- ✅ Просмотреть пользователей
- ✅ Статистика
- ✅ Логи
- ✅ Параметры системы

---

## 🔧 API для тестирования

### Проверить здоровье сервера:
```bash
curl http://localhost:5000/api/health
```

### Получить список прокси:
```bash
curl http://localhost:5000/api/proxies
```

### Вход админа:
```bash
curl -X POST http://localhost:5000/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

---

## 📁 Структура проекта

```
ROOCOD/
├── extension/          ← Браузерное расширение
│   ├── popup.html      (UI для пользователя)
│   ├── background.js   (System Worker)
│   └── admin/          (Админ-панель)
├── backend/            ← Flask сервер
│   ├── server.py       (API endpoints)
│   └── database.py     (SQLite БД)
├── README.md           (Полная документация)
├── DEPLOYMENT.md       (Развёртывание на VPS)
├── ARCHITECTURE.md     (Техническая архитектура)
├── REFERENCES.md       (Ссылки и репозитории)
└── QUICKSTART.md       (Этот файл)
```

---

## ⚠️ Частые проблемы

### ❌ "Cannot connect to localhost:5000"

**Решение:**
```bash
# Убедитесь, что backend запущен в другом терминале:
cd ROOCOD/backend
python server.py
```

### ❌ "Admin login fails"

**Решение:**
```bash
# Очистите localStorage в браузере (F12 → Console):
localStorage.clear()
# Потом перезагрузите страницу
```

### ❌ "Расширение не загружается"

**Решение:**
1. Проверьте, что находитесь в папке `ROOCOD/extension/`
2. Убедитесь, что есть файл `manifest.json`
3. Попробуйте обновить расширение (кнопка обновления в chrome://extensions)

---

## 🎓 Следующие шаги

### Для тестирования:
1. Добавить новый прокси через админ-панель
2. Подключиться к нему в основном интерфейсе
3. Проверить, что расширение работает

### Для развёртывания на сервер:
```bash
# Прочитайте:
cat ROOCOD/DEPLOYMENT.md
```

### Для понимания архитектуры:
```bash
# Прочитайте:
cat ROOCOD/ARCHITECTURE.md
```

---

## 📚 Документация

| Файл | Описание |
|------|---------|
| **README.md** | Полный обзор проекта |
| **DEPLOYMENT.md** | Развёртывание на VPS (AWS, DigitalOcean и т.д.) |
| **ARCHITECTURE.md** | Техническая архитектура и дизайн |
| **REFERENCES.md** | Ссылки на использованные технологии |
| **QUICKSTART.md** | Этот файл (быстрый старт) |

---

## 🔐 Безопасность

⚠️ **Для Development только:**
- Пароль админа: `admin123`
- JWT Secret: `your-secret-key-change-in-production`

🔒 **Для Production:**
- Измените пароль админа в базе данных
- Установите надёжный JWT_SECRET_KEY в `.env`
- Используйте HTTPS с SSL сертификатом
- Включите логирование всех действий

---

## 💡 Советы

1. **Используйте разные вкладки браузера:**
   - Одна для тестирования расширения
   - Одна для админ-панели

2. **Просмотрите логи:**
   - Браузер: F12 → Console
   - Backend: Посмотрите terminal где запущен `python server.py`

3. **Добавьте тестовые прокси:**
   - Используйте бесплатные прокси сервисы для тестирования
   - Или создайте локальный прокси с Tinyproxy

---

## 🤝 Помощь

- **Ошибки и баги:** Откройте Issue в GitHub
- **Вопросы:** Посмотрите DEPLOYMENT.md или ARCHITECTURE.md
- **Улучшения:** Создайте Pull Request

---

## ✅ Checklist

- [ ] Клонировал репозиторий
- [ ] Установил зависимости Python
- [ ] Запустил Backend сервер
- [ ] Загрузил расширение в браузер
- [ ] Протестировал основные функции
- [ ] Залогинился в админ-панель
- [ ] Добавил тестовый прокси
- [ ] Подключился к VPN
- [ ] Проверил, что всё работает

---

**Поздравляем! VPN Manager готов к использованию!** 🎉

Для полной информации смотрите файлы документации.
